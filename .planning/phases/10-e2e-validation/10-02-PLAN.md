---
phase: 10
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - test/01-fresh-start.spec.ts
  - test/02-watchlist-crud.spec.ts
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "`test/01-fresh-start.spec.ts` exists and contains a single `test(...)` block that asserts: (a) all 10 default-seed tickers (AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V, NFLX) render as watchlist rows, (b) the header shows `$10,000.00` cash, and (c) at least one watchlist price cell stops being the `—` em-dash placeholder within ~10s of page load (proves SSE streaming is live)."
    - "`test/02-watchlist-crud.spec.ts` exists and contains a single `test(...)` block that uses Playwright's `request` fixture (NOT `page`) to POST `/api/watchlist {ticker: 'PYPL'}`, GET `/api/watchlist` to confirm `PYPL` appears in `items[].ticker`, then DELETE `/api/watchlist/PYPL` and asserts the response status is `removed` or `not_present`."
    - "Both specs run green across all three Playwright projects (chromium, firefox, webkit) inside the compose harness. Tested by running each spec individually via `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test {spec} --project={browser}` (3 browsers x 2 specs = 6 invocations all green) and via the full one-command harness."
    - "01-fresh-start uses the seed-only assertions from PATTERNS.md per-spec table (no mutation; ticker isolation N/A) and 02-watchlist-crud uses `PYPL` (NOT in the default seed) per D-08 to test the true add path without colliding with any other spec."
    - "Neither spec depends on prior test state; each owns its setup. Within one `up` invocation 02-watchlist-crud's PYPL add+remove leaves the watchlist back to the seed-only state for any later spec."
  artifacts:
    - path: "test/01-fresh-start.spec.ts"
      provides: "PLAN.md §12 row 1 — fresh-start scenario."
      contains: "import { test, expect } from '@playwright/test';"
    - path: "test/02-watchlist-crud.spec.ts"
      provides: "PLAN.md §12 row 2 — watchlist add+remove via REST through the running container."
      contains: "request.post"
  key_links:
    - from: "test/01-fresh-start.spec.ts"
      to: "frontend/src/components/terminal/WatchlistRow.tsx:46 (`<tr role=\"button\" aria-label=\"Select {ticker}\">`)"
      via: "Playwright `getByRole('button', { name: /^Select (...)$/ })` — the canonical user-facing locator strategy. Each watchlist row exposes itself as a button via `role` + `aria-label`."
      pattern: "Select AAPL"
    - from: "test/01-fresh-start.spec.ts (header cash assertion)"
      to: "frontend/src/components/terminal/Header.tsx (`data-testid=\"header-cash\"` from Plan 10-00)"
      via: "Plan 10-00 added `header-cash` and `header-total` test-ids to the value `<span>`s. If 10-00 ships before this plan, use `getByTestId('header-cash')`; otherwise fall back to `page.locator('header').getByText(/\\$10,000\\.00/)`."
      pattern: "header-cash"
    - from: "test/02-watchlist-crud.spec.ts"
      to: "backend/app/watchlist/routes.py (POST /api/watchlist returns {status: 'added'|'exists'}, DELETE /api/watchlist/{ticker} returns {status: 'removed'|'not_present'})"
      via: "Phase 4 idempotent CRUD — duplicates are 200 OK with a status-string discriminator, not 4xx. Spec asserts both `response.ok()` AND `response.json().status` matches the expected string set."
      pattern: "/api/watchlist"
---

<objective>
Land the first two of seven Phase 10 Playwright spec files: `01-fresh-start.spec.ts` (PLAN.md §12 row 1 — default seed + cash + streaming) and `02-watchlist-crud.spec.ts` (PLAN.md §12 row 2 — REST-driven watchlist add/remove). Both are read-only or REST-only; neither uses the TradeBar UI; neither writes long-lived state to the shared compose-run SQLite (02 leaves the watchlist back to seed-state).

Purpose: Closes 2 of the 7 §12 scenarios for TEST-04 — the simplest pair, picking up `data-testid` selectors from Plan 10-00 where helpful and falling back to `aria-label` / role selectors where 10-00 hasn't shipped yet.

Output: Two new TypeScript spec files under `test/`. No other files touched.

Wave 2 rationale: This plan is parallel-safe with 10-03, 10-04, 10-05 (each Wave 2 plan creates disjoint `*.spec.ts` files). All Wave 2 plans depend on 10-01 (foundation). 10-00 is parallel-safe-but-not-strictly-required (specs can fall back to aria-label selectors per RESEARCH.md). If the plan order is 10-00 ships first, use the `data-testid` selectors; otherwise use the documented fallbacks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-e2e-validation/10-CONTEXT.md
@.planning/phases/10-e2e-validation/10-RESEARCH.md
@.planning/phases/10-e2e-validation/10-PATTERNS.md
@.planning/phases/10-e2e-validation/10-VALIDATION.md
@.planning/phases/10-e2e-validation/10-01-PLAN.md
@frontend/src/components/terminal/WatchlistRow.tsx
@frontend/src/components/terminal/Header.tsx
@frontend/src/components/terminal/ConnectionDot.tsx
@backend/app/watchlist/routes.py
@backend/app/db/seed.py
@backend/app/market/seed_prices.py

<interfaces>
<!-- Reference selector inventory (PATTERNS.md + RESEARCH.md), spec-by-spec.
     Verify against the actual Header.tsx after Plan 10-00 ships. -->

### Default seed (verified backend/app/market/seed_prices.py + backend/app/db/seed.py):
AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V, NFLX (10 tickers).
users_profile.cash_balance = 10000.0.

### Watchlist row selector (verified frontend/src/components/terminal/WatchlistRow.tsx:45-46):
Each row is `<tr role="button" aria-label="Select {TICKER}">`. Stable across browsers.

### Header cash format (verified frontend/src/components/terminal/Header.tsx:15-20 formatMoney):
`Intl.NumberFormat('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })` formatted as `$10,000.00`.
After Plan 10-00 ships: `data-testid="header-cash"` on the cash value `<span>`.
Before Plan 10-00: use `page.locator('header').getByText(/^\$10,000\.00$/)`.

### Streaming proof (verified RESEARCH.md §01-fresh-start lines 408-411):
Each watchlist row renders price `'—'` (em-dash) when no tick has arrived yet. Once SSE delivers the first tick, the price text changes to a numeric dollar value. `expect(row).not.toContainText('—', { timeout: 10_000 })` proves SSE is alive.

### Watchlist API contract (verified backend/app/watchlist/routes.py — Phase 4):
- `GET /api/watchlist` -> `200 OK { items: [{ ticker, price, ... }, ...] }`
- `POST /api/watchlist` body `{ ticker: "PYPL" }` -> `200 OK { status: "added" | "exists", ticker: "PYPL" }`
- `DELETE /api/watchlist/PYPL` -> `200 OK { status: "removed" | "not_present", ticker: "PYPL" }`

Idempotent: duplicates return 200 with a status-string discriminator, NEVER 4xx.

### Reference test bodies (verbatim from RESEARCH.md §"Per-Scenario Spec Outlines")

**01-fresh-start.spec.ts** — RESEARCH.md lines 394-415 outlines the assertions:

```typescript
import { test, expect } from '@playwright/test';

const SEED_TICKERS = ['AAPL', 'GOOGL', 'MSFT', 'AMZN', 'TSLA', 'NVDA', 'META', 'JPM', 'V', 'NFLX'] as const;

test('fresh start: default watchlist + $10k cash + streaming prices', async ({ page }) => {
  await page.goto('/');

  // 1. All 10 seed tickers render as watchlist rows.
  for (const ticker of SEED_TICKERS) {
    await expect(
      page.getByRole('button', { name: `Select ${ticker}` }),
    ).toBeVisible({ timeout: 10_000 });
  }

  // 2. Header cash reads $10,000.00.
  // After Plan 10-00 ships:
  await expect(page.getByTestId('header-cash')).toHaveText('$10,000.00');
  // Fallback (if 10-00 hasn't shipped yet, replace the line above with):
  // await expect(page.locator('header').getByText(/^\$10,000\.00$/)).toBeVisible();

  // 3. At least one watchlist row's price cell leaves the '—' placeholder
  //    within 10s — proves SSE is streaming live ticks.
  const aaplRow = page.getByRole('button', { name: 'Select AAPL' });
  await expect(aaplRow).not.toContainText('—', { timeout: 10_000 });
});
```

**02-watchlist-crud.spec.ts** — RESEARCH.md lines 433-444 (REST strategy A):

```typescript
import { test, expect } from '@playwright/test';

test('watchlist CRUD: add then remove PYPL via REST', async ({ request }) => {
  // POST /api/watchlist -> { status: 'added' | 'exists' }
  const add = await request.post('/api/watchlist', { data: { ticker: 'PYPL' } });
  expect(add.ok()).toBeTruthy();
  const addBody = await add.json();
  expect(addBody.status).toMatch(/^(added|exists)$/);

  // GET /api/watchlist -> verify PYPL is in items
  const list = await request.get('/api/watchlist');
  expect(list.ok()).toBeTruthy();
  const listBody = await list.json();
  const tickers = (listBody.items as Array<{ ticker: string }>).map((i) => i.ticker);
  expect(tickers).toContain('PYPL');

  // DELETE /api/watchlist/PYPL -> { status: 'removed' | 'not_present' }
  const del = await request.delete('/api/watchlist/PYPL');
  expect(del.ok()).toBeTruthy();
  const delBody = await del.json();
  expect(delBody.status).toMatch(/^(removed|not_present)$/);
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write `test/01-fresh-start.spec.ts`</name>
  <read_first>
    - frontend/src/components/terminal/WatchlistRow.tsx (lines 45-46 — `role="button" aria-label="Select {TICKER}"` is the row selector contract)
    - frontend/src/components/terminal/Header.tsx (lines 38-53 — current shape; if Plan 10-00 has shipped, the value spans now have `data-testid="header-total"` / `data-testid="header-cash"`)
    - backend/app/db/seed.py + backend/app/market/seed_prices.py (lines 5-39 — confirm the 10-ticker default seed list)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 394-415 — full assertion + selector strategy + edge cases)
  </read_first>
  <action>
    Write `test/01-fresh-start.spec.ts` based on the verbatim shape in the `<interfaces>` block above.

    Specific decisions for the executor:
    1. **Imports:** Single line — `import { test, expect } from '@playwright/test';`. No render helpers, no fixtures beyond the default `page`.
    2. **Seed list constant:** Declare `const SEED_TICKERS = ['AAPL', 'GOOGL', 'MSFT', 'AMZN', 'TSLA', 'NVDA', 'META', 'JPM', 'V', 'NFLX'] as const;` at module scope.
    3. **`page.goto('/')`:** No path prefix; `baseURL` from playwright.config.ts resolves to `http://app:8000/`.
    4. **Watchlist row assertion:** Loop over `SEED_TICKERS` and assert each row is visible via `getByRole('button', { name: 'Select ' + ticker })`. Use `{ timeout: 10_000 }` to absorb WebKit first-paint slowness (RESEARCH.md Pitfall 7).
    5. **Header cash assertion:** Use `page.getByTestId('header-cash')` IF Plan 10-00 has shipped (verify by `grep -q 'data-testid="header-cash"' frontend/src/components/terminal/Header.tsx` before writing). Otherwise use the fallback `page.locator('header').getByText(/^\$10,000\.00$/)` and document the fallback choice in the SUMMARY.
    6. **Streaming proof:** Pick AAPL (any seed ticker works) and assert the row text DOES NOT contain `'—'` within 10s. The em-dash is `—` — copy it directly; it's Unicode punctuation, NOT an emoji (CLAUDE.md no-emoji rule preserved).
    7. **Single `test()` block.** No `describe()`, no `beforeEach()`. Per PATTERNS.md "Selector hierarchy" + RESEARCH.md spec outline, the entire scenario fits in one test.
    8. **No `test.only`, no `test.skip`.** `forbidOnly: !!process.env.CI` in playwright.config.ts catches `.only`, but don't write it in the first place.
    9. **No emojis.** Test names, comments, console.log: plain text.
    10. **No `waitForTimeout`.** Auto-wait via `expect(...).toBeVisible({ timeout })` and `not.toContainText({ timeout })` — RESEARCH.md "Selector hierarchy" anti-patterns.

    **Test name:** `'fresh start: default watchlist + $10k cash + streaming prices'`. The string `$10k` is plain ASCII; do NOT use emoji or special money glyphs.
  </action>
  <verify>
    <automated>test -f test/01-fresh-start.spec.ts && grep -q "import { test, expect } from '@playwright/test';" test/01-fresh-start.spec.ts && grep -q "SEED_TICKERS" test/01-fresh-start.spec.ts && grep -q "Select AAPL" test/01-fresh-start.spec.ts && grep -q "10,000.00" test/01-fresh-start.spec.ts && grep -q "not.toContainText" test/01-fresh-start.spec.ts && cd test && npx playwright test 01-fresh-start.spec.ts --list 2>&1 | grep -qE "(fresh start|1 test)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/01-fresh-start.spec.ts` exists and parses as valid TypeScript (Playwright `--list` flag exits 0).
    - Contains `SEED_TICKERS` const with all 10 default tickers.
    - Loops `getByRole('button', { name: 'Select ' + ticker })` over the 10 tickers (or equivalent — `name: \`Select ${ticker}\`` with template literal is also acceptable).
    - Asserts `'$10,000.00'` text using either `getByTestId('header-cash')` (preferred, post-10-00) or `page.locator('header').getByText(...)` (fallback).
    - Streaming proof uses `not.toContainText('—', { timeout })` with timeout >= 10_000.
    - Single `test(...)` block; no `describe`, no `beforeEach`, no `test.only`, no `test.skip`.
    - No emojis; no `waitForTimeout`.
    - Smoke run: `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 01-fresh-start.spec.ts --project=chromium` exits 0 (single browser smoke; full 3-project run validated in Task 3).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write `test/02-watchlist-crud.spec.ts`</name>
  <read_first>
    - backend/app/watchlist/routes.py (Phase 4 — confirm POST returns `{status:"added"|"exists"}`, DELETE returns `{status:"removed"|"not_present"}`, both 200 OK on idempotent calls)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 417-452 — Strategy A REST-via-`request` reasoning, full code shape)
    - .planning/phases/10-e2e-validation/10-PATTERNS.md (lines 282-294 — REST CRUD invariants; D-08 picks `PYPL` outside the seed)
  </read_first>
  <action>
    Write `test/02-watchlist-crud.spec.ts` based on the verbatim shape in the `<interfaces>` block above.

    Specific decisions for the executor:
    1. **Imports:** `import { test, expect } from '@playwright/test';` — single line, same as 01.
    2. **Use the `request` fixture, NOT `page`.** Playwright's `request` fixture issues HTTP from the test runner directly. No browser process spawned. Resolves against `baseURL: http://app:8000` (from playwright.config.ts).
    3. **Ticker choice:** `'PYPL'` — confirmed NOT in the default seed (AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V, NFLX). RESEARCH.md / PATTERNS.md per-spec tickers: 03-buy uses NVDA, 04-sell uses JPM, 05-portfolio-viz uses META, 06-chat uses AMZN. PYPL has zero collision.
    4. **POST request shape:** `request.post('/api/watchlist', { data: { ticker: 'PYPL' } })`. The `data:` field auto-serialises to JSON with `Content-Type: application/json`.
    5. **GET assertion:** Decode `items` array (NOT just `tickers` — the API response shape is `{ items: [{ ticker, price, ... }, ...] }` per Phase 4 routes). Map to ticker strings, assert `expect(tickers).toContain('PYPL')`.
    6. **DELETE assertion:** Path-parameter form `/api/watchlist/PYPL` (no body). Status string regex `/^(removed|not_present)$/` covers idempotency.
    7. **Single `test()` block.** No `describe`, no fixtures beyond `request`.
    8. **No emojis.** Test names + comments are plain text.
    9. **No UI assertions.** This spec is intentionally REST-only per RESEARCH.md Strategy A — the watchlist UI has no add/remove form (Phase 7/8 scope) and chat-driven add/remove is exercised in 06-chat.

    **Test name:** `'watchlist CRUD: add then remove PYPL via REST'`.
  </action>
  <verify>
    <automated>test -f test/02-watchlist-crud.spec.ts && grep -q "import { test, expect } from '@playwright/test';" test/02-watchlist-crud.spec.ts && grep -q "request.post('/api/watchlist'" test/02-watchlist-crud.spec.ts && grep -q "PYPL" test/02-watchlist-crud.spec.ts && grep -q "request.delete('/api/watchlist/PYPL')" test/02-watchlist-crud.spec.ts && grep -qE "(added|exists)" test/02-watchlist-crud.spec.ts && grep -qE "(removed|not_present)" test/02-watchlist-crud.spec.ts && cd test && npx playwright test 02-watchlist-crud.spec.ts --list 2>&1 | grep -qE "(watchlist CRUD|1 test)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/02-watchlist-crud.spec.ts` exists and parses as valid TypeScript.
    - Uses Playwright's `request` fixture (NOT `page`) — `async ({ request })`.
    - Issues `request.post('/api/watchlist', { data: { ticker: 'PYPL' } })`.
    - Asserts `add.ok()` AND `add.json().status` matches `/^(added|exists)$/`.
    - Issues `request.get('/api/watchlist')` and asserts `'PYPL'` is in `items.map(i => i.ticker)`.
    - Issues `request.delete('/api/watchlist/PYPL')` and asserts `del.ok()` AND status matches `/^(removed|not_present)$/`.
    - Single `test(...)` block.
    - Ticker is `'PYPL'` (NOT in seed; D-08 isolation).
    - No emojis; no UI selectors (`page.*`, `getByRole`, `getByText` are all absent — this is a REST-only spec).
    - Smoke run: `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 02-watchlist-crud.spec.ts --project=chromium` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full 3-browser harness gate for 01 and 02</name>
  <read_first>
    - test/01-fresh-start.spec.ts (just-written file)
    - test/02-watchlist-crud.spec.ts (just-written file)
    - test/playwright.config.ts (confirm three projects defined)
    - test/docker-compose.test.yml (confirm `playwright` service ready)
  </read_first>
  <action>
    Run the full canonical harness ONCE against the just-built `app` image to validate both new specs across all three browsers (chromium, firefox, webkit). With only 01 and 02 in `test/`, Playwright will run `2 specs x 3 projects = 6 tasks` distributed across `workers: 3`.

    Command (from repo root):
    `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright`

    Expected:
    - All 6 (spec, project) pairs pass.
    - Stdout includes `6 passed` (or `Total: 6` followed by `passed: 6`) — exact wording depends on Playwright 1.59.1's reporter formatting.
    - Compose exits 0.
    - HTML report regenerated at `test/playwright-report/index.html`.

    **If any (spec, project) fails:** Read the HTML report (or the per-test trace at `test/test-results/`) to identify the failing browser. Common causes:
    - WebKit first-paint slowness: bump the timeout in 01-fresh-start to 15s.
    - Header cash selector mismatch (10-00 not yet shipped): switch to the fallback `page.locator('header').getByText(...)`.
    - PYPL already in watchlist from a previous flake-recovery run: D-06 anonymous volume per `up` should prevent this; if it persists, re-run `up --build` to force a fresh app container.
    - Cold-cache em-dash on AAPL: bump the streaming-proof timeout.

    Apply the minimal fix and re-run. DO NOT add `test.only` or `test.skip` to "iterate faster" — the suite must run as written across all 3 browsers.
  </action>
  <verify>
    <automated>docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0.
    - Playwright report (in compose stdout) shows all 6 (spec, project) pairs passed.
    - `test/playwright-report/index.html` exists and is regenerated (size > 0 bytes; mtime newer than the start of the run).
    - No flakes attributable to spec quality (transient infrastructure flakes — e.g. one-off network hiccup — are acceptable; rerun once before declaring failure).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ App API | Both specs cross this boundary. 01 reads only (GET-equivalents via the rendered UI). 02 mutates via REST. The compose-internal anonymous SQLite volume isolates state to one `up` invocation, so 02's PYPL add+remove is atomic for the run. |
| Test browser ↔ Real DOM | 01 reads rendered DOM. The `aria-label="Select {ticker}"` and `data-testid="header-cash"` selectors are stable across the three engines per RESEARCH.md cross-browser notes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | T (Tampering) | Production frontend / backend | mitigate | Both specs are read-only or REST-only — neither modifies code, deployment artefacts, or persistence beyond the per-`up` anonymous volume. The PYPL add+remove is symmetric (add then remove) so the watchlist returns to seed-only state by the test's end. |
| T-10-02-02 | I (Information disclosure) | Test report screenshots | accept | Playwright's `screenshot: 'only-on-failure'` config means screenshots are taken only when an assertion fails. The captured DOM contains only seeded demo data (10 default tickers, $10k starting cash, mock LLM enabled) — no PII, no secrets. Reports are gitignored per Plan 10-01. |
| T-10-02-03 | D (Denial of service) | Test runtime budget | accept | Per-test timeout 30s (config); two specs across three browsers ≈ 60-180s total. Well under the typical 5-10 minute CI budget. |
| T-10-02-04 | E (Elevation of privilege) | REST API attack surface | accept | The 02 spec hits only Phase 4's documented `POST /api/watchlist` and `DELETE /api/watchlist/{ticker}` endpoints with valid PYPL ticker shape (`^[A-Z][A-Z0-9.]{0,9}$`). No auth bypass attempts, no boundary fuzzing — Phase 10 is functional E2E, not security testing. Boundary fuzzing is out of scope per CONTEXT.md "out of scope" list. |

</threat_model>

<verification>
- `test/01-fresh-start.spec.ts` and `test/02-watchlist-crud.spec.ts` exist and pass `cd test && npx playwright test --list` (Tasks 1-2 grep gates).
- Both specs run green across all three Playwright projects in the harness (Task 3 gate).
- `test/playwright-report/index.html` is regenerated by the Task 3 run (proves the html reporter wired correctly in Plan 10-01).
- No files modified outside `test/01-fresh-start.spec.ts` and `test/02-watchlist-crud.spec.ts` (`git diff --name-only test/` returns exactly those two files).
</verification>

<success_criteria>
- [ ] `test/01-fresh-start.spec.ts` asserts the 10-ticker seed, `$10,000.00` cash, and streaming-stops-em-dash, all in one `test(...)` block.
- [ ] `test/02-watchlist-crud.spec.ts` add+remove PYPL via REST in one `test(...)` block using the `request` fixture.
- [ ] Both pass on chromium, firefox, AND webkit inside the canonical compose harness.
- [ ] No emojis, no `waitForTimeout`, no `test.only`/`test.skip`.
- [ ] Tickers chosen per D-08 — 01 reads the seed only (no mutation); 02 uses PYPL (NOT in seed).
- [ ] Compose harness one-command run exits 0; HTML report regenerated.
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-validation/10-02-SUMMARY.md` recording the two spec files written, which selector strategy 01-fresh-start used for the cash assertion (testId vs locator fallback — depending on whether Plan 10-00 has shipped), the green Task 3 harness output, and confirmation that the run took less than the 30s per-test budget per (spec, project) pair.
</output>
