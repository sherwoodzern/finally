---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/package.json
  - test/package-lock.json
  - test/playwright.config.ts
  - test/docker-compose.test.yml
  - test/README.md
  - .gitignore
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "`test/` directory exists at the repo root with `package.json` (pinning `@playwright/test` to exact `1.59.1`), `package-lock.json` (committed, generated by `npm install`), `playwright.config.ts` (full body per RESEARCH.md lines 311-379), `docker-compose.test.yml` (full body per RESEARCH.md lines 209-271), and `README.md` (two paragraphs)."
    - "`docker compose -f test/docker-compose.test.yml config` parses successfully (compose YAML well-formed)."
    - "`docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` starts the `app` service (built from repo-root Dockerfile via `build: context: ..`), waits for `/api/health` via the compose-side HEALTHCHECK, then starts the `playwright` service which runs `npm ci` followed by `npx playwright test` against `http://app:8000`. With NO spec files yet (Wave 1 has no specs), Playwright reports `0 tests` and exits 0; the compose harness exits 0."
    - "`.gitignore` has three new entries — `test/playwright-report/`, `test/test-results/`, `test/node_modules/` — appended to the existing file in a clearly-labelled section. `test/package.json`, `test/package-lock.json`, `test/playwright.config.ts`, `test/docker-compose.test.yml`, `test/README.md` are tracked by git."
    - "No changes to `Dockerfile`, `.dockerignore`, `backend/`, `frontend/`, `scripts/`, `docs/`, `.env.example`, or repo-root `README.md`."
    - "Selected Playwright Docker image tag (`mcr.microsoft.com/playwright:v1.59.1-jammy` per RESEARCH.md A2; planner-verifies pull) is in the compose file and matches the npm `@playwright/test` exact pin in `test/package.json`."
  artifacts:
    - path: "test/package.json"
      provides: "Self-contained Node project for the E2E harness; pins `@playwright/test` to exact `1.59.1`."
      contains: "@playwright/test"
    - path: "test/package-lock.json"
      provides: "Deterministic lockfile generated by `npm install` once and committed; consumed by `npm ci` inside the Playwright container."
      contains: "@playwright/test"
    - path: "test/playwright.config.ts"
      provides: "Playwright runner config — three browser projects (chromium/firefox/webkit), workers=3, fullyParallel=false, retries=0/1, baseURL http://app:8000, list+html reporters, outputDir test-results."
      contains: "defineConfig"
    - path: "test/docker-compose.test.yml"
      provides: "Two-service compose file: `app` (build: context: .., LLM_MOCK=true, compose-side HEALTHCHECK against /api/health) + `playwright` (mcr.microsoft.com/playwright:v1.59.1-jammy, depends_on app service_healthy, mounts ../test:/work, runs npm ci && npx playwright test)."
      contains: "context: .."
    - path: "test/README.md"
      provides: "Two-paragraph harness doc — exact one-command invocation, where to find the HTML report, single-spec/single-browser debugging recipe."
      contains: "docker compose -f test/docker-compose.test.yml up"
    - path: ".gitignore"
      provides: "Existing `.gitignore` (211 lines) appended with three test-only entries."
      contains: "test/playwright-report/"
  key_links:
    - from: "test/docker-compose.test.yml (`app.build.context: ..`)"
      to: "Dockerfile (repo root)"
      via: "Compose builds `finally:latest` from local source per D-04 — never a registry pull. `..` is host-relative to the compose file's directory (i.e. repo root)."
      pattern: "context: \\.\\."
    - from: "test/docker-compose.test.yml (`app.environment.LLM_MOCK: \"true\"`)"
      to: "backend/app/chat/mock.py:9-55 (LLM_MOCK gated mock client)"
      via: "Phase 5 mock-mode env switch — single env override is the entire delta from production runtime per CONTEXT.md D-05."
      pattern: "LLM_MOCK"
    - from: "test/docker-compose.test.yml (`playwright.depends_on.app.condition: service_healthy`)"
      to: "test/docker-compose.test.yml (`app.healthcheck` — `python3 -c \"import urllib.request...\"` against /api/health)"
      via: "Compose-side healthcheck only (D-10) — production Dockerfile deliberately omits HEALTHCHECK per Phase 9 D-08."
      pattern: "service_healthy"
    - from: "test/playwright.config.ts (`baseURL: process.env.BASE_URL ?? 'http://app:8000'`)"
      to: "test/docker-compose.test.yml (`playwright.environment.BASE_URL: \"http://app:8000\"` + service name `app`)"
      via: "Compose-internal DNS — `app` service name resolves to the running app container in the compose project's default network. Belt-and-suspenders BASE_URL env override + hardcoded fallback covers ad-hoc local runs against localhost:8000."
      pattern: "http://app:8000"
    - from: ".gitignore (`test/playwright-report/`, `test/test-results/`, `test/node_modules/`)"
      to: "test/playwright.config.ts (`outputFolder: 'playwright-report'`, `outputDir: 'test-results'`)"
      via: "Run-artefact paths produced by Playwright must NOT be committed; node_modules belt-and-suspenders for IDE-driven local installs."
      pattern: "playwright-report"
---

<objective>
Stand up the Phase 10 E2E harness foundation under a brand-new `test/` directory at the repo root: package manifest, lockfile, Playwright config, docker-compose file, README, and `.gitignore` updates. After this plan ships, `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` is the canonical one-command invocation (CONTEXT.md D-03) and runs to completion — even though Wave 2 has not yet added any spec files (Playwright reports `0 tests` and exits 0).

Purpose: Closes ROADMAP Phase 10 SC#1 (compose harness up + LLM_MOCK + Playwright service co-located) and most of TEST-03 (the harness). Wave 2 plans land specs that are picked up automatically via `testMatch: /\d{2}-.+\.spec\.ts$/` — no further config edits required to add specs.

Output: Six new/modified files. All other 7 spec files in `test/` are produced by Wave 2 plans (10-02 through 10-05).

Critical scope boundary: Phase 10 is "purely additive" except for the documented Wave 0 frontend test-id additions (10-00) and `.gitignore` (this plan). NO touches to `Dockerfile`, `.dockerignore`, `backend/`, `frontend/` source, `scripts/`, `docs/`, `.env.example`, or repo-root `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-e2e-validation/10-CONTEXT.md
@.planning/phases/10-e2e-validation/10-RESEARCH.md
@.planning/phases/10-e2e-validation/10-PATTERNS.md
@.planning/phases/10-e2e-validation/10-VALIDATION.md
@planning/PLAN.md
@Dockerfile
@.dockerignore
@.gitignore
@scripts/start_mac.sh
@frontend/vitest.config.mts
@frontend/package.json
@backend/app/chat/mock.py

<interfaces>
<!-- Verbatim file bodies from RESEARCH.md. The executor copies these into the
     new files; no invention or substitution beyond what each task specifies. -->

### test/package.json (verbatim, RESEARCH.md lines 185-198)

```json
{
  "name": "finally-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Phase 10 E2E harness — runs against finally:latest via docker compose",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1"
  }
}
```

### test/playwright.config.ts (verbatim, RESEARCH.md lines 311-379)

The full body is in RESEARCH.md lines 311-379. Critical invariants (must be present in the written file, all verifiable by grep):
- `import { defineConfig, devices } from '@playwright/test';`
- `testDir: '.'`
- `testMatch: /\d{2}-.+\.spec\.ts$/`
- `retries: process.env.CI ? 1 : 0`
- `workers: 3`
- `fullyParallel: false`
- `forbidOnly: !!process.env.CI`
- `reporter: [['list'], ['html', { open: 'never', outputFolder: 'playwright-report' }]]`
- `baseURL: process.env.BASE_URL ?? 'http://app:8000'`
- `trace: 'on-first-retry'`, `screenshot: 'only-on-failure'`, `video: 'retain-on-failure'`, `actionTimeout: 10_000`
- `timeout: 30_000`
- `outputDir: 'test-results'`
- Three projects: `{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }`, `{ name: 'firefox', use: { ...devices['Desktop Firefox'] } }`, `{ name: 'webkit', use: { ...devices['Desktop Safari'] } }`
- NO `setupFiles` field (jsdom-specific; irrelevant in real browsers per PATTERNS.md line 197)

### test/docker-compose.test.yml (verbatim, RESEARCH.md lines 209-271)

The full YAML body is in RESEARCH.md lines 209-271. Critical invariants (must be present, verifiable):
- Top-level `services:` with two children: `app` and `playwright`
- `services.app.build.context: ..` and `services.app.build.dockerfile: Dockerfile` (D-04)
- `services.app.environment.LLM_MOCK: "true"` (D-05; quoted string)
- `services.app.environment.OPENROUTER_API_KEY: ""` and `MASSIVE_API_KEY: ""` (empty strings; selects SimulatorDataSource)
- `services.app.environment.DB_PATH: /app/db/finally.db` (matches Dockerfile line 53)
- `services.app.environment.PYTHONUNBUFFERED: "1"`
- NO `services.app.volumes:` block (D-06 — anonymous volume per `up` for clean SQLite)
- `services.app.healthcheck.test`: the `python3 -c "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:8000/api/health', timeout=2).status==200 else 1)"` one-liner (curl absent from python:3.12-slim per RESEARCH.md lines 300-304)
- `services.app.healthcheck.interval: 5s`, `timeout: 3s`, `retries: 12`, `start_period: 15s`
- `services.playwright.image: mcr.microsoft.com/playwright:v1.59.1-jammy`
- `services.playwright.depends_on.app.condition: service_healthy` (D-10)
- `services.playwright.working_dir: /work`
- `services.playwright.volumes: [ "../test:/work" ]` (host-relative to compose file directory)
- `services.playwright.environment.BASE_URL: "http://app:8000"`, `PLAYWRIGHT_BROWSERS_PATH: /ms-playwright`, `CI: "1"`
- `services.playwright.command: sh -c "npm ci && npx playwright test"`
- NO top-level `volumes:` named-volume block (D-06)
- NO `version:` key (modern compose v2 ignores it; RESEARCH YAML omits it)

### .gitignore additions (verbatim, RESEARCH.md lines 997-1002)

Append to the END of the existing `.gitignore` (after `__marimo__/` on line 211):

```gitignore

# Phase 10 E2E artefacts (Playwright)
test/playwright-report/
test/test-results/
test/node_modules/
```

### test/README.md skeleton

Two paragraphs only. No emojis, no extra headings beyond a single H1. Required content:
- The exact one-command invocation: `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright`
- HTML report path after a run: `test/playwright-report/index.html`
- Single-spec / single-browser debugging recipe (PATTERNS.md "Quick run command"):
  `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 01-fresh-start.spec.ts --project=chromium`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create `test/` directory + `test/package.json` + `test/package-lock.json`</name>
  <read_first>
    - frontend/package.json (shape analog — confirms `name`, `private`, `version`, `scripts.test`, `devDependencies` field shapes)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 144-203 — pinned versions + recommended package.json body)
  </read_first>
  <action>
    1. `mkdir -p test/` at the repo root.
    2. Write `test/package.json` verbatim from the `<interfaces>` block above. Pin `"@playwright/test": "1.59.1"` exact — NO caret, NO `^`. Reproducibility wins (RESEARCH.md line 244).
    3. Generate the lockfile by running `cd test && npm install` (NOT `npm ci` — `ci` requires a pre-existing lockfile). This downloads `@playwright/test@1.59.1` and produces `test/package-lock.json`.
    4. Verify the resulting `test/node_modules/` directory exists locally (will be gitignored in Task 4) and that `test/package-lock.json` references `@playwright/test` exact `1.59.1`.
    5. Commit `test/package.json` + `test/package-lock.json`. DO NOT commit `test/node_modules/`.

    **DO NOT** introduce a root-level `package.json`. **DO NOT** modify `frontend/package.json`. The harness is self-contained under `test/` per RESEARCH.md "Recommended install location" (lines 173-181).
  </action>
  <verify>
    <automated>test -f test/package.json && test -f test/package-lock.json && grep -q '"@playwright/test": "1.59.1"' test/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `test/package.json` exists, contains `"name": "finally-e2e"`, `"private": true`, `"version": "0.0.0"`, `"scripts": { "test": "playwright test" }`, and `"devDependencies": { "@playwright/test": "1.59.1" }` (exact, no caret).
    - `test/package-lock.json` exists and references `@playwright/test` version `1.59.1`.
    - `test/node_modules/` exists locally (created by `npm install`) but is NOT staged for commit.
    - No new files or modifications outside `test/`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create `test/playwright.config.ts`</name>
  <read_first>
    - frontend/vitest.config.mts (shape-only analog — confirms `defineConfig` from framework, ESM single-default-export style)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 306-388 — full TS body draft + worker-semantics confirmation)
    - .planning/phases/10-e2e-validation/10-CONTEXT.md (D-02, D-07, D-09 — locked decisions for projects, workers, baseURL)
  </read_first>
  <action>
    Write `test/playwright.config.ts` VERBATIM from RESEARCH.md lines 311-379 (the body is summarised in the `<interfaces>` block above). Copy line-for-line.

    DO NOT add a `setupFiles` field — the Vitest jsdom-specific setup file is irrelevant to a real browser per PATTERNS.md "DO NOT" line 197.

    Smoke-syntax-check after writing: `cd test && npx playwright test --list`. With no specs yet (testMatch regex matches `\d{2}-.+\.spec\.ts$`, none exist), Playwright should print "Listing 0 tests" or print a project line for each browser without errors. If TypeScript syntax is broken or a required field is missing, this command fails — fix the file and re-run.
  </action>
  <verify>
    <automated>test -f test/playwright.config.ts && cd test && npx playwright test --list 2>&1 | grep -qE "(Listing|0 tests|chromium|Total: 0)" && cd ..</automated>
  </verify>
  <acceptance_criteria>
    - `test/playwright.config.ts` exists.
    - Contains: `import { defineConfig, devices } from '@playwright/test';` (single import line).
    - Contains: `testDir: '.'`, `testMatch: /\d{2}-.+\.spec\.ts$/`.
    - Contains: `workers: 3` AND `fullyParallel: false` (both — this is the D-07 intent realisation per RESEARCH.md §worker-semantics).
    - Contains: `retries: process.env.CI ? 1 : 0` and `forbidOnly: !!process.env.CI`.
    - Three projects: `{ name: 'chromium', ... }`, `{ name: 'firefox', ... }`, `{ name: 'webkit', ... }` using `devices['Desktop Chrome'/'Desktop Firefox'/'Desktop Safari']`.
    - `baseURL: process.env.BASE_URL ?? 'http://app:8000'`.
    - Reporter array includes `['list']` and `['html', { open: 'never', outputFolder: 'playwright-report' }]`.
    - `outputDir: 'test-results'`, `timeout: 30_000`, `actionTimeout: 10_000`.
    - NO `setupFiles` field anywhere in the file (`grep -c setupFiles test/playwright.config.ts` returns 0).
    - `cd test && npx playwright test --list` exits 0 (proves config parses + TypeScript is valid).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create `test/docker-compose.test.yml`</name>
  <read_first>
    - Dockerfile (whole file — confirms WORKDIR /app/backend, EXPOSE 8000, STOPSIGNAL SIGINT, ENV DB_PATH=/app/db/finally.db, VOLUME /app/db, no HEALTHCHECK)
    - .dockerignore (line 43 — confirms `test/` already excluded from production build context — Phase 10 files won't pollute the production image)
    - scripts/start_mac.sh (lines 52-57 — canonical `docker run` arg set; compose `app` service mirrors this with one delta `LLM_MOCK=true` and no host volume mount per CONTEXT.md D-04, D-05, D-06)
    - backend/app/chat/mock.py (lines 9-55 — confirms the `LLM_MOCK=true` env switch and trigger-phrase regexes that drive the mock client)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 205-304 — full YAML body + healthcheck rationale + key choreography diagram)
  </read_first>
  <action>
    Write `test/docker-compose.test.yml` VERBATIM from RESEARCH.md lines 209-271. The required invariants are summarised in the `<interfaces>` block above.

    **Tag-verification step (per RESEARCH.md A2):** Before finalizing, run `docker pull mcr.microsoft.com/playwright:v1.59.1-jammy`. If it succeeds, keep the `-jammy` tag. If it 404s, fall back to `mcr.microsoft.com/playwright:v1.59.1-noble` (one-line change) and document the substitution in the SUMMARY. The npm `@playwright/test` version `1.59.1` is verified — only the OS variant tag may need adjustment.

    **DO NOT:**
    - Add a `version: '3.x'` top-level key (modern Compose v2 ignores it; RESEARCH YAML omits it).
    - Add a top-level `volumes:` block for named volumes (D-06 mandates anonymous-only).
    - Add a `volumes:` mapping under `services.app` for `/app/db` (D-06 — anonymous volume each `up`).
    - Add `ports:` for `app` UNLESS the planner needs ad-hoc local debugging; PATTERNS.md does not require it (the playwright container reaches `app` over the compose default network).
    - Modify `Dockerfile`, `.dockerignore`, or `scripts/start_mac.sh` (out of scope per CONTEXT.md "in scope" boundary lines 11-15).
  </action>
  <verify>
    <automated>test -f test/docker-compose.test.yml && docker compose -f test/docker-compose.test.yml config >/dev/null 2>&1 && grep -q "context: \\.\\." test/docker-compose.test.yml && grep -q 'LLM_MOCK: "true"' test/docker-compose.test.yml && grep -q "service_healthy" test/docker-compose.test.yml && grep -q "mcr.microsoft.com/playwright:v1.59.1" test/docker-compose.test.yml && grep -q "../test:/work" test/docker-compose.test.yml</automated>
  </verify>
  <acceptance_criteria>
    - `test/docker-compose.test.yml` exists.
    - `docker compose -f test/docker-compose.test.yml config` exits 0 (YAML is well-formed and compose-valid).
    - Contains `services.app.build.context: ..` (NOT a registry image — D-04).
    - Contains `services.app.environment.LLM_MOCK: "true"` (D-05).
    - Contains `services.app.environment.OPENROUTER_API_KEY: ""` and `MASSIVE_API_KEY: ""` (empty strings).
    - Contains `services.app.healthcheck.test` with `python3` + `urllib.request` + `/api/health` (NOT curl).
    - Contains `services.app.healthcheck.start_period: 15s` and `retries: 12`.
    - **Does NOT contain** any `services.app.volumes:` block (verify by reading the YAML — D-06).
    - **Does NOT contain** any top-level `volumes:` key.
    - Contains `services.playwright.image: mcr.microsoft.com/playwright:v1.59.1-jammy` (or `-noble` if the tag-verification step required substitution — substitution must be documented in the SUMMARY).
    - Contains `services.playwright.depends_on.app.condition: service_healthy`.
    - Contains `services.playwright.volumes: [ "../test:/work" ]`.
    - Contains `services.playwright.environment.BASE_URL: "http://app:8000"`.
    - Contains `services.playwright.command: sh -c "npm ci && npx playwright test"`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Append `.gitignore` entries + create `test/README.md` + harness smoke run</name>
  <read_first>
    - .gitignore (existing 211 lines — confirm append target; section-comment style per `.gitignore:1-3, 33-35, 49-51`)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 993-1019 — full additions + intentional exclusions)
    - planning/PLAN.md §12 (the seven scenarios — README references the one-command UX they test)
    - docs/DOCKER.md (Phase 9 D-14 user-facing voice — README mirrors that two-paragraph style)
  </read_first>
  <action>
    **4a — Append to `.gitignore`:** Add an empty line, then a section comment, then three entries at the END of the existing `.gitignore`:
    ```gitignore

    # Phase 10 E2E artefacts (Playwright)
    test/playwright-report/
    test/test-results/
    test/node_modules/
    ```
    DO NOT touch any existing line. DO NOT add `test/package.json`, `test/package-lock.json`, `test/playwright.config.ts`, `test/docker-compose.test.yml`, `test/README.md`, or `test/0X-*.spec.ts` to `.gitignore` — these are tracked per RESEARCH.md lines 1012-1018.

    **4b — Write `test/README.md`** as two paragraphs of plain text. No emojis. Single H1. Suggested body (planner-final wording — adjust phrasing as needed but preserve the three pieces of information):

    ```markdown
    # FinAlly E2E Harness

    Phase 10 brings up the production `finally` image alongside a Playwright runner via docker compose, then runs the seven `planning/PLAN.md` §12 demo scenarios against it in Chromium, Firefox, and WebKit. The single canonical command, run from the repo root, is:

    `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright`

    The compose process exits with Playwright's exit code. After a run, the inspectable HTML report is at `test/playwright-report/index.html` and per-test traces, screenshots, and videos for failures are at `test/test-results/`. To debug a single spec on a single browser locally, run:

    `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 01-fresh-start.spec.ts --project=chromium`
    ```

    **4c — Harness smoke run (gate):** With Tasks 1-3 complete, run the canonical one-command invocation. Because no spec files exist yet (Wave 2 hasn't shipped them), Playwright reports `Total: 0` and exits 0. The compose process tears down both containers cleanly and exits 0.

    Command: `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright`

    Expected output includes:
    - `app` service builds successfully (Stage 1 Node, Stage 2 Python uv).
    - `app` healthcheck transitions to `healthy` within ~15-30s (start_period 15s + first interval).
    - `playwright` service starts, runs `npm ci` (downloads `@playwright/test` from local cache or registry), then `npx playwright test`.
    - Playwright stdout: "Total: 0 tests" or "No tests found" (depending on Playwright 1.59.1's exact wording).
    - Compose stops the `app` container (SIGINT propagates per Phase 9 Dockerfile STOPSIGNAL).
    - `docker compose ... up ... ; echo $?` reports `0`.
  </action>
  <verify>
    <automated>tail -5 .gitignore | grep -q "test/playwright-report/" && tail -5 .gitignore | grep -q "test/test-results/" && tail -5 .gitignore | grep -q "test/node_modules/" && test -f test/README.md && grep -q "docker compose -f test/docker-compose.test.yml up" test/README.md && docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` ends with the three new entries (`test/playwright-report/`, `test/test-results/`, `test/node_modules/`) under a `# Phase 10 E2E artefacts (Playwright)` section comment.
    - `git check-ignore test/playwright-report/foo.html` returns the path (proving the rule applies).
    - `git check-ignore test/package.json` returns nothing (proving `package.json` is NOT ignored).
    - `git check-ignore test/package-lock.json` returns nothing.
    - `git check-ignore test/playwright.config.ts` returns nothing.
    - `git check-ignore test/docker-compose.test.yml` returns nothing.
    - `git check-ignore test/README.md` returns nothing.
    - `test/README.md` exists, contains the exact one-command string `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright`, references `test/playwright-report/index.html`, and shows the single-spec/single-browser debugging recipe.
    - `test/README.md` is two paragraphs (no emojis; single H1 — verify with `grep -c "^# " test/README.md` returns 1).
    - The canonical compose command exits 0.
    - The Playwright service ran successfully (its log includes `Listing` or `0 tests` or `No tests found` with no test failures).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production image ↔ test harness | The compose `app` service uses the SAME image as production, with one delta: `LLM_MOCK=true`. Test code does NOT live inside the production image (`.dockerignore:43` excludes `test/`). This is the critical boundary — test affordances must not bleed into the image users run. |
| Compose internal network ↔ host | `app:8000` is reachable only inside the compose project's default network. No host port-publish for `app` by default — the host has no observable side effect from a test run beyond produced report files (gitignored). |
| Test harness ↔ developer's `.env` | The compose file deliberately sets `OPENROUTER_API_KEY: ""` and `MASSIVE_API_KEY: ""` literally — it does NOT use `--env-file ../.env`. Test runs MUST NOT read or depend on the developer's local secrets, per PATTERNS.md "DO NOT use `--env-file .env` here" (line 152). |
| `.gitignore` ↔ committed test source | Spec files, config, lockfile, and compose YAML are committed; report directories and node_modules are not. The split protects the lockfile/source-of-truth from churn while keeping artefacts out of git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | T (Tampering) | Production image | mitigate | Phase 10 makes ZERO modifications to `Dockerfile`, `.dockerignore`, `backend/`, `frontend/` source. Verified by `git diff --name-only` after the plan ships — only `.gitignore` and new files under `test/` appear. The compose-side HEALTHCHECK is in `test/docker-compose.test.yml`, NOT in the Dockerfile (CONTEXT.md D-10). |
| T-10-01-02 | I (Information disclosure) | Developer `.env` secrets | mitigate | Compose file explicitly sets `OPENROUTER_API_KEY: ""` and `MASSIVE_API_KEY: ""` in `services.app.environment`. NO `env_file:` directive. NO `${OPENROUTER_API_KEY}` interpolation. A test run on a developer machine never reads `.env`; a test run in CI never has access to the user's keys. PATTERNS.md "DO NOT use `--env-file .env` here" (line 152) is the binding rule. |
| T-10-01-03 | I | Test report artefacts in version control | mitigate | `.gitignore` adds `test/playwright-report/`, `test/test-results/`, `test/node_modules/`. Verified by `git check-ignore` in Task 4 acceptance. |
| T-10-01-04 | E (Elevation of privilege) | Compose network exposing `app:8000` to host | accept | The compose file does NOT publish `app`'s port to the host (no `ports:` block). `app:8000` is reachable only from `playwright` over the compose default network. If a developer chooses to add `ports: ["8000:8000"]` for ad-hoc debugging, the existing app already exposes itself on localhost — no new attack surface. Localhost-only by design per PLAN.md "single-user demo". |
| T-10-01-05 | D (Denial of service) | Compose-side HEALTHCHECK in production Dockerfile | accept-by-design | The production `Dockerfile` deliberately omits `HEALTHCHECK` (Phase 9 D-08). Phase 10 puts the HEALTHCHECK in the COMPOSE FILE only. The production image therefore has no test-affordance bloat. |
| T-10-01-06 | T | LLM_MOCK env propagating to a non-test docker run | mitigate | The `LLM_MOCK: "true"` env var is set ONLY in `test/docker-compose.test.yml`. `scripts/start_mac.sh`, `scripts/start_windows.ps1`, `docs/DOCKER.md` are NOT touched by this plan (verified by `git diff --name-only`). The production runtime behaviour with no `.env` LLM_MOCK setting is unchanged. |

</threat_model>

<verification>
- `test/package.json` pins `@playwright/test` to exact `1.59.1` (Task 1 grep gate).
- `test/package-lock.json` exists and references `@playwright/test@1.59.1` (Task 1 grep gate).
- `cd test && npx playwright test --list` exits 0 (Task 2 — proves config parses).
- `docker compose -f test/docker-compose.test.yml config` exits 0 (Task 3 — proves YAML is well-formed).
- `.gitignore` ends with the three test-artefact entries; `git check-ignore` confirms `test/playwright-report/foo.html` is ignored AND `test/package.json`, `test/package-lock.json`, `test/playwright.config.ts`, `test/docker-compose.test.yml`, `test/README.md` are NOT ignored (Task 4 gate).
- The canonical one-command invocation runs end-to-end: `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 with zero tests run (Task 4 smoke gate).
- `git diff --name-only` after the plan completes shows only: `.gitignore`, `test/package.json`, `test/package-lock.json`, `test/playwright.config.ts`, `test/docker-compose.test.yml`, `test/README.md`. No other files touched.
</verification>

<success_criteria>
- [ ] `test/` directory exists with five committed source files (package.json, package-lock.json, playwright.config.ts, docker-compose.test.yml, README.md).
- [ ] `.gitignore` appended with the three Phase-10 artefact entries; existing rules untouched.
- [ ] `@playwright/test` pinned exact `1.59.1` in package.json AND package-lock.json.
- [ ] Playwright config is valid TypeScript (smoke `npx playwright test --list` passes).
- [ ] Compose file is valid YAML (`docker compose ... config` passes).
- [ ] Canonical one-command invocation runs end-to-end with zero specs and exits 0.
- [ ] No modifications outside `test/` and `.gitignore`.
- [ ] No emojis anywhere in the new files.
- [ ] No `--env-file .env` reference, no `${OPENROUTER_API_KEY}` interpolation in compose.
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-validation/10-01-SUMMARY.md` recording the six files created/modified, the exact pinned Playwright version (and any `-jammy` → `-noble` substitution), the `docker compose ... config` output, the smoke-run timing (build + healthcheck wait + Playwright zero-test run), and confirmation that the harness is ready for the four Wave 2 spec plans.
</output>
