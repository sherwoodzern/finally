---
phase: 10
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - test/05-portfolio-viz.spec.ts
  - test/06-chat.spec.ts
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "`test/05-portfolio-viz.spec.ts` exists and contains a single `test(...)` block that buys `META × 1` (D-08 isolation), clicks the Heatmap tab, asserts `[data-testid=heatmap-treemap]` is visible, then clicks the P&L tab and asserts both `[data-testid=pnl-chart]` and `[data-testid=pnl-summary]` are visible."
    - "`test/06-chat.spec.ts` exists and contains a single `test(...)` block that fills the chat input with `'buy AMZN 1'`, clicks Send, and asserts `[data-testid=action-card-executed]` becomes visible within 15s. Bonus: asserts `getByRole('button', { name: 'Select AMZN' })` (the AMZN positions row) appears in the table — proving the auto-execution path produced a real position."
    - "06-chat MUST NOT assert on `[data-testid=chat-message-assistant]` text content — the documented ChatResponse field-shape mismatch (Pitfall 4 / RESEARCH.md lines 621-625) makes the assistant bubble render empty. Asserting on `action-card-executed` is the stable signal."
    - "Both specs run green across all three Playwright projects in the harness. Tickers per D-08: 05 uses `META` (in seed; not used by 03/04/06), 06 uses `AMZN` (in seed; not used by 03/04/05)."
    - "05-portfolio-viz uses tab-button selectors `getByRole('tab', { name: /heatmap/i })` (or `getByTestId('tab-heatmap')` post-10-00); 06-chat uses `getByLabel('Ask the assistant')` for the input and `getByRole('button', { name: 'Send' })` for the submit, plus the stable `data-testid=action-card-*` family from Phase 8."
  artifacts:
    - path: "test/05-portfolio-viz.spec.ts"
      provides: "PLAN.md §12 row 5 — heatmap + P&L chart render scenario."
      contains: "heatmap-treemap"
    - path: "test/06-chat.spec.ts"
      provides: "PLAN.md §12 row 6 — mocked-chat trade-execution-shown-inline scenario."
      contains: "action-card-executed"
  key_links:
    - from: "test/05-portfolio-viz.spec.ts (tab clicks)"
      to: "frontend/src/components/terminal/TabBar.tsx (`<button role=\"tab\">` per tab id)"
      via: "Playwright `getByRole('tab', { name: /heatmap/i })` resolves via the accessibility tree (`role=\"tab\"` + visible button text). Post-10-00, `getByTestId('tab-heatmap')` is the stable alternative."
      pattern: "role: 'tab'"
    - from: "test/05-portfolio-viz.spec.ts (heatmap + pnl assertions)"
      to: "frontend/src/components/portfolio/Heatmap.tsx:117 (`data-testid=\"heatmap-treemap\"`); frontend/src/components/portfolio/PnLChart.tsx:59,93 (`pnl-summary`, `pnl-chart`)"
      via: "Stable Phase 8 test-ids — RESEARCH.md Component Test-ID Inventory line 807-811. No selector fallbacks needed — these test-ids shipped in Plan 08-03 and 08-04 and are still in the source."
      pattern: "heatmap-treemap"
    - from: "test/06-chat.spec.ts (chat input + send)"
      to: "frontend/src/components/chat/ChatInput.tsx:48,56 (`aria-label=\"Ask the assistant\"` + `<button>Send</button>`)"
      via: "`getByLabel('Ask the assistant')` for the textarea; `getByRole('button', { name: 'Send' })` for submit. Stable across all three engines."
      pattern: "Ask the assistant"
    - from: "test/06-chat.spec.ts (action-card assertion)"
      to: "frontend/src/components/chat/ActionCard.tsx:98 (`data-testid={`action-card-${status}`}`)"
      via: "ActionCard renders `data-testid` based on the `status` field returned from the trade auto-execution. Mock client's `\"buy AMZN 1\"` triggers the BUY regex (`backend/app/chat/mock.py:9-15`), the chat service auto-executes, and the resulting status is `executed` (or `failed` on cold cache — but the simulator should have AMZN priced by the time this spec runs)."
      pattern: "action-card-executed"
    - from: "test/06-chat.spec.ts (Mock LLM trigger)"
      to: "backend/app/chat/mock.py:9-15 (BUY regex `\\bbuy\\s+([A-Z][A-Z0-9.]{0,9})\\s+(\\d+(?:\\.\\d+)?)\\b`)"
      via: "RESEARCH.md verified the trigger phrase + Phase 5 UAT confirmed the auto-execution path works. `'buy AMZN 1'` parses cleanly."
      pattern: "buy AMZN 1"
---

<objective>
Land the third pair of Wave 2 spec files: `05-portfolio-viz.spec.ts` (PLAN.md §12 row 5 — heatmap + P&L chart render) and `06-chat.spec.ts` (PLAN.md §12 row 6 — mocked chat with visible trade execution). Both rely on the stable Phase 8 `data-testid` family already present in `frontend/src/components/portfolio/` and `frontend/src/components/chat/` source files; both pre-condition their assertions on a small UI-driven mutation (META buy in 05, AMZN-via-chat trade in 06).

Purpose: Closes 2 of the 7 §12 scenarios for TEST-04 — the portfolio visualization surface and the agentic chat surface. Documents (and works around) the known ChatResponse field-shape mismatch per RESEARCH.md Pitfall 4 / D-05.

Output: Two new TypeScript spec files under `test/`. No other files touched.

Wave 2 rationale: Files disjoint from 10-02, 10-03, 10-05. Shared `app` build with the rest of Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-e2e-validation/10-CONTEXT.md
@.planning/phases/10-e2e-validation/10-RESEARCH.md
@.planning/phases/10-e2e-validation/10-PATTERNS.md
@.planning/phases/10-e2e-validation/10-VALIDATION.md
@.planning/phases/10-e2e-validation/10-01-PLAN.md
@frontend/src/components/portfolio/Heatmap.tsx
@frontend/src/components/portfolio/PnLChart.tsx
@frontend/src/components/terminal/TabBar.tsx
@frontend/src/components/terminal/TradeBar.tsx
@frontend/src/components/chat/ChatInput.tsx
@frontend/src/components/chat/ChatThread.tsx
@frontend/src/components/chat/ChatDrawer.tsx
@frontend/src/components/chat/ActionCard.tsx
@frontend/src/components/chat/ActionCardList.tsx
@backend/app/chat/mock.py
@backend/app/chat/models.py

<interfaces>
<!-- Reference selectors and test bodies. -->

### Portfolio surface test-ids (verified RESEARCH.md Component Test-ID Inventory):
- `data-testid="heatmap-treemap"` — Heatmap.tsx:117 (Recharts Treemap container, populated when ≥1 position)
- `data-testid="heatmap-skeleton"` — Heatmap.tsx:83 (empty/loading state when 0 positions)
- `data-testid="pnl-chart"` — PnLChart.tsx:93 (Recharts LineChart container)
- `data-testid="pnl-skeleton"` — PnLChart.tsx:70 (loading state)
- `data-testid="pnl-summary"` — PnLChart.tsx:59 (header `Total ($) vs $10k` text)

### TabBar selectors:
- Each `<button role="tab">` exists for ids `chart`, `heatmap`, `pnl` (verified TabBar.tsx:29-46).
- Pre-10-00: `getByRole('tab', { name: /heatmap/i })` resolves via the visible text "Heatmap" / "P&L".
- Post-10-00: `getByTestId('tab-heatmap')` / `getByTestId('tab-pnl')` is preferred.
- Tab text "P&L" — the regex `/p.+l/i` tolerates copy variants ("P&L", "PnL", "P/L"). Use it (RESEARCH.md line 579) so the test survives a future copy tweak.

### Chat surface (verified RESEARCH.md Component Test-ID Inventory + Pitfall 4):
- `data-testid="chat-drawer"` — outer `<aside>` (initial state: open per ChatDrawer.tsx:19).
- `data-testid="chat-thread"` — scrollable message list.
- `data-testid="chat-message-assistant"` — assistant bubble. **DO NOT assert on its text** (Pitfall 4: rendering bug makes content empty).
- `data-testid="action-card-executed"` — successful trade card (ActionCard.tsx:98).
- `data-testid="action-card-failed"` — failed trade card.
- `data-testid="action-card-list"` — wrapper around all action cards.
- `aria-label="Ask the assistant"` — ChatInput textarea (ChatInput.tsx:48).
- `<button>Send</button>` — ChatInput submit button (ChatInput.tsx:56).
- `aria-label="Collapse chat"/"Expand chat"` — ChatHeader toggle (only needed if drawer is closed; default state is open).

### Mock LLM trigger (verified backend/app/chat/mock.py:9-15):
`\bbuy\s+([A-Z][A-Z0-9.]{0,9})\s+(\d+(?:\.\d+)?)\b` — `'buy AMZN 1'` matches. Mock returns:
```json
{ "message": "Mock: executing buy AMZN 1.0", "trades": [{ "ticker": "AMZN", "side": "buy", "quantity": 1.0, "status": "executed" | "failed" }], "watchlist_changes": [] }
```
Phase 5 service auto-executes the trade through the same validation path as a manual TradeBar trade.

### Reference test bodies (verbatim from RESEARCH.md §"Per-Scenario Spec Outlines")

**05-portfolio-viz.spec.ts** — RESEARCH.md lines 553-573:

```typescript
import { test, expect } from '@playwright/test';

test('portfolio viz: heatmap and P&L chart render after a position exists', async ({ page }) => {
  await page.goto('/');

  // Buy a position so the heatmap has data to render past the skeleton.
  // META — D-08 isolation from NVDA(03), JPM(04), AMZN(06).
  await page.getByLabel('Ticker').fill('META');
  await page.getByLabel('Quantity').fill('1');
  await page.getByRole('button', { name: 'Buy' }).click();
  await expect(
    page.getByRole('button', { name: 'Select META' }),
  ).toBeVisible({ timeout: 10_000 });

  // Heatmap tab
  await page.getByRole('tab', { name: /heatmap/i }).click();
  await expect(page.getByTestId('heatmap-treemap')).toBeVisible({ timeout: 10_000 });

  // P&L tab
  await page.getByRole('tab', { name: /p.+l/i }).click();
  await expect(page.getByTestId('pnl-chart')).toBeVisible({ timeout: 10_000 });
  await expect(page.getByTestId('pnl-summary')).toBeVisible();
});
```

**06-chat.spec.ts** — RESEARCH.md lines 600-619:

```typescript
import { test, expect } from '@playwright/test';

test('chat trade execution: mock buy AMZN 1 produces inline action card', async ({ page }) => {
  await page.goto('/');

  // Drawer is open by default (ChatDrawer initial state = true).
  // If a future change closes the drawer by default, click `getByLabel('Expand chat')` first.

  await page.getByLabel('Ask the assistant').fill('buy AMZN 1');
  await page.getByRole('button', { name: 'Send' }).click();

  // The mock client returns a TradeAction; the service auto-executes; ChatThread
  // renders the response with an ActionCardList containing an action-card-executed.
  // KNOWN GAP: the assistant chat bubble renders empty due to ChatResponse
  // field-shape mismatch (RESEARCH.md Pitfall 4) — DO NOT assert bubble text.
  await expect(page.getByTestId('action-card-executed')).toBeVisible({
    timeout: 15_000,
  });

  // Bonus: the AMZN position appears in the positions table.
  await expect(
    page.getByRole('button', { name: 'Select AMZN' }),
  ).toBeVisible({ timeout: 10_000 });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write `test/05-portfolio-viz.spec.ts`</name>
  <read_first>
    - frontend/src/components/portfolio/Heatmap.tsx (lines 83, 117 — confirm `data-testid="heatmap-skeleton"` and `data-testid="heatmap-treemap"` are still in source)
    - frontend/src/components/portfolio/PnLChart.tsx (lines 59, 70, 93 — confirm `pnl-summary`, `pnl-skeleton`, `pnl-chart` test-ids)
    - frontend/src/components/terminal/TabBar.tsx (lines 26-46 — confirm three tabs render with `role="tab"` and visible text "Chart" / "Heatmap" / "P&L")
    - frontend/src/components/terminal/TradeBar.tsx (lines 82-141 — same TradeBar selectors as 03-buy)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 542-582 — full assertion + selector strategy + edge cases)
  </read_first>
  <action>
    Write `test/05-portfolio-viz.spec.ts` based on the verbatim shape in the `<interfaces>` block above.

    Specific decisions for the executor:
    1. **Imports + goto:** Same as 03-buy.
    2. **Setup buy:** Per D-08 ticker isolation, use `META` (NOT in any other Wave 2 spec). The buy is necessary because the Heatmap renders the SKELETON state when positions are empty — only with ≥1 position does `[data-testid=heatmap-treemap]` mount (verified Heatmap.tsx:83 vs 117). The `start_period: 15s` healthcheck + `goto('/')` warm-up ensures META has a streaming price before the buy fires.
    3. **Heatmap tab assertion:** `getByRole('tab', { name: /heatmap/i }).click()` then `expect(getByTestId('heatmap-treemap')).toBeVisible({ timeout: 10_000 })`. The 10s timeout absorbs Recharts' SVG mount + ResponsiveContainer measurement (cleanly handled in a real browser per RESEARCH.md line 581).
    4. **P&L tab assertion:** `getByRole('tab', { name: /p.+l/i }).click()` — regex `/p.+l/i` tolerates copy variants. Then assert BOTH `[data-testid=pnl-chart]` AND `[data-testid=pnl-summary]` are visible.
    5. **Optional post-10-00 simplification:** If Plan 10-00 has shipped, swap `getByRole('tab', { name: ... })` for `getByTestId('tab-heatmap')` / `getByTestId('tab-pnl')`. Document the choice in the SUMMARY.
    6. **Test name:** `'portfolio viz: heatmap and P&L chart render after a position exists'`.
    7. **Single `test()` block.** No emojis, no `waitForTimeout`, no `.only`/`.skip`.
  </action>
  <verify>
    <automated>test -f test/05-portfolio-viz.spec.ts && grep -q "import { test, expect } from '@playwright/test';" test/05-portfolio-viz.spec.ts && grep -q "META" test/05-portfolio-viz.spec.ts && grep -q "heatmap-treemap" test/05-portfolio-viz.spec.ts && grep -q "pnl-chart" test/05-portfolio-viz.spec.ts && grep -q "pnl-summary" test/05-portfolio-viz.spec.ts && (grep -q "name: /heatmap/i" test/05-portfolio-viz.spec.ts || grep -q "tab-heatmap" test/05-portfolio-viz.spec.ts) && cd test && npx playwright test 05-portfolio-viz.spec.ts --list 2>&1 | grep -qE "(portfolio viz|1 test)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/05-portfolio-viz.spec.ts` exists and parses as valid TypeScript.
    - Buys `META × 1` via the TradeBar UI (D-08 isolation).
    - Asserts `[data-testid=heatmap-treemap]` visible after Heatmap tab click.
    - Asserts BOTH `[data-testid=pnl-chart]` AND `[data-testid=pnl-summary]` visible after P&L tab click.
    - Tab clicks use either `getByRole('tab', { name: /heatmap/i })` / `/p.+l/i` (pre-10-00) or `getByTestId('tab-heatmap')` / `getByTestId('tab-pnl')` (post-10-00).
    - Single `test(...)` block; no emojis, no `waitForTimeout`, no `.only`/`.skip`.
    - Smoke run: `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 05-portfolio-viz.spec.ts --project=chromium` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write `test/06-chat.spec.ts`</name>
  <read_first>
    - frontend/src/components/chat/ChatInput.tsx (line 48 — `aria-label="Ask the assistant"`; line 56 — `<button>Send</button>`)
    - frontend/src/components/chat/ChatDrawer.tsx (line 19 — confirm initial state `useState(true)` so drawer is open by default; if changed, the test must `getByLabel('Expand chat').click()` first)
    - frontend/src/components/chat/ActionCard.tsx (line 98 — `data-testid={`action-card-${status}`}`)
    - frontend/src/components/chat/ChatThread.tsx (lines 30-35 — for awareness of the Pitfall 4 field-shape mismatch; the spec MUST NOT assert on bubble text)
    - backend/app/chat/mock.py (lines 9-15 — confirm BUY regex matches `'buy AMZN 1'`)
    - backend/app/chat/models.py (lines 83-88 — confirm `ChatResponse` shape `{ message, trades, watchlist_changes }`; trades[].status is `executed` | `failed`)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 584-633 — full code shape + KNOWN GAP documentation)
  </read_first>
  <action>
    Write `test/06-chat.spec.ts` based on the verbatim shape in the `<interfaces>` block above.

    Specific decisions for the executor:
    1. **Imports + goto:** Same as the other specs.
    2. **No drawer-open click.** ChatDrawer's initial state is `useState(true)` per `ChatDrawer.tsx:19`. The drawer is open on page load. If a future plan flips the default, the test will fail and the fix is to add `await page.getByLabel('Expand chat').click()` BEFORE filling the input.
    3. **Chat input fill:** `getByLabel('Ask the assistant').fill('buy AMZN 1')`. The exact aria-label string is verified at `ChatInput.tsx:48`.
    4. **Click Send:** `getByRole('button', { name: 'Send' }).click()`. Do NOT submit via Enter — explicit click is more deterministic across engines per RESEARCH.md line 633.
    5. **Action-card assertion:** `expect(getByTestId('action-card-executed')).toBeVisible({ timeout: 15_000 })`. The 15s timeout is generous because the chat path includes: HTTP POST → mock client regex match → service auto-execute (validate-then-write) → response → React Query state propagation → ActionCardList re-render. Phase 5 UAT measured this end-to-end at <2s.
    6. **DO NOT assert on `[data-testid=chat-message-assistant]` text content.** RESEARCH.md Pitfall 4 documents the ChatResponse field-shape mismatch — frontend reads `res.id` / `res.content` / `res.created_at` (all undefined in the actual `{message, trades, watchlist_changes}` response), so the bubble renders empty. Add a comment in the spec citing Pitfall 4 / D-05 so future maintainers don't "fix" the test by adding a bubble-text assertion.
    7. **Bonus AMZN position assertion:** `expect(getByRole('button', { name: 'Select AMZN' })).toBeVisible({ timeout: 10_000 })`. This proves the auto-execution path produced a real position row, not just a UI confirmation card. RESEARCH.md line 617 recommends this.
    8. **D-08 ticker:** `'AMZN'` — in default seed; not used by 03-buy / 04-sell / 05-portfolio-viz.
    9. **Test name:** `'chat trade execution: mock buy AMZN 1 produces inline action card'`.
    10. **Single `test()` block.** No emojis, no `waitForTimeout`, no `.only`/`.skip`.
  </action>
  <verify>
    <automated>test -f test/06-chat.spec.ts && grep -q "import { test, expect } from '@playwright/test';" test/06-chat.spec.ts && grep -q "Ask the assistant" test/06-chat.spec.ts && grep -q "buy AMZN 1" test/06-chat.spec.ts && grep -q "{ name: 'Send' }" test/06-chat.spec.ts && grep -q "action-card-executed" test/06-chat.spec.ts && ! grep -A2 "chat-message-assistant" test/06-chat.spec.ts | grep -qE "toContainText|toHaveText|toHaveValue" && grep -q "Select AMZN" test/06-chat.spec.ts && cd test && npx playwright test 06-chat.spec.ts --list 2>&1 | grep -qE "(chat trade|1 test)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/06-chat.spec.ts` exists and parses as valid TypeScript.
    - Fills `getByLabel('Ask the assistant')` with `'buy AMZN 1'`.
    - Clicks `getByRole('button', { name: 'Send' })`.
    - Asserts `[data-testid=action-card-executed]` visible (15s timeout).
    - Asserts `getByRole('button', { name: 'Select AMZN' })` visible (10s timeout) — bonus position-row check.
    - **DOES NOT** assert on `[data-testid=chat-message-assistant]` text content (Pitfall 4 guard).
    - Contains a comment citing RESEARCH.md Pitfall 4 / CONTEXT.md D-05 explaining why bubble text is not asserted.
    - Uses ticker `'AMZN'` (D-08 isolation).
    - Single `test(...)` block; no emojis, no `waitForTimeout`, no `.only`/`.skip`.
    - Smoke run: `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 06-chat.spec.ts --project=chromium` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Per-spec 3-browser harness gate (05 + 06 only)</name>
  <read_first>
    - test/05-portfolio-viz.spec.ts + test/06-chat.spec.ts (just-written)
    - test/playwright.config.ts (confirm three projects defined)
    - test/docker-compose.test.yml (confirm `playwright` service ready and `LLM_MOCK=true`)
  </read_first>
  <action>
    Validate both new specs across all three browsers, scoped to JUST the new specs.

    Command (from repo root):
    `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 05-portfolio-viz.spec.ts 06-chat.spec.ts`

    Expected:
    - All `2 specs × 3 projects = 6` (spec, project) pairs pass.
    - Compose `run --rm` exits 0.
    - The two new specs co-exist with any specs from prior Wave 2 plans without state interference — D-08 unique tickers (META, AMZN here; NVDA, JPM, PYPL elsewhere) prevent collisions.

    **If any (spec, project) fails:** Common causes:
    - **05 — Heatmap-skeleton instead of treemap:** The buy didn't process before the tab click. Add `await page.waitForLoadState('networkidle')` after the buy, OR bump the `Select META` row visibility timeout to 15s.
    - **05 — Recharts SVG not yet rendered:** Bump the `heatmap-treemap` / `pnl-chart` visibility timeout to 15s. Real browsers don't have the Vitest ResizeObserver issue, but first-paint can still be slow on WebKit.
    - **06 — `action-card-executed` not visible:** Read the compose `app` logs (`docker compose -f test/docker-compose.test.yml logs app`) for chat service errors. Common causes: `LLM_MOCK` not set (compose env var didn't propagate — check `services.app.environment.LLM_MOCK: "true"`); AMZN price not yet streamed (`price_unavailable` — the trade returns `status="failed"` instead of `executed`). For the latter, switch the assertion to a regex matching either status: `expect(page.getByTestId(/action-card-(executed|failed)/)).toBeVisible(...)` and document the relaxation in the SUMMARY as a Phase 10.1 candidate.
    - **06 — Drawer closed:** Insert `await page.getByLabel('Expand chat').click()` before the input fill if the default state has changed.

    Apply the minimal fix and re-run. Do NOT run the full one-command harness here.
  </action>
  <verify>
    <automated>docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 05-portfolio-viz.spec.ts 06-chat.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 05-portfolio-viz.spec.ts 06-chat.spec.ts` exits 0.
    - Stdout shows all 6 (spec, project) pairs passed.
    - If `06-chat` was relaxed to accept `executed | failed` (instead of just `executed`), document the relaxation as a Phase 10.1 candidate (per RESEARCH.md A1 / D-05) in the SUMMARY but do NOT block this plan on it.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test browser ↔ Recharts SVG | 05 asserts visibility of Recharts-rendered DOM nodes. Stable per Phase 8 work; cross-engine SVG rendering is uniform. |
| Chat input ↔ LLM mock backend | 06 sends user text to the chat endpoint, which routes through `MockChatClient` (Phase 5) when `LLM_MOCK=true` (set in compose file). The mock keyword `"buy AMZN 1"` is plain ASCII; no injection risk because the mock parses with anchored regex (`\bbuy\s+...\b`) — non-matching text would fall through to the empty-action default response. |
| ChatResponse field-shape mismatch | The frontend reads `res.id`/`res.content`/`res.created_at` from a response that ships `{message, trades, watchlist_changes}`. This is a documented Phase 5/8 bug (Pitfall 4). The test acknowledges and works around it by NOT asserting on bubble text. The test does not introduce or hide the bug; surface as Phase 10.1 candidate per D-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | T (Tampering) | Production code | mitigate | Phase 10 modifies only spec files. ChatResponse mismatch is a pre-existing bug (Pitfall 4) — not introduced or papered over by this plan. |
| T-10-04-02 | I (Information disclosure) | Chat content in test reports | accept | The single chat message in 06-chat is `"buy AMZN 1"` — plain ASCII, no PII, no secrets. Reports gitignored per Plan 10-01. |
| T-10-04-03 | E (Elevation of privilege) | Mock LLM auto-execute path | accept | The mock client only matches the four documented regexes (`buy`, `sell`, `add`, `remove`) and produces structured trade actions that go through the SAME validation pipeline as manual trades. There is no auth bypass path — the trade either passes Phase 3 validation (sufficient cash, valid ticker, price available) or returns `status="failed"`. |
| T-10-04-04 | S (Spoofing) | Trade attribution | accept | All trades in Phase 10 are against `user_id="default"` (single-user demo). No multi-user attribution exists in v1. |

</threat_model>

<verification>
- `test/05-portfolio-viz.spec.ts` and `test/06-chat.spec.ts` exist; both pass `cd test && npx playwright test --list` (Tasks 1-2 grep gates).
- Both specs run green across chromium, firefox, webkit (Task 3 gate).
- 06-chat does NOT assert on `chat-message-assistant` text (Pitfall 4 guard verified by negative grep).
- 06-chat contains a comment citing Pitfall 4 / D-05.
- D-08 unique tickers: 05 uses META, 06 uses AMZN. Combined with 10-02's PYPL, 10-03's NVDA + JPM, no cross-spec collision in a single compose `up`.
- No files modified outside `test/05-portfolio-viz.spec.ts` and `test/06-chat.spec.ts`.
</verification>

<success_criteria>
- [ ] `test/05-portfolio-viz.spec.ts` buys META × 1, then asserts heatmap-treemap + pnl-chart + pnl-summary visible after tab clicks.
- [ ] `test/06-chat.spec.ts` sends `"buy AMZN 1"` via chat, asserts action-card-executed visible, plus the AMZN position row.
- [ ] 06-chat does NOT assert on chat-message-assistant text content (Pitfall 4 guard).
- [ ] Both pass on chromium, firefox, webkit.
- [ ] No emojis, no `waitForTimeout`, no `test.only`/`test.skip`.
- [ ] D-08 isolation maintained (META + AMZN unique to this plan within Wave 2).
- [ ] Per-spec scoped harness run (Task 3) exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-validation/10-04-SUMMARY.md` recording the two spec files, which selector strategy each used (testId vs aria-label/role — depending on 10-00 ship order), Task 3's green run output, and a confirmation/relaxation note for 06-chat (whether `action-card-executed` was sufficient or whether the executed-or-failed regex relaxation was required, with a pointer to Phase 10.1 if the latter).
</output>
