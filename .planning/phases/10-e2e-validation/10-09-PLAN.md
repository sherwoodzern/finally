---
phase: 10-e2e-validation
plan: 09
type: execute
wave: 5
depends_on: [10-06, 10-07, 10-08]
files_modified:
  - test/playwright.config.ts
  - test/docker-compose.test.yml
  - frontend/src/components/portfolio/Heatmap.tsx
autonomous: true
gap_closure: true
requirements: [TEST-03, TEST-04]
tags: [playwright, e2e, gap-closure, viewport-alignment, sqlite-tmpfs, recharts-tooltip]

must_haves:
  truths:
    - "Mode A (corrected) closed: At viewport 1440×900 the right-column PositionsTable does not visually overlap the center-column TabBar; `tab-pnl` is clickable across chromium, firefox, and webkit. The interceptor `<td class=\"px-4 font-semibold\">{ticker}</td>` from `frontend/src/components/terminal/PositionRow.tsx:57` no longer absorbs clicks on the P&L tab."
    - "Mode A.2 closed: Each canonical-command run starts from a clean SQLite state regardless of how many prior `up` invocations ran. Cross-run carry-over of positions, cash, and chat history is eliminated by mounting tmpfs at `/app/db` for the appsvc service. Production Dockerfile `VOLUME /app/db` is preserved unchanged (production deploys still persist; only the test compose overrides it)."
    - "The canonical harness command from CONTEXT.md D-03 finishes green reproducibly: `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 with `21 passed`, `0 failed`, `0 flaky`, and `Container test-appsvc-1 Healthy` in the log — no flags, no spec filters, no per-project orchestration."
    - "ROADMAP Phase 10 SC#3 (single canonical command finishes green reproducibly) is met. TEST-03 and TEST-04 are satisfied in evidence terms (REQUIREMENTS.md ticks are owned by the verifier on its next pass)."
    - "The 10-08 production fix `<Tooltip wrapperStyle={{ pointerEvents: 'none' }} />` at `Heatmap.tsx:138` is preserved unchanged (latent UX hardening). The 10-07 `dismissChartTooltip` helper in `test/05-portfolio-viz.spec.ts` is preserved unchanged (harmless belt-and-suspenders for v1)."
  artifacts:
    - path: "test/playwright.config.ts"
      provides: "Per-project explicit viewport override at 1440×900 across chromium, firefox, and webkit. Aligns the test environment with the production design contract from `planning/PLAN.md §10` (`desktop-first ... wide screens`). Defaults from `devices['Desktop Chrome' | 'Desktop Firefox' | 'Desktop Safari']` are spread in first; viewport is then overridden last so the override wins."
      contains: "width: 1440, height: 900"
    - path: "test/docker-compose.test.yml"
      provides: "Explicit `tmpfs:` block on the `appsvc` service that mounts `/app/db` as an in-memory tmpfs. Each `docker compose up` therefore starts with a fresh, empty `/app/db` regardless of any prior anonymous Docker volumes. The original line 31 comment (which falsely claimed compose would create a fresh anonymous volume per `up`) is replaced with a comment that documents the tmpfs reality and references CONTEXT D-06."
      contains: "tmpfs:"
    - path: "frontend/src/components/portfolio/Heatmap.tsx"
      provides: "Recharts `<Tooltip>` element gains a `formatter` prop so the default tooltip body renders the position weight (a dollar value) as `$1,234.56` rather than the raw float `1234.561234`. Existing `wrapperStyle={{ pointerEvents: 'none' }}` is preserved unchanged. Closes WR-01 from `10-REVIEW.md` as a v1.0 polish bundled with this plan since the file is already being touched and the change is one prop addition."
      contains: "formatter="
    - path: "/tmp/phase10-final-harness.log"
      provides: "Captured stdout+stderr of the canonical harness invocation for this gate. Used as evidence in the SUMMARY for the `21 passed` line and the `Container test-appsvc-1 Healthy` line."
      contains: "21 passed"
  key_links:
    - from: "test/playwright.config.ts projects array"
      to: "Terminal.tsx 3-column grid (320px / 1fr / 360px) + ChatDrawer flex sibling"
      via: "Per-project `viewport: { width: 1440, height: 900 }` gives the 3-col grid + chat drawer enough horizontal room (1440 − 48 padding − 48 gaps − 320 left − 360 right = 664px center, plus drawer accommodation) so that the right-column PositionsTable does not overlap the center-column TabBar."
      pattern: "viewport: \\{ width: 1440, height: 900 \\}"
    - from: "test/docker-compose.test.yml appsvc service"
      to: "/app/db inside the appsvc container (where SQLite writes finally.db)"
      via: "Compose `tmpfs:` directive backs `/app/db` with an in-memory tmpfs. tmpfs is destroyed on container stop, so each `up` starts with an empty directory and the FastAPI lifespan re-seeds default $10k cash + 10-ticker watchlist."
      pattern: "tmpfs:\\s*\\n\\s*-\\s*/app/db"
    - from: "Canonical command from CONTEXT.md D-03"
      to: "21 passed + 0 failed + 0 flaky + Healthy appsvc"
      via: "docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright (verbatim — no flags added)"
      pattern: "21 passed"
---

<objective>
Close the two remaining reproducibly-demonstrable blockers from `10-VERIFICATION.md` (Mode A re-diagnosed as layout overlap at viewport 1280×720, and Mode A.2 cross-run SQLite carry-over via persistent docker volume) with two minimal test-environment edits, optionally bundle the WR-01 advisory tooltip-formatter polish since `Heatmap.tsx` is already a touched file, and own the canonical 21/21 green gate that closes Phase 10 SC#3.

Purpose: meet ROADMAP Phase 10 SC#3 ("Running the full E2E pack is a single command and finishes green locally against the freshly built image, with reproducible results on repeat runs") so TEST-03 and TEST-04 can be ticked. Plan 10-08 closed Modes B and C and applied a Heatmap Tooltip pointerEvents fix that — while correct as latent UX hardening — was not the actual closer for Mode A. Today's verifier proved by direct DOM evidence that the interceptor is the right-column PositionsTable cell, not a Recharts tooltip, and that the compose service was silently reusing prior anonymous volumes. This plan addresses both root causes directly and proves the gate green.

Output:
- One-block edit to `test/playwright.config.ts` that adds an explicit `viewport: { width: 1440, height: 900 }` to each of the 3 projects (closes Mode A on all 3 browsers).
- One-block edit to `test/docker-compose.test.yml` that adds a `tmpfs: - /app/db` directive on the `appsvc` service and rewrites the misleading line 31 comment (closes Mode A.2; keeps CONTEXT D-03 canonical command unchanged).
- One-prop edit to `frontend/src/components/portfolio/Heatmap.tsx` adding a `formatter` to the existing `<Tooltip>` so the default tooltip body shows `$1,234.56` rather than a raw float (closes WR-01 advisory; preserves the 10-08 `wrapperStyle={{ pointerEvents: 'none' }}` prop unchanged).
- A captured canonical-command run at `/tmp/phase10-final-harness.log` proving 21 passed / 0 failed / 0 flaky / appsvc Healthy / exit 0.
- A `.planning/phases/10-e2e-validation/10-09-SUMMARY.md` documenting the corrected Mode A diagnosis, the tmpfs ephemerality fix, the WR-01 bundle, and the harness summary line + Healthy line copied from the log as self-contained evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-e2e-validation/10-CONTEXT.md
@.planning/phases/10-e2e-validation/10-RESEARCH.md
@.planning/phases/10-e2e-validation/10-PATTERNS.md
@.planning/phases/10-e2e-validation/10-VERIFICATION.md
@.planning/phases/10-e2e-validation/10-UAT.md
@.planning/phases/10-e2e-validation/10-REVIEW.md
@.planning/phases/10-e2e-validation/10-08-PLAN.md
@.planning/phases/10-e2e-validation/10-08-SUMMARY.md
@test/playwright.config.ts
@test/docker-compose.test.yml
@frontend/src/components/portfolio/Heatmap.tsx
@CLAUDE.md
@planning/PLAN.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from the source files this plan modifies. -->

From `test/playwright.config.ts` (current state — lines 71-75, the projects array this plan modifies):
```typescript
projects: [
  { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  { name: 'firefox',  use: { ...devices['Desktop Firefox'] } },
  { name: 'webkit',   use: { ...devices['Desktop Safari'] } },
],
```

From `test/docker-compose.test.yml` (current state — lines 17-42, the `appsvc` service this plan modifies; the misleading comment is at lines 31-33):
```yaml
services:
  appsvc:
    # D-04: build from repo-root Dockerfile, not a registry pull.
    # `..` is relative to this compose file's directory, i.e. the repo root.
    build:
      context: ..
      dockerfile: Dockerfile
    # No image: tag - compose synthesises one as `<projectname>-appsvc`. We
    # never need to reference the image elsewhere.
    environment:
      LLM_MOCK: "true"          # D-05: deterministic mock chat path
      OPENROUTER_API_KEY: ""    # set explicitly empty; suppresses Phase 5 warning
      MASSIVE_API_KEY: ""       # empty -> SimulatorDataSource
      DB_PATH: /app/db/finally.db
      PYTHONUNBUFFERED: "1"
    # D-06: NO `volumes:` mapping for /app/db -> compose creates a fresh
    # anonymous volume per `up` invocation. Each run starts with a clean
    # SQLite (Phase 2 lazy init re-seeds the 10-ticker watchlist + $10k cash).
    # D-10: compose-side HEALTHCHECK only - the production Dockerfile
    # deliberately omits HEALTHCHECK (Phase 9 / D-08 of Phase 9 CONTEXT).
    healthcheck:
      ...
```

From `frontend/src/components/portfolio/Heatmap.tsx` (current state — lines 119-139 after 10-08; the existing Tooltip element is at line 138):
```tsx
            <Treemap
              data={treeData}
              dataKey="weight"
              stroke="#30363d"
              content={<HeatmapCell />}
              isAnimationActive
              animationDuration={300}
              onClick={(node) => handleHeatmapCellClick(node)}
            >
              {/*
                Explicit Recharts Tooltip with wrapperStyle pointerEvents: 'none'.
                ...
              */}
              <Tooltip wrapperStyle={{ pointerEvents: 'none' }} />
            </Treemap>
```

From `Dockerfile` (line 57 — relevant context only, NOT modified by this plan):
```dockerfile
VOLUME /app/db
```

The production `VOLUME` declaration must remain unchanged. This plan overrides it on the test side via the compose `tmpfs:` directive only; production deploys keep persistence.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan touches three files: a Playwright config (test environment only, no runtime impact), the test compose file (a tmpfs mount on the test-side appsvc service — affects only the test stack), and one Recharts prop on the heatmap tooltip (a CSS/formatter change with no new event handlers). The compose stack still runs `LLM_MOCK=true` against a now-tmpfs-backed SQLite on `localhost`. No new endpoints, no new secrets, no new network surface, no new external dependencies, no auth changes. Production Dockerfile `VOLUME /app/db` is preserved unchanged so production deploys still persist data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | T (Tampering) / I (Information disclosure) | `test/docker-compose.test.yml` `tmpfs: - /app/db` directive | accept | The tmpfs directive could be misread by a future maintainer as guidance for production. Mitigation: the inline comment in `docker-compose.test.yml` explicitly states this is a test-only override that exists ONLY to give CONTEXT D-06's "fresh per run" promise teeth, and that the production `Dockerfile:57 VOLUME /app/db` is intentionally preserved for production persistence. Compose file path itself (`test/docker-compose.test.yml`) reinforces test-only scope. |
| T-10-09-02 | I (Information disclosure) | `test/playwright.config.ts` viewport bump 1280×720 → 1440×900 | accept | The viewport change exposes a wider area of the rendered page to test runner snapshots and traces. No new code paths are exercised — only existing code at a different layout breakpoint that aligns with `planning/PLAN.md §10`'s documented `desktop-first ... wide screens` design contract. The page contents are the user's own simulated portfolio data and are already captured in test traces at the smaller viewport, so no new categories of data are surfaced. Single-user demo, no PII at risk. |
| T-10-09-03 | I (Information disclosure) | `Heatmap.tsx` `<Tooltip formatter={...}>` rendering of position weight | accept | The formatter exposes per-position dollar amounts in the tooltip body. This is the same data the heatmap already labels via `<HeatmapCell />` and the positions table already shows in full. No new data surface — only a formatting change from raw float to dollar-formatted string. Same data path as T-10-08-01 in `10-08-PLAN.md`. Single-user demo, no PII at risk. |

No new authentication, network, or data-flow surfaces are introduced.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Test-environment fix — add explicit `viewport: { width: 1440, height: 900 }` to each Playwright project (closes Mode A on all 3 browsers)</name>
  <files>test/playwright.config.ts</files>
  <read_first>
    - test/playwright.config.ts (current state — full file; particularly the projects array at lines 71-75 which this task replaces, and the existing `use:` block at lines 43-62 which is NOT modified)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md gaps[0] (Mode A — corrected diagnosis). Confirms the interceptor is `<td class="px-4 font-semibold">{ticker}</td>` from `frontend/src/components/terminal/PositionRow.tsx:57`, not a Recharts tooltip. Also confirms `Desktop Chrome' | 'Desktop Firefox' | 'Desktop Safari']` all default to viewport 1280×720, which is below the documented `wide screens` design contract.
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md gaps[0].artifacts[0] — proves the math: `1280 - 48 - 48 - 320 - 360 = 504px` center column at the default viewport, with the chat drawer expanded reducing effective grid width to ~920px. At 1440×900: `1440 - 48 - 48 - 320 - 360 = 664px` center column, which clears the overlap.
    - .planning/phases/10-e2e-validation/10-CONTEXT.md D-02 (full chromium + firefox + webkit project matrix — must apply the override to all 3, no project-skipping).
    - planning/PLAN.md §10 (`Responsive but desktop-first: optimized for wide screens, functional on tablet`) — establishes 1440×900 as the documented design target this task aligns with.
    - .planning/phases/10-e2e-validation/10-08-PLAN.md (the immediately superseded prior plan — confirms the pointerEvents fix is independently correct UX and must not be reverted; this plan does not touch Heatmap.tsx for that reason).
    - CLAUDE.md root rules — "Identify root cause before fixing issues. Prove with evidence, then fix." The verifier proved the root cause; this fix addresses it directly with the smallest possible change.
  </read_first>
  <action>
    Make exactly one structural edit to `test/playwright.config.ts`: replace the projects array at lines 71-75 with a version that adds an explicit `viewport: { width: 1440, height: 900 }` to each of the 3 project `use:` blocks. The `...devices['Desktop Chrome' | 'Desktop Firefox' | 'Desktop Safari']` spread stays in place; the viewport override appears AFTER the spread so the override wins (Playwright merges left-to-right and the later property wins).

    **Edit — replace lines 71-75 verbatim.**

    Current state (lines 71-75):
    ```typescript
      projects: [
        { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
        { name: 'firefox',  use: { ...devices['Desktop Firefox'] } },
        { name: 'webkit',   use: { ...devices['Desktop Safari'] } },
      ],
    ```

    Replace those 5 lines with this block (preserve the same 2-space indentation that matches the surrounding `defineConfig({...})` body, and the same column alignment of `name:` / `use:` to keep the diff minimal):
    ```typescript
      projects: [
        // CONTEXT D-02 full browser matrix. Explicit viewport: 1440x900 overrides
        // the default 1280x720 from devices['Desktop Chrome' | 'Desktop Firefox'
        // | 'Desktop Safari'] and aligns the test environment with the production
        // design contract from `planning/PLAN.md` §10 ("desktop-first ... wide
        // screens"). Below 1440 the Terminal.tsx 3-col grid (320px / 1fr / 360px)
        // plus the ChatDrawer flex sibling overflows and the right-column
        // PositionsTable visually overlaps the center-column TabBar (10-VERIFICATION
        // .md Mode A — corrected diagnosis). Override is per-project and applied
        // after the device spread so it wins.
        { name: 'chromium', use: { ...devices['Desktop Chrome'],  viewport: { width: 1440, height: 900 } } },
        { name: 'firefox',  use: { ...devices['Desktop Firefox'], viewport: { width: 1440, height: 900 } } },
        { name: 'webkit',   use: { ...devices['Desktop Safari'],  viewport: { width: 1440, height: 900 } } },
      ],
    ```

    Notes:
    - Each project line keeps the existing alphabetical ordering (chromium, firefox, webkit) and the same alignment style (the trailing `webkit` device is `Desktop Safari` per Playwright convention — preserved).
    - Do NOT change the top-level `defineConfig({...})`, the `use:` block at lines 43-62 (which sets baseURL, trace, screenshot, video, actionTimeout — these stay untouched), or the `timeout`, `outputDir`, `reporter`, `workers`, `fullyParallel`, `forbidOnly`, or `retries` keys.
    - Do NOT change `testDir`, `testMatch`, or remove any existing comment in the file. Only the projects array changes.
    - Do NOT add a top-level `use: { viewport: ... }` (Playwright would apply it to ALL projects but it would be overridden again by the per-project `...devices[...]` spread which carries its own viewport — the per-project override after the spread is the correct shape).

    After the edit, run `cd test && npx playwright test --list 2>&1 | tail -3` (no actual run) to confirm the config still parses cleanly. The list output should show 21 tests across 3 projects.
  </action>
  <verify>
    <automated>grep -c "viewport: { width: 1440, height: 900 }" test/playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "viewport: { width: 1440, height: 900 }" test/playwright.config.ts` outputs `3` (one occurrence per project — chromium, firefox, webkit; no project skipped).
    - `grep -c "...devices\['Desktop Chrome'\]" test/playwright.config.ts` outputs `1` (the chromium device spread is preserved).
    - `grep -c "...devices\['Desktop Firefox'\]" test/playwright.config.ts` outputs `1` (the firefox device spread is preserved).
    - `grep -c "...devices\['Desktop Safari'\]" test/playwright.config.ts` outputs `1` (the webkit device spread is preserved — Desktop Safari is the Playwright convention for webkit).
    - `grep -c "name: 'chromium'" test/playwright.config.ts` outputs `1` AND `grep -c "name: 'firefox'" test/playwright.config.ts` outputs `1` AND `grep -c "name: 'webkit'" test/playwright.config.ts` outputs `1` (all 3 project names preserved exactly once).
    - `grep -c "workers: 1" test/playwright.config.ts` outputs `1` (10-06's workers: 1 setting preserved — depends_on ordering enforces this should already be in place; sanity-check).
    - `grep -c "baseURL: process.env.BASE_URL" test/playwright.config.ts` outputs `1` (10-01/D-09 baseURL wiring preserved).
    - `cd test && npx playwright test --list 2>&1 | grep -E 'Total: 21 tests in 7 files'` exits 0 (21 tests across 7 spec files × 3 projects — config still parses, no project lost).
  </acceptance_criteria>
  <done>The projects array contains an explicit `viewport: { width: 1440, height: 900 }` for each of the 3 projects, applied after the device spread so the override wins. The `use:` block, timeout, retries, workers, and reporter config are all preserved unchanged. `npx playwright test --list` reports 21 tests across 7 files.</done>
</task>

<task type="auto">
  <name>Task 2: Test-environment fix — add `tmpfs: - /app/db` to the appsvc service in docker-compose.test.yml and rewrite the misleading volume comment (closes Mode A.2)</name>
  <files>test/docker-compose.test.yml</files>
  <read_first>
    - test/docker-compose.test.yml (current state — full file; particularly the appsvc service block at lines 16-42 and the misleading comment at lines 31-33 which is what this task corrects)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md gaps[1] (Mode A.2 — cross-RUN SQLite carry-over). Confirms by direct DOM evidence (page accessibility snapshot at `test/test-results/05-portfolio-viz-portfolio-3f443-der-after-a-position-exists-chromium/error-context.md:189`) that the appsvc carries `Total: $10,005.38, Cash: $200.56` plus 4 carry-over positions (META qty 9, NVDA qty 4, JPM qty 7, AMZN qty 4) and a 33-minute chat-history span — proving anonymous volumes from prior `up` invocations are being reused.
    - Dockerfile (line 57 — `VOLUME /app/db`). This declaration MUST stay (Phase 9 OPS-02 production persistence requirement); only the test compose overrides it. Confirm by `grep -n "VOLUME /app/db" Dockerfile` returns line 57 BEFORE the edit AND AFTER the edit (this task does not touch the Dockerfile).
    - .planning/phases/10-e2e-validation/10-CONTEXT.md D-03 (canonical command — single `up`, NO per-project compose orchestration, NO compose-down-and-up wrapper). The tmpfs path is the only fix consistent with leaving D-03's command unchanged.
    - .planning/phases/10-e2e-validation/10-CONTEXT.md D-06 (no test-only `/api/test/reset` endpoint). Tmpfs honors this — the per-`up` ephemerality is achieved at the container-storage layer, not at the application layer.
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md gaps[1].missing[0] (recommended fix path (a) — tmpfs at /app/db). This is the verifier's preferred fix and is what this task implements.
    - CLAUDE.md root rules — "Be simple. Approach tasks in a simple, incremental way." Tmpfs is the simplest fix: zero host filesystem touch, zero changes to the canonical command, zero application-layer changes.
  </read_first>
  <action>
    Make exactly two structural edits to `test/docker-compose.test.yml`:

    **Edit 1 — replace the misleading comment block at lines 31-33.**

    Current state (lines 31-33):
    ```yaml
        # D-06: NO `volumes:` mapping for /app/db -> compose creates a fresh
        # anonymous volume per `up` invocation. Each run starts with a clean
        # SQLite (Phase 2 lazy init re-seeds the 10-ticker watchlist + $10k cash).
    ```

    Replace those 3 lines with the corrected comment (use the same 4-space indentation that matches the surrounding `appsvc:` service body):
    ```yaml
        # D-06: tmpfs at /app/db gives each `docker compose up` a fresh, empty
        # in-memory directory mounted over the Dockerfile's `VOLUME /app/db`
        # declaration. SQLite vanishes per container stop (FastAPI lifespan
        # re-seeds the 10-ticker watchlist + $10k cash on the next start).
        # The earlier `no volumes: mapping` approach was wrong: `compose up
        # --abort-on-container-exit` does NOT remove anonymous volumes (only
        # `compose down -v` does), so prior runs' SQLite carry-over leaked
        # across `up` invocations and broke ROADMAP SC#3's `reproducibly`
        # clause (10-VERIFICATION.md Mode A.2). Production `Dockerfile:57
        # VOLUME /app/db` is preserved unchanged for production persistence
        # (Phase 9 OPS-02); only this test-side compose overrides it.
    ```

    **Edit 2 — add a `tmpfs:` block to the appsvc service.**

    Insert the `tmpfs:` block between the `environment:` block (which ends at line 30 with `PYTHONUNBUFFERED: "1"`) and the new comment block from Edit 1. After both edits, the appsvc service body (between `services: appsvc:` and the `healthcheck:` line) should read in this order:

    1. `build:` block (unchanged from current lines 18-22)
    2. `environment:` block (unchanged from current lines 25-30)
    3. New `tmpfs:` block (this edit) — exactly:
       ```yaml
           tmpfs:
             - /app/db
       ```
    4. The corrected D-06 comment block (Edit 1)
    5. `# D-10:` comment (unchanged from current line 34)
    6. `healthcheck:` block (unchanged from current lines 36-42)

    The `tmpfs:` directive uses 4-space indentation under `appsvc:` (matches `environment:`, `build:`, `healthcheck:`); the `- /app/db` list item uses 6-space indentation (matches the existing list-item style under `volumes:` in the playwright service at lines 50-51).

    Notes:
    - Do NOT touch the playwright service block (lines 44-68). It still owns the host bind-mount `../test:/work`, the `BASE_URL` env, `CI: "1"`, and the `npm ci && npx playwright test --pass-with-no-tests` command. None of those change.
    - Do NOT touch the `healthcheck:` block on appsvc. The python3 urllib check + 5s interval + 12 retries + 15s start_period are all preserved.
    - Do NOT remove the `DB_PATH: /app/db/finally.db` env. The tmpfs is mounted at `/app/db`, so the SQLite file at `/app/db/finally.db` ends up on the tmpfs as expected.
    - Do NOT add a `volumes:` block to appsvc. tmpfs is the right primitive (no host filesystem touch); a named-volume + `down -v` flow would require changing CONTEXT D-03's canonical command, which is locked.
    - Do NOT add a `restart:` policy or `deploy:` block — those are out of scope.

    After the edits, run `docker compose -f test/docker-compose.test.yml config 2>&1 | tail -20` to confirm the YAML still parses. The output must include the `tmpfs:` directive under the appsvc service and must not emit any error tokens.
  </action>
  <verify>
    <automated>grep -A1 'tmpfs:' test/docker-compose.test.yml | grep -c '/app/db'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A1 'tmpfs:' test/docker-compose.test.yml | grep -c '/app/db'` outputs at least `1` (the tmpfs directive lists `/app/db` immediately under it).
    - `grep -c '^\s*tmpfs:\s*$' test/docker-compose.test.yml` outputs `1` (a `tmpfs:` line exists exactly once at service-level indentation).
    - `grep -c '/app/db' test/docker-compose.test.yml` outputs at least `2` (the existing `DB_PATH: /app/db/finally.db` env on line 29 plus the new `- /app/db` tmpfs entry; comments referring to /app/db may add more — that is fine).
    - `grep -c 'compose creates a fresh' test/docker-compose.test.yml` outputs `0` (the misleading wording from the old line 31 comment is gone).
    - `grep -c 'tmpfs at /app/db' test/docker-compose.test.yml` outputs `1` (the new comment's distinctive phrase is present, uniquely greppable).
    - `grep -c 'D-06' test/docker-compose.test.yml` outputs at least `1` (CONTEXT D-06 is still cited in the comment block; the executor may expand the citation count).
    - `grep -c 'VOLUME /app/db' Dockerfile` outputs `1` (the production Dockerfile is unchanged — this plan does NOT touch the Dockerfile).
    - `grep -c 'condition: service_healthy' test/docker-compose.test.yml` outputs `1` (the playwright `depends_on` healthcheck wiring is preserved).
    - `grep -c 'BASE_URL: "http://appsvc:8000"' test/docker-compose.test.yml` outputs `1` (the playwright service env is preserved).
    - `grep -c 'image: mcr.microsoft.com/playwright' test/docker-compose.test.yml` outputs `1` (the playwright image pin is preserved).
    - `docker compose -f test/docker-compose.test.yml config 2>&1 | grep -c 'tmpfs'` outputs at least `1` (compose normalises tmpfs into its config dump — proves the YAML parses and the directive is recognised).
  </acceptance_criteria>
  <done>The `appsvc` service in `test/docker-compose.test.yml` declares `tmpfs: - /app/db`. The misleading line 31-33 comment block ("compose creates a fresh anonymous volume per `up` invocation") is replaced with a correct comment that documents tmpfs ephemerality, references CONTEXT D-06, and notes the production Dockerfile VOLUME is preserved. The Dockerfile is untouched. The healthcheck, environment vars, build context, and playwright service are all preserved unchanged. `docker compose -f test/docker-compose.test.yml config` parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: WR-01 advisory polish — add a `formatter` prop to the Heatmap Tooltip so position weights render as `$1,234.56` (preserves 10-08's pointerEvents fix unchanged)</name>
  <files>frontend/src/components/portfolio/Heatmap.tsx</files>
  <read_first>
    - frontend/src/components/portfolio/Heatmap.tsx (current state — full file; particularly the import block at line 10 which already imports `Tooltip` from recharts after 10-08, and the Tooltip element at line 138 which this task adds the formatter prop to)
    - .planning/phases/10-e2e-validation/10-REVIEW.md WR-01 advisory — establishes the gap (default Recharts tooltip body renders the raw `weight` float as a bare number with no `$`, no commas, no rounding) and the recommended fix (a `formatter` prop returning a dollar-formatted string).
    - .planning/phases/10-e2e-validation/10-08-PLAN.md Task 1 — confirms the existing `<Tooltip wrapperStyle={{ pointerEvents: 'none' }} />` is correct and must be preserved unchanged. This task ADDS a prop; it does NOT replace the existing element.
    - .planning/phases/10-e2e-validation/10-08-SUMMARY.md — confirms the 10-08 Tooltip fix is in place and that adding a formatter is non-blocking for SC#3 (this task is bundled here only because the file is already touched in spirit by `git log` for the same phase; it is the only WR-01 fix bundled with this gap closure).
    - planning/PLAN.md §10 (`Position panel ... tabular view of all positions: ticker, quantity, avg cost, current price, unrealized P&L, % change`) — confirms dollar-formatted display is the correct convention for position values.
    - CLAUDE.md root rules — "Favor short modules, short methods and functions. Name things clearly." The formatter is a single inline arrow function; do not extract it to a top-level helper unless the codebase already has one.
    - frontend/src/lib/format.ts (if it exists) — check via `ls frontend/src/lib/format.ts 2>&1 | grep -v "No such"` AND `grep -n "formatCurrency\|formatDollars\|formatUsd" frontend/src/lib/format.ts 2>/dev/null`. If a currency formatter helper already exists in the codebase, USE IT instead of inlining `Intl.NumberFormat`. Reuse over reinvention.
  </read_first>
  <action>
    Make exactly one structural edit to `frontend/src/components/portfolio/Heatmap.tsx`: add a `formatter` prop to the existing `<Tooltip>` element at line 138. The `wrapperStyle={{ pointerEvents: 'none' }}` prop from 10-08 is preserved unchanged.

    **Discovery step (executor MUST do this BEFORE editing).**

    Run `ls frontend/src/lib/format.ts 2>&1` and `grep -rn "formatCurrency\|formatDollars\|formatUsd\|Intl.NumberFormat.*currency" frontend/src --include='*.ts' --include='*.tsx'` to discover whether a currency formatter helper already exists in the codebase. Two outcomes:

    **Outcome A: a currency formatter helper exists** (e.g., `formatCurrency` in `frontend/src/lib/format.ts` or similar). Use it. Add an import for it at the top of `Heatmap.tsx` (alphabetical with the other relative imports), and reference it in the Tooltip formatter prop:
    ```tsx
    <Tooltip
      wrapperStyle={{ pointerEvents: 'none' }}
      formatter={(value: number) => formatCurrency(value)}
    />
    ```

    **Outcome B: no currency formatter helper exists** (or the search returns nothing or only unrelated matches). Inline `Intl.NumberFormat` directly in the formatter. Use the en-US locale + USD currency style + 2 fraction digits (the project is single-currency USD per `planning/PLAN.md §1`):
    ```tsx
    <Tooltip
      wrapperStyle={{ pointerEvents: 'none' }}
      formatter={(value: number) =>
        new Intl.NumberFormat('en-US', {
          style: 'currency',
          currency: 'USD',
          minimumFractionDigits: 2,
          maximumFractionDigits: 2,
        }).format(value)
      }
    />
    ```

    **Edit — replace line 138 only.**

    Current state (line 138):
    ```tsx
                <Tooltip wrapperStyle={{ pointerEvents: 'none' }} />
    ```

    Replace it with the multi-line element from Outcome A or Outcome B above (use 16-space indentation for the opening `<Tooltip` to match the existing element's column position; use 18-space indentation for the prop lines to keep the JSX readable).

    Notes:
    - The `formatter` prop is a Recharts API: it receives the data value (the `weight` from `dataKey="weight"` on `<Treemap>` — a number representing the position's dollar value) and returns the React node to render in the tooltip body. Returning a string is fine; Recharts wraps it in a default `<li>` per its DefaultTooltipContent contract.
    - Do NOT change the `wrapperStyle={{ pointerEvents: 'none' }}` prop. It MUST be preserved exactly — the Mode A pointerEvents fix from 10-08 is independent of this WR-01 polish.
    - Do NOT change the explanatory comment block at lines 128-137 (the `wrapperStyle` rationale). Leave it as is.
    - Do NOT add a `labelFormatter` prop, a `cursor` prop, or a `content` prop. Only `formatter` is added.
    - Do NOT touch the `<Treemap>` props (data, dataKey, stroke, content, isAnimationActive, animationDuration, onClick — all preserved).
    - Do NOT touch any other file. The watchlist sparkline component, the PnLChart, the HeatmapCell, and all tests stay unchanged.

    After the edit, run `cd frontend && npm run build` to confirm the change compiles cleanly. The build must succeed (any tsc error means the prop type or import was introduced incorrectly and must be fixed before commit).
  </action>
  <verify>
    <automated>grep -c 'formatter=' frontend/src/components/portfolio/Heatmap.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'formatter=' frontend/src/components/portfolio/Heatmap.tsx` outputs at least `1` (the formatter prop landed on the Tooltip element).
    - `grep -c "wrapperStyle={{ pointerEvents: 'none' }}" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (the 10-08 pointerEvents fix is preserved unchanged).
    - `grep -c '<Tooltip' frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (the Tooltip element is rendered exactly once; the formatter was added inline to the existing element, not added as a duplicate Tooltip).
    - `grep -c '</Tooltip>' frontend/src/components/portfolio/Heatmap.tsx` plus the count of self-closing `<Tooltip ... />` (via `grep -c '<Tooltip[^>]*/>' frontend/src/components/portfolio/Heatmap.tsx`) sums to exactly `1` (Tooltip is closed exactly once whether self-closing or paired).
    - `grep -c '<Treemap' frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (Treemap still rendered exactly once).
    - `grep -c "import { ResponsiveContainer, Tooltip, Treemap } from 'recharts'" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (the recharts import block is unchanged from 10-08; Tooltip already imported, no duplicate).
    - `grep -c 'data-testid="heatmap-treemap"' frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (the test-id used by 05-portfolio-viz.spec.ts is preserved).
    - Either `grep -c 'formatCurrency' frontend/src/components/portfolio/Heatmap.tsx` outputs at least `1` (Outcome A — helper reused) OR `grep -c "style: 'currency'" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` AND `grep -c "currency: 'USD'" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (Outcome B — Intl.NumberFormat inlined). Exactly one outcome is satisfied.
    - `cd frontend && npm run build 2>&1 | tail -1` includes no `Failed to compile` and no `error` token. The build either completes successfully or the task is blocked.
  </acceptance_criteria>
  <done>The existing `<Tooltip>` element in `Heatmap.tsx` gains a `formatter` prop that renders the position weight as a USD-formatted dollar string. The `wrapperStyle={{ pointerEvents: 'none' }}` prop from 10-08 is preserved unchanged. The Treemap props, the data-testid, the recharts import, and all surrounding component structure are unchanged. `npm run build` is green.</done>
</task>

<task type="auto">
  <name>Task 4: Verification gate — run the canonical command verbatim and prove 21 passed / 0 failed / 0 flaky / appsvc Healthy / exit 0</name>
  <files>(no files modified — verification only; captures /tmp/phase10-final-harness.log as evidence for the SUMMARY)</files>
  <read_first>
    - .planning/phases/10-e2e-validation/10-CONTEXT.md D-03 (canonical command verbatim — `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright`. Do NOT add flags. Do NOT use `-p chromium` or any spec filter. The whole point is the unmodified D-03 invocation.)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md (lists today's 3 hard failures + 0 flaky; this task proves all 3 hard failures close once Mode A and Mode A.2 are fixed by Tasks 1-2 and 0 flaky is preserved)
    - test/playwright.config.ts (must contain `viewport: { width: 1440, height: 900 }` exactly 3 times after Task 1 of this plan)
    - test/docker-compose.test.yml (must contain `tmpfs:` followed by `- /app/db` after Task 2 of this plan)
    - frontend/src/components/portfolio/Heatmap.tsx (must contain `formatter=` and preserve `wrapperStyle={{ pointerEvents: 'none' }}` after Task 3 of this plan)
    - test/05-portfolio-viz.spec.ts (must already have the `dismissChartTooltip` helper from Plan 10-07 Task 1 — depends_on enforces ordering. After Tasks 1-2 of this plan land, the helper becomes belt-and-suspenders rather than the primary fix; that is fine — leave it in place)
    - test/01-fresh-start.spec.ts (must already have NO `$10,000.00` literal after Plan 10-08 — depends_on enforces ordering; sanity-check)
    - test/04-sell.spec.ts (must already have `toBeGreaterThanOrEqual(2)` after Plan 10-08 — depends_on enforces ordering; sanity-check)
    - CLAUDE.md root rules — "Try one test at a time. Be methodical. Don't jump to conclusions. Don't apply workarounds." If this task fails, follow the same protocol Plan 10-08 established: stop, capture evidence, surface a fresh VERIFICATION.md update; do NOT silently retry or add ad-hoc flags.
  </read_first>
  <action>
    Execute the canonical Phase 10 harness command from the repo root and capture its full output. This task IS the SC#3 gate. The command is fixed by CONTEXT.md D-03 — do not substitute, do not add flags, do not change the working directory away from the repo root:

    ```
    docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright
    ```

    Capture stdout+stderr to `/tmp/phase10-final-harness.log` so the SUMMARY can reference exact lines from the run. Capture the exit code separately into a shell variable. The recommended invocation pattern (matches what 10-07 / 10-08 used and what prior harness logs were captured with):

    ```bash
    docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright 2>&1 | tee /tmp/phase10-final-harness.log
    EXIT_CODE=${PIPESTATUS[0]}
    echo "Harness exit code: $EXIT_CODE"
    ```

    After the command completes:

    1. **Check the exit code is `0`.** If non-zero:
       - Do NOT mark this task done.
       - Do NOT silently retry (no `-p chromium`, no `--retries=2`, no spec filter — those mask root cause).
       - Inspect `/tmp/phase10-final-harness.log` to identify the failed (spec, project) pair(s).
       - If the failure is in 05-portfolio-viz on any browser AND mentions a `<td class="px-4 font-semibold">` interceptor, the viewport bump from Task 1 may not have taken effect. Verify by `grep -c "viewport: { width: 1440, height: 900 }" test/playwright.config.ts` returns `3` AND that the harness log shows the Playwright run launched browsers at the new viewport (Playwright logs viewport on test start in trace mode; alternatively, check `test-results/.../trace.zip`).
       - If the failure is `400 Bad Request` on a buy or sell action, the tmpfs from Task 2 may not have taken effect. Verify by `grep -c "tmpfs:" test/docker-compose.test.yml` returns `1` AND that `docker compose -f test/docker-compose.test.yml config` shows the tmpfs in its normalised output. Also verify by inspecting the appsvc container's `/app/db` after the run (e.g., `docker compose -f test/docker-compose.test.yml exec appsvc ls -la /app/db` — but only as diagnostic; do not run during a green-gate measurement).
       - If the failure surfaces in a file NEITHER this plan nor 10-06/10-07/10-08 touched, that is a NEW gap and should be surfaced via a fresh VERIFICATION.md update (NOT silently retried within this task).

    2. **Verify the harness summary line is `21 passed`.** Playwright's list reporter ends with a line like `21 passed (1.5m)`. The acceptance criteria below assert four distinct invariants that together prove SC#3:
       - `21 passed` appears (count must be exactly 21 — proves all 7 spec files ran for all 3 browser projects).
       - The pattern ` failed` (with leading space, to avoid matching unrelated tokens) does NOT appear in the harness summary section. If a single spec failed, Playwright prints `X failed` somewhere in the tail; absence of that pattern proves zero failures.
       - The string `flaky` does NOT appear (no retry markers — proves reproducibility).
       - `Container test-appsvc-1\s+Healthy` appears (proves the appsvc container reached healthy state, ruling out infrastructure issues).

    3. **Verify reproducibility (Mode A.2 closure proof).** Re-run the canonical command a second time without any cleanup between runs:
       ```bash
       docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright 2>&1 | tee /tmp/phase10-final-harness-rerun.log
       SECOND_EXIT=${PIPESTATUS[0]}
       echo "Second harness exit code: $SECOND_EXIT"
       ```
       Both runs must independently exit 0 with `21 passed` / `0 failed` / `0 flaky`. The second run is the reproducibility proof for Mode A.2 — if tmpfs is working correctly, the second run starts with a fresh `/app/db` regardless of what the first run wrote. Capture both logs.

    4. **Do NOT delete `/tmp/phase10-final-harness.log` or `/tmp/phase10-final-harness-rerun.log`.** The SUMMARY (the implicit final step in the `<output>` block of this plan) MUST reference both as evidence and copy the exact `21 passed (...)` line and the exact `Container test-appsvc-1 Healthy` line from each into the SUMMARY's "harness-gate result" subsection. The two log files together are the verifier's audit trail for both SC#3 (single canonical command finishes green) and the `reproducibly` clause.

    5. **If both gate runs are green:** the task is complete. Phase 10 ROADMAP SC#3 is met. Proceed to write the SUMMARY per the `<output>` block below.
  </action>
  <verify>
    <automated>docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright 2>&1 | tee /tmp/phase10-final-harness.log | tail -10 | grep -E '21 passed' && ! grep -E ' [1-9][0-9]* failed' /tmp/phase10-final-harness.log && ! grep -cE 'flaky' /tmp/phase10-final-harness.log | grep -E '^[1-9]' && grep -E 'Container test-appsvc-1\s+Healthy' /tmp/phase10-final-harness.log</automated>
  </verify>
  <acceptance_criteria>
    - The canonical command `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 (captured via `${PIPESTATUS[0]}` immediately after the command) on the FIRST run.
    - The same canonical command exits 0 on a SECOND run (no cleanup between runs — proves Mode A.2 closure / `reproducibly` clause).
    - `grep -E '21 passed' /tmp/phase10-final-harness.log` exits 0 (Playwright reports exactly 21 passing tests — 7 specs × 3 browser projects) on the FIRST run.
    - `grep -E '21 passed' /tmp/phase10-final-harness-rerun.log` exits 0 on the SECOND run.
    - `grep -E ' [1-9][0-9]* failed' /tmp/phase10-final-harness.log` exits non-zero (no `N failed` line where N is positive — zero failures) on the FIRST run.
    - `grep -E ' [1-9][0-9]* failed' /tmp/phase10-final-harness-rerun.log` exits non-zero on the SECOND run.
    - `grep -c 'flaky' /tmp/phase10-final-harness.log` outputs `0` (no `flaky` anywhere — no retries — Modes A/A.2 fully closed, not just masked by retry) on the FIRST run.
    - `grep -c 'flaky' /tmp/phase10-final-harness-rerun.log` outputs `0` on the SECOND run.
    - `grep -c 'No tests found' /tmp/phase10-final-harness.log` outputs `0` (the `--pass-with-no-tests` flag from Plan 10-01 should not be triggered now that all 7 specs exist).
    - `grep -E 'Container test-appsvc-1\s+Healthy' /tmp/phase10-final-harness.log` exits 0 (the appsvc container reported healthy on the FIRST run — proves the HSTS rename + healthcheck wiring + tmpfs mount all coexist).
    - `grep -E 'Container test-appsvc-1\s+Healthy' /tmp/phase10-final-harness-rerun.log` exits 0 on the SECOND run.
    - The log shows results for all 3 browser projects: `grep -c '\[chromium\]\|\[firefox\]\|\[webkit\]' /tmp/phase10-final-harness.log` outputs at least `21` (every test entry tagged with a project, no project skipped).
    - `/tmp/phase10-final-harness.log` AND `/tmp/phase10-final-harness-rerun.log` both exist, are non-empty, and contain `21 passed`. The SUMMARY (created via the `<output>` block) references both.
  </acceptance_criteria>
  <done>Canonical command exits 0 on two consecutive runs with no inter-run cleanup; both runs report `21 passed` / `0 failed` / `0 flaky` across all 3 browser projects; appsvc Healthy on both; both logs captured at `/tmp/phase10-final-harness.log` and `/tmp/phase10-final-harness-rerun.log` for SUMMARY evidence; ROADMAP Phase 10 SC#3 met (including the `reproducibly` clause).</done>
</task>

</tasks>

<verification>
This plan owns the authoritative ROADMAP Phase 10 SC#3 verification gate. Task 4's automated command IS the gate, plus the second-run requirement that gives the `reproducibly` clause teeth. Tasks 1-3 are the three independent fixes the verifier identified (Tasks 1-2 are blocking; Task 3 is the bundled WR-01 polish).

Cross-cutting verification (run before declaring 10-09 complete):
- `git diff --stat test/playwright.config.ts test/docker-compose.test.yml frontend/src/components/portfolio/Heatmap.tsx` shows exactly 3 files changed; no other files touched by this plan.
- The Plan 10-06 / 10-07 / 10-08 edits MUST be present (depends_on: [10-06, 10-07, 10-08] enforces ordering). Sanity-check:
  - `grep -E '^\s*workers:\s*1\b' test/playwright.config.ts` exits 0 (10-06 Task 1)
  - `grep -c "getByTestId('watchlist-panel')" test/01-fresh-start.spec.ts` outputs at least `2` (10-06 Task 2)
  - `grep -c '\$10,000.00' test/03-buy.spec.ts` outputs `0` (10-06 Task 3)
  - `grep -c '\$10,000.00' test/01-fresh-start.spec.ts` outputs `0` (10-08 Task 2)
  - `grep -c 'toBeGreaterThanOrEqual(2)' test/04-sell.spec.ts` outputs `1` (10-08 Task 3)
  - `grep -c 'dismissChartTooltip' test/05-portfolio-viz.spec.ts` outputs at least `2` (10-07 Task 1; helper stays in place as belt-and-suspenders)
  - `grep -c "wrapperStyle={{ pointerEvents: 'none' }}" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (10-08 Task 1; preserved through Task 3 of this plan)
- The Dockerfile is unchanged: `grep -c 'VOLUME /app/db' Dockerfile` outputs `1`.
- `/tmp/phase10-final-harness.log` and `/tmp/phase10-final-harness-rerun.log` both exist, both contain `21 passed`, and both contain `Container test-appsvc-1 Healthy`.
- After Task 4 succeeds, `git status` shows only the 3 modified files staged (or already committed via per-task atomic commits).
</verification>

<success_criteria>
- `test/playwright.config.ts` projects array contains `viewport: { width: 1440, height: 900 }` exactly 3 times — one per project — and the `...devices['Desktop Chrome' | 'Desktop Firefox' | 'Desktop Safari']` spreads are preserved.
- `test/docker-compose.test.yml` appsvc service declares `tmpfs: - /app/db`. The misleading "anonymous volume per `up`" comment is replaced with a correct comment that documents tmpfs ephemerality and the production VOLUME preservation.
- `frontend/src/components/portfolio/Heatmap.tsx` `<Tooltip>` element gains a `formatter` prop returning a USD dollar-formatted string. The `wrapperStyle={{ pointerEvents: 'none' }}` prop from 10-08 is preserved unchanged.
- The canonical command `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 on two consecutive runs with no inter-run cleanup.
- Both runs report `21 passed`, zero failures, zero flaky retries, across chromium + firefox + webkit. appsvc container Healthy on both runs.
- `/tmp/phase10-final-harness.log` and `/tmp/phase10-final-harness-rerun.log` capture the runs; the SUMMARY copies the `21 passed` and `Healthy` lines from both into its evidence block.
- The Dockerfile is unchanged (production `VOLUME /app/db` preserved).
- ROADMAP Phase 10 SC#3 ("single command finishes green reproducibly") is met. Phase 10 verification status will move from `gaps_found` to `verified` on the next verifier pass (this plan does NOT write VERIFICATION.md itself).
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-validation/10-09-SUMMARY.md` per the standard summary template (`templates/summary.md`). The SUMMARY MUST include:

- **frontmatter**:
  - `phase: 10-e2e-validation`
  - `plan: 09`
  - `subsystem: testing`
  - `tags: [playwright, e2e, gap-closure, viewport-alignment, sqlite-tmpfs, recharts-tooltip-formatter]`
  - `requires:` Plan 10-06 (Mode B+C v1 closure — workers: 1, scoped selectors, dropped $10k in 03-buy, relative-delta in 04-sell) + Plan 10-07 (Gap Group B Task 1 — `dismissChartTooltip` helper) + Plan 10-08 (Modes B+C v2 closure — dropped $10k in 01-fresh-start, expect.poll-stabilised postBuyQty, Tooltip pointerEvents fix)
  - `provides:` Mode A (corrected) closure + Mode A.2 closure + WR-01 polish + the canonical 21/21 green gate ON TWO CONSECUTIVE RUNS
  - `affects:` (none — Phase 10 is the final phase; the SUMMARY notes Phase 10 ROADMAP SC#3 is now MET upon successful 10-09 completion)
  - `tech-stack.added: []` (no new dependencies)
  - `tech-stack.patterns: ["explicit per-project Playwright viewport override aligned with documented design contract", "compose-side tmpfs to override Dockerfile VOLUME for test-only ephemerality", "Recharts Tooltip formatter for currency-aware default tooltip body"]`
  - `key-files.created: []`
  - `key-files.modified: [test/playwright.config.ts, test/docker-compose.test.yml, frontend/src/components/portfolio/Heatmap.tsx]`
  - `requirements-completed: [TEST-03, TEST-04]`
  - `duration:` actual end-to-end time
  - `completed:` ISO date

- **key-decisions** section (must include all four):
  1. **Mode A re-diagnosed: layout overlap, not Recharts tooltip.** The previous VERIFICATION.md (commit 4f690e6) blamed the Recharts default Treemap tooltip. Today's verifier (post-10-08) demonstrated by direct DOM evidence that the actual interceptor is `<td class="px-4 font-semibold">{ticker}</td>` from `frontend/src/components/terminal/PositionRow.tsx:57` — the right-column PositionsTable cell — at viewport 1280×720. The 10-08 `<Tooltip wrapperStyle={{ pointerEvents: 'none' }} />` fix is correct as latent UX hardening (it stays — do not revert) but it is not the closer for this failure shape. The actual closer is a test-environment alignment with `planning/PLAN.md §10`'s `desktop-first ... wide screens` design contract: a per-project viewport bump to 1440×900 in `playwright.config.ts`. This is alignment, not a workaround — 1440×900 is the production design target.
  2. **Mode A.2 closed at the container-storage layer, not the application layer.** The verifier proved that `docker compose up --abort-on-container-exit` does NOT remove anonymous volumes (only `compose down -v` does), so the original line 31 comment in `docker-compose.test.yml` ("compose creates a fresh anonymous volume per `up` invocation") was wrong. Three fix paths existed: (a) compose tmpfs on `/app/db`, (b) named volume + wrapper script with `down -v`, (c) test-only `/api/test/reset` endpoint. Paths (b) and (c) are blocked by locked CONTEXT decisions D-03 (no flags on canonical command, no wrapper) and D-06 (no test-only endpoint). Path (a) is the only path consistent with both locks AND it gives the line-31-comment promise teeth. The production Dockerfile `VOLUME /app/db` is preserved unchanged (Phase 9 OPS-02 production persistence requirement); only the test compose overrides it.
  3. **Bundle WR-01 advisory polish since `Heatmap.tsx` is already a touched file in this phase.** The advisory recommended a `formatter` prop on the new `<Tooltip>` so the default tooltip body shows `$1,234.56` rather than a raw float. The change is one prop addition, non-blocking for SC#3, and removing the "default tooltip body shows raw weight" UX regression is a clean follow-up for v1.0. Scoped to its own task with explicit grep-greppable acceptance criteria so the SC#3 gate (Task 4) stays unambiguous.
  4. **Canonical command run verbatim per CONTEXT D-03 — twice — to prove the `reproducibly` clause.** A single green run only proves the `single command finishes green` clause; the second run with no inter-run cleanup is what proves `reproducibly` (Mode A.2 closure). No flags, no filters, no `-p chromium`, no `--retries=2` — all of those would mask root cause. Both logs are captured for the verifier's audit trail.

- **patterns-established** section:
  - "Test-environment viewport alignment: when the production design contract documents a target viewport, the Playwright `projects` array must explicitly set that viewport per project (after the device spread) rather than inheriting the device default. Aligns test conditions with production design intent and avoids spurious layout-overlap failures."
  - "Compose-side tmpfs to enforce per-`up` ephemerality without changing the canonical command: `tmpfs: - /app/db` in `docker-compose.test.yml` overrides the Dockerfile's `VOLUME /app/db` for the test stack only. Production deploys still persist; test runs always start fresh. No host filesystem touch, no wrapper scripts, no application-layer test-only endpoints."
  - "Recharts default tooltip body formatting: when using `<Tooltip>` without a custom `content` component, the `formatter` prop is the smallest API for currency / unit / precision formatting of the data value. Pairs cleanly with `wrapperStyle` for pointer-event control."

- **harness-gate result** subsection: copy the EXACT `21 passed (...)` line and the EXACT `Container test-appsvc-1 ... Healthy` line from BOTH `/tmp/phase10-final-harness.log` AND `/tmp/phase10-final-harness-rerun.log` so the SUMMARY is self-contained evidence for both the `single command` and `reproducibly` clauses of SC#3. Include the harness exit codes (0 for both runs).

- **Phase 10 closure note**: "ROADMAP Phase 10 SC#3 (single command finishes green reproducibly) is now MET on two consecutive canonical-command runs with no inter-run cleanup. TEST-03 and TEST-04 will be ticked in REQUIREMENTS.md by the verifier on its next pass — this plan does not write VERIFICATION.md itself."

- **Self-Check** (per the template's standard checklist), with each line greppable. Examples:
  - `[ ] test/playwright.config.ts contains "viewport: { width: 1440, height: 900 }" exactly 3 times` (verified by `grep -c`)
  - `[ ] test/docker-compose.test.yml contains tmpfs: directive on appsvc service` (verified by `grep -c 'tmpfs:'` outputs `1`)
  - `[ ] Dockerfile is unchanged: grep -c 'VOLUME /app/db' Dockerfile outputs 1` (production persistence preserved)
  - `[ ] frontend/src/components/portfolio/Heatmap.tsx contains formatter= AND wrapperStyle={{ pointerEvents: 'none' }}` (verified by `grep -c` for both — both >= 1)
  - `[ ] /tmp/phase10-final-harness.log exists and contains 21 passed AND Container test-appsvc-1 Healthy`
  - `[ ] /tmp/phase10-final-harness-rerun.log exists and contains 21 passed AND Container test-appsvc-1 Healthy`
  - `[ ] git diff HEAD~3 HEAD --name-only returns exactly the 3 expected paths` (or HEAD~4 HEAD if Task 4's gate run included a docs-update commit)
  - `[ ] cd test && npx playwright test --list 2>&1 | tail -1 reports Total: 21 tests in 7 files`
</output>
