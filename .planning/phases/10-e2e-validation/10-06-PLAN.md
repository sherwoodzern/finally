---
phase: 10-e2e-validation
plan: 06
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - test/playwright.config.ts
  - test/01-fresh-start.spec.ts
  - test/03-buy.spec.ts
  - test/04-sell.spec.ts
autonomous: true
gap_closure: true
requirements: [TEST-03, TEST-04]

must_haves:
  truths:
    - "Single Playwright worker serializes all 7 spec files across all 3 browser projects (no cross-spec SQLite contention within a single `up`)."
    - "01-fresh-start asserts only on watchlist rows; the unscoped Select-button locator no longer collides with positions-table rows when prior specs created positions."
    - "03-buy makes no absolute pre-trade cash assertion; the relative `< 10000` post-trade assertion is the only cash check."
    - "04-sell asserts the post-sell quantity using a relative delta (post-buy qty minus 1), not the absolute value `1`."
    - "After Plans 10-06 and 10-07 both land, the canonical harness command exits 0 with all 21 (spec, project) pairs passing — Gap Group A from VERIFICATION.md is fully closed."
  artifacts:
    - path: "test/playwright.config.ts"
      provides: "workers: 1 (D-07 intent realized) + comment block updated to describe actual realized behavior"
      contains: "workers: 1"
    - path: "test/01-fresh-start.spec.ts"
      provides: "All `Select <ticker>` button locators scoped to `page.getByTestId('watchlist-panel')`"
      contains: "getByTestId('watchlist-panel')"
    - path: "test/03-buy.spec.ts"
      provides: "Pre-trade `$10,000.00` cash assertion removed"
      contains: "< 10_000"
    - path: "test/04-sell.spec.ts"
      provides: "Relative delta assertion: post-sell qty == post-buy qty - 1"
      contains: "post-buy"
  key_links:
    - from: "test/playwright.config.ts (workers: 1)"
      to: "shared SQLite cash_balance + positions state"
      via: "single worker process serializes all 21 (spec, project) pairs sequentially"
      pattern: "workers:\\s*1"
    - from: "test/01-fresh-start.spec.ts"
      to: "Watchlist DOM region only"
      via: "page.getByTestId('watchlist-panel').getByRole('button', ...)"
      pattern: "getByTestId\\('watchlist-panel'\\)\\.getByRole"
---

<objective>
Close VERIFICATION.md Gap Group A (8 of 9 failures): cross-spec parallelism contention plus three spec-design defects (unscoped Select-button selector, hardcoded $10k cash assertion, absolute qty=1 assertion). The simplest fix path that the verifier confirms will close all 8 failures is `workers: 1` (per CONTEXT.md D-07's stated intent) plus three small spec edits to make the assertions worker-isolated AND honest.

Purpose: realise the D-07 intent ("workers: 1 within a Playwright project") in the actual config, and tighten three spec assertions so they survive both the worker-isolated path and any future re-introduction of parallelism.

Output: edits to `test/playwright.config.ts` + `test/01-fresh-start.spec.ts` + `test/03-buy.spec.ts` + `test/04-sell.spec.ts`. Together with Plan 10-07, the canonical harness exits 0 with 21/21 (spec, project) pairs green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-e2e-validation/10-CONTEXT.md
@.planning/phases/10-e2e-validation/10-VERIFICATION.md
@.planning/phases/10-e2e-validation/10-PATTERNS.md
@.planning/phases/10-e2e-validation/10-00-SUMMARY.md
@.planning/phases/10-e2e-validation/10-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan only edits files under `test/`. The compose stack still runs `LLM_MOCK=true` against an anonymous SQLite volume on `localhost`. No new endpoints, no new secrets, no new network surface. |

## STRIDE Threat Register

No new threat surfaces introduced; modifications are limited to `test/` files and run only inside the local docker-compose.test.yml stack with LLM_MOCK=true.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Set workers=1 in playwright.config.ts and rewrite the parallelism comment block</name>
  <files>test/playwright.config.ts</files>
  <read_first>
    - test/playwright.config.ts (current state — `workers: 3` at line 26, comment block at lines 17-27)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md (artifacts[0] — explains why workers:3 contradicts D-07)
    - .planning/phases/10-e2e-validation/10-CONTEXT.md (D-07 stated intent: "workers: 1 within a Playwright project; cross-browser projects parallel")
    - .planning/phases/10-e2e-validation/10-01-SUMMARY.md (decisions section — original rationale for workers:3 + fullyParallel:false)
  </read_first>
  <action>
    Make exactly two edits to `test/playwright.config.ts`:

    1. Change `workers: 3` (currently line 26) to `workers: 1`.

    2. Replace the comment block at lines 17-27 (the block that currently starts with `// D-07: "workers: 1 within a Playwright project; cross-browser projects parallel".` and ends just before the `workers:` line) with a comment that accurately describes what `workers: 1` actually does in this harness. Use this exact replacement comment block (preserve indentation; the block must immediately precede the `workers: 1` line):

       ```
       // D-07 (corrected): A single worker process serialises ALL 21 (spec,
       // project) pairs across the 3 browser projects, eliminating cross-spec
       // contention on shared SQLite state (cash_balance, default seed-watchlist
       // positions). Cross-browser projects are still defined as separate
       // Playwright projects below; with workers: 1 they run sequentially within
       // the same `up` invocation rather than in parallel. Original config used
       // workers: 3 + fullyParallel: false in an attempt to realise D-07's
       // "workers: 1 within a Playwright project; cross-browser projects parallel"
       // wording, but Playwright does not support per-project worker caps and
       // the result was three concurrent workers picking up different spec files
       // against the same backend (8 of 9 failures in 10-VERIFICATION.md Gap
       // Group A). Single-worker serialisation is the simplest fix and is the
       // right answer for a single-user demo project — the runtime cost (a few
       // extra seconds end-to-end) is irrelevant; reproducible green is.
       ```

    Leave `fullyParallel: false` exactly as-is on the next line (it is a defensive no-op when workers=1 but is still correct documentation of intent).

    Do NOT touch any other line in the file (testDir, testMatch, retries, reporter, use, timeout, outputDir, projects). Do NOT introduce a per-spec `compose run` strategy or sharding — those are explicitly out of scope per the gap-closure-strategy received from the orchestrator.
  </action>
  <verify>
    <automated>grep -E '^\s*workers:\s*1\b' test/playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^\s*workers:\s*1\b' test/playwright.config.ts` exits 0 (the literal `workers: 1` is present at the start of a line, ignoring leading whitespace).
    - `grep -c '^\s*workers:\s*3\b' test/playwright.config.ts` outputs `0` (the old `workers: 3` is gone).
    - `grep 'D-07 (corrected)' test/playwright.config.ts` exits 0 (the new comment block landed).
    - `grep -c 'workers: 3' test/playwright.config.ts` outputs `0` (the literal string `workers: 3` does not appear anywhere, including the comment).
    - `grep -E '^\s*fullyParallel:\s*false' test/playwright.config.ts` exits 0 (the existing `fullyParallel: false` line is preserved).
    - `grep -E "^\s*name: '(chromium|firefox|webkit)'" test/playwright.config.ts | wc -l` outputs `3` (all three browser projects are still defined).
    - `cd test && npx playwright test --list 2>&1 | tail -1 | grep -E '21 tests in 7 files'` exits 0 (all 7 specs × 3 projects still discovered).
  </acceptance_criteria>
  <done>workers: 1 active; comment block accurately describes the realised single-worker serialisation; all three browser projects still listed; 21-test discovery still works.</done>
</task>

<task type="auto">
  <name>Task 2: Scope every Select-ticker locator in 01-fresh-start to the watchlist panel</name>
  <files>test/01-fresh-start.spec.ts</files>
  <read_first>
    - test/01-fresh-start.spec.ts (current state — three `getByRole('button', { name: 'Select ...' })` call sites: the loop at lines 25-29 over `SEED_TICKERS`, and the `aaplRow` locator at line 36)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md (artifacts[1] — describes the strict-mode collision when concurrent specs create positions with the same accessible name)
    - .planning/phases/10-e2e-validation/10-00-SUMMARY.md (confirms `data-testid="watchlist-panel"` is on the root `<aside>` of `Watchlist.tsx` line 27)
    - .planning/phases/10-e2e-validation/10-PATTERNS.md (selector hierarchy lines 481-495 — getByTestId is rule 1, scoping via parent testid is the established pattern)
  </read_first>
  <action>
    In `test/01-fresh-start.spec.ts`, scope ALL three `Select <ticker>` button lookups to the watchlist panel via `page.getByTestId('watchlist-panel')`. Even with `workers: 1` (Task 1) the unscoped lookup is a latent bug if any spec ahead of 01-fresh-start in the serial schedule creates a position whose ticker is in the seed (e.g., 03-buy creates NVDA, 04-sell creates JPM, 05-portfolio-viz creates META, 06-chat creates AMZN — all in the seed watchlist). The scoped form is correct under both serial and parallel execution.

    Specifically, replace the three call sites:

    1. Inside the `for (const ticker of SEED_TICKERS)` loop (currently lines 25-29), replace:
       ```typescript
           await expect(
             page.getByRole('button', { name: `Select ${ticker}` }),
           ).toBeVisible({ timeout: 10_000 });
       ```
       with:
       ```typescript
           await expect(
             page
               .getByTestId('watchlist-panel')
               .getByRole('button', { name: `Select ${ticker}` }),
           ).toBeVisible({ timeout: 10_000 });
       ```

    2. The `aaplRow` constant (currently line 36):
       ```typescript
         const aaplRow = page.getByRole('button', { name: 'Select AAPL' });
       ```
       becomes:
       ```typescript
         const aaplRow = page
           .getByTestId('watchlist-panel')
           .getByRole('button', { name: 'Select AAPL' });
       ```

    The header-cash assertion at line 32 (`page.getByTestId('header-cash').toHaveText('$10,000.00')`) and the `not.toContainText('—')` streaming-proof assertion at line 37 are CORRECT FOR 01-fresh-start because 01-fresh-start runs FIRST in alphabetical spec order and `workers: 1` (Task 1) guarantees no prior spec has mutated cash. Leave both untouched.

    No other edits.
  </action>
  <verify>
    <automated>grep -c "page.getByTestId('watchlist-panel').getByRole" test/01-fresh-start.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getByTestId('watchlist-panel')" test/01-fresh-start.spec.ts` outputs at least `2` (the loop call site and the aaplRow constant both scoped).
    - `grep -E "^\s+page\.getByRole\('button', \{ name: \`?Select " test/01-fresh-start.spec.ts | wc -l` outputs `0` (no remaining unscoped `page.getByRole('button', { name: 'Select ...' })` at the start of a line — every Select-button locator is now under the `watchlist-panel` testid scope).
    - `grep -c "page.getByTestId('header-cash')).toHaveText('\$10,000.00')" test/01-fresh-start.spec.ts` outputs `1` (header-cash assertion preserved — 01-fresh-start runs first under workers:1 so $10k is valid).
    - `grep -c "not.toContainText('—'" test/01-fresh-start.spec.ts` outputs `1` (streaming-proof assertion preserved).
    - `cd test && npx playwright test 01-fresh-start.spec.ts --list 2>&1 | grep -c 'fresh start'` outputs `3` (one entry per browser project).
  </acceptance_criteria>
  <done>All three Select-button locators scoped to `watchlist-panel`; pre-trade $10k assertion and streaming-proof assertion preserved; spec still discoverable across all 3 browser projects.</done>
</task>

<task type="auto">
  <name>Task 3: Drop the hardcoded pre-trade $10,000.00 assertion from 03-buy</name>
  <files>test/03-buy.spec.ts</files>
  <read_first>
    - test/03-buy.spec.ts (current state — pre-trade assertion at line 16, post-trade relative assertion at lines 38-40)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md (artifacts[2] — explains why hardcoded $10k is brittle; post-trade `< 10000` is sufficient)
    - .planning/phases/10-e2e-validation/10-CONTEXT.md (D-07 + D-08 — workers and per-spec ticker isolation)
  </read_first>
  <action>
    In `test/03-buy.spec.ts`, delete the pre-trade `$10,000.00` cash sanity assertion entirely (currently line 16, plus the comment block on lines 13-15 that explains it). The post-trade `cashAmount < 10_000` relative assertion at lines 38-40 is sufficient to prove the trade debited cash, and is robust to any prior state from other specs in the serial schedule (Task 1 sets workers: 1, so 03-buy still sees the seed $10k under the current schedule — but the assertion does not need to depend on that fact, and dropping it removes a fragility).

    Specifically, delete these four lines (currently 13-16):
    ```typescript
      // Pre-trade sanity: page loaded with full starting cash. Scoped to
      // header-cash because the page also renders header-total = $10,000.00 on
      // a fresh boot (no positions yet), and getByText would fail strict-mode.
      await expect(page.getByTestId('header-cash')).toHaveText('$10,000.00');
    ```

    Replace them with a single one-line comment so the next reader understands the deliberate omission:

    ```typescript
      // No pre-trade cash assertion: the post-trade `< 10_000` relative check
      // (below) is sufficient and survives any prior-state cash debits.
    ```

    Leave the rest of the file unchanged: `page.goto('/')` (line 11), the TradeBar interactions (lines 20-22), the positions-table assertion (lines 29-33), and the post-trade relative cash assertion (lines 38-40).

    No imports change; the file remains a single `test(...)` block.
  </action>
  <verify>
    <automated>test "$(grep -c '\$10,000.00' test/03-buy.spec.ts)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\$10,000.00' test/03-buy.spec.ts` outputs `0` (the hardcoded pre-trade sanity is gone).
    - `grep -c "toHaveText('\$10,000.00')" test/03-buy.spec.ts` outputs `0` (the assertion call is gone).
    - `grep -c 'No pre-trade cash assertion' test/03-buy.spec.ts` outputs `1` (the explanatory one-line comment landed).
    - `grep -c 'cashAmount).toBeLessThan(10_000)' test/03-buy.spec.ts` outputs `1` (post-trade relative assertion preserved).
    - `grep -c "page.getByLabel('Ticker').fill('NVDA')" test/03-buy.spec.ts` outputs `1` (the NVDA buy interaction is preserved).
    - `grep -c "getByTestId('positions-table')" test/03-buy.spec.ts` outputs at least `1` (positions-table scoping preserved).
    - `cd test && npx playwright test 03-buy.spec.ts --list 2>&1 | grep -c 'buy NVDA 1'` outputs `3` (one entry per browser project; spec still parses).
  </acceptance_criteria>
  <done>Pre-trade $10k assertion removed and replaced with explanatory comment; all other interactions and the post-trade relative cash check preserved; spec still discoverable across 3 browser projects.</done>
</task>

<task type="auto">
  <name>Task 4: Convert 04-sell qty assertion from absolute to relative delta</name>
  <files>test/04-sell.spec.ts</files>
  <read_first>
    - test/04-sell.spec.ts (current state — buy 2 at lines 22-27, sell 1 at lines 29-33, qty assertion at line 42)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md (artifacts[3] — explains why absolute `1` is brittle; relative delta is robust)
    - .planning/phases/10-e2e-validation/10-CONTEXT.md (D-07 + D-08 — workers and per-spec ticker isolation)
  </read_first>
  <action>
    In `test/04-sell.spec.ts`, change the post-sell quantity assertion from the absolute regex match `/^\s*1(?:\.0+)?\s*$/` to a relative delta: capture the JPM qty cell text immediately AFTER the buy succeeds (and BEFORE the sell), parse it as a number, then assert that the qty cell text AFTER the sell is exactly `(post-buy qty) - 1`. This is robust against any preceding state pollution: even if a prior spec added shares to JPM, the buy adds exactly 2 and the sell removes exactly 1, so the post-sell value is post-buy minus 1.

    Specifically:

    1. Locate the existing buy/sell flow (currently lines 22-33). After the existing `await expect(positionsTable.getByRole('button', { name: 'Select JPM' })).toBeVisible(...)` (the post-buy visibility wait at lines 25-27), add a snapshot of the JPM qty BEFORE the sell:

       ```typescript
         // Capture post-buy JPM qty so the post-sell assertion can be a relative
         // delta rather than an absolute `1`. Robust to any prior state on JPM.
         const jpmRow = positionsTable.getByRole('button', { name: 'Select JPM' });
         const jpmQty = jpmRow.locator('td').nth(1);
         const postBuyQtyText = await jpmQty.innerText();
         const postBuyQty = parseFloat(postBuyQtyText.trim());
       ```

       (`jpmRow` and `jpmQty` already exist as local consts further down at lines 40-41 — move those declarations UP to this block so they are available for both the snapshot and the final assertion. Delete the original declarations at lines 40-41 to avoid redeclaration.)

    2. Replace the final assertion (currently line 42):

       ```typescript
         await expect(jpmQty).toHaveText(/^\s*1(?:\.0+)?\s*$/, { timeout: 10_000 });
       ```

       with:

       ```typescript
         // Post-sell qty == post-buy qty - 1 (we sold 1 of the 2 we bought).
         // Use a generous timeout for the React Query refetch + render after sell.
         await expect
           .poll(
             async () => parseFloat((await jpmQty.innerText()).trim()),
             { timeout: 10_000 },
           )
           .toBe(postBuyQty - 1);
       ```

       Rationale: `expect.poll` re-evaluates the parsed numeric value until it matches `postBuyQty - 1` or times out. `toHaveText` with a regex would still work but coupling to text formatting (e.g., `1` vs `1.0` vs `1.00`) is unnecessary now that we have a clean numeric assertion path.

    3. Leave the rest of the file unchanged: imports, `test(...)` signature, the JPM × 2 buy interaction (lines 22-24), the JPM × 1 sell interaction (lines 31-33), the watchlist/positions-table scoping comments.

    Do NOT delete the existing comment block at lines 35-39 explaining column order — it documents PositionRow.tsx layout and remains useful for the next reader. Just keep it adjacent to (or moved up next to) the new snapshot/assertion logic, whichever flows more naturally.
  </action>
  <verify>
    <automated>grep -c 'postBuyQty - 1' test/04-sell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'postBuyQty - 1' test/04-sell.spec.ts` outputs `1` (the relative-delta assertion target landed).
    - `grep -c 'const postBuyQty' test/04-sell.spec.ts` outputs `1` (the post-buy qty snapshot landed).
    - `grep -c 'expect.poll' test/04-sell.spec.ts` outputs `1` (poll-based assertion present).
    - `grep -c '/\\^\\\\s\\*1(?:\\\\.0+)?\\\\s\\*\\$/' test/04-sell.spec.ts` outputs `0` (the old absolute regex is gone).
    - `grep -c "page.getByLabel('Ticker').fill('JPM')" test/04-sell.spec.ts` outputs `2` (both the buy and the sell still target JPM via the Ticker label).
    - `grep -c "getByLabel('Quantity').fill('2')" test/04-sell.spec.ts` outputs `1` (the buy is still 2).
    - `grep -c "getByLabel('Quantity').fill('1')" test/04-sell.spec.ts` outputs `1` (the sell is still 1).
    - `grep -c "getByTestId('positions-table')" test/04-sell.spec.ts` outputs at least `1` (positions-table scoping preserved).
    - `cd test && npx playwright test 04-sell.spec.ts --list 2>&1 | grep -c 'sell JPM'` outputs `3` (one entry per browser project; spec still parses).
  </acceptance_criteria>
  <done>04-sell asserts post-sell qty as post-buy qty minus 1 via expect.poll; absolute regex assertion removed; buy-2 / sell-1 interactions unchanged; spec discoverable across 3 browsers.</done>
</task>

</tasks>

<verification>
After Tasks 1-4 land, the gap-closure-strategy received from the orchestrator names Plan 10-07 as the OWNER of the canonical-command verification gate (10-07 must run AFTER 10-06 to assert the combined fix passes 21/21). 10-06 itself does NOT re-run the canonical command. Per-task acceptance criteria above are the authoritative gates for 10-06's edits; the harness gate is owned by 10-07's final task.

Cross-cutting verification (run before declaring 10-06 complete):
- `git diff --stat test/playwright.config.ts test/01-fresh-start.spec.ts test/03-buy.spec.ts test/04-sell.spec.ts` shows exactly 4 files changed, no other test files touched.
- `cd test && npx playwright test --list 2>&1 | tail -1 | grep -E '21 tests in 7 files'` exits 0 (no specs accidentally broken at parse time).
</verification>

<success_criteria>
- `test/playwright.config.ts` has `workers: 1` (replacing `workers: 3`) and an updated comment block describing the single-worker realised behaviour (no longer claims "cross-browser projects parallel" since with workers:1 they run sequentially within the same `up`).
- `test/01-fresh-start.spec.ts` scopes every `Select <ticker>` button locator to `page.getByTestId('watchlist-panel')`.
- `test/03-buy.spec.ts` no longer contains the hardcoded `$10,000.00` pre-trade assertion; the explanatory one-line comment is in its place.
- `test/04-sell.spec.ts` asserts post-sell qty via a relative delta (`postBuyQty - 1`) using `expect.poll` instead of the absolute `/^\s*1(?:\.0+)?\s*$/` regex.
- All 21 tests still discovered by `npx playwright test --list` (7 spec files × 3 browser projects); no spec accidentally broken at parse time.
- All 4 files committed atomically per task; final commit list referenced in SUMMARY.
- Combined with Plan 10-07 (tooltip dismissal), the canonical command `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 with 21/21 (spec, project) pairs passing — Gap Group A from VERIFICATION.md is fully closed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-validation/10-06-SUMMARY.md` per the standard summary template:
- key-files.modified lists exactly the 4 files touched
- requirements-completed: [TEST-03, TEST-04] (Gap Group A fixes restore the green gate that proves both)
- key-decisions notes:
  1. Chose option (a) workers: 1 over (b) per-project cap, (c) per-spec compose run, (d) sharding — simplest path; runtime cost is irrelevant for a single-user demo project
  2. Dropped 03-buy pre-trade $10k assertion entirely instead of moving it under a worker-isolated branch (post-trade relative check is sufficient)
  3. 04-sell switched to expect.poll relative-delta to decouple from text formatting variants
- patterns-established: relative-delta assertions over absolute-value assertions when shared mutable state could leak; scope getByRole locators with parent getByTestId whenever the same accessible name appears in multiple regions
- The harness-gate result (21/21 green or remaining failures) is OWNED BY Plan 10-07's SUMMARY, not this one. 10-06 just notes "Gap Group A spec/config edits landed; harness gate run by 10-07."
</output>
