---
phase: 10
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - test/03-buy.spec.ts
  - test/04-sell.spec.ts
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "`test/03-buy.spec.ts` exists and contains a single `test(...)` block that drives the TradeBar UI: fills `Ticker` with `NVDA`, `Quantity` with `1`, clicks `Buy`, then asserts (a) the NVDA position row appears in the positions table within 10s, and (b) the header cash value drops below `$10,000` (parsed from the cash text node)."
    - "`test/04-sell.spec.ts` exists and contains a single `test(...)` block that buys `JPM × 2`, then sells `JPM × 1`, then asserts the JPM position row's quantity cell reads `1` (the round-trip leaves a half-position)."
    - "Both specs run green across all three Playwright projects (chromium, firefox, webkit) inside the compose harness. Per-spec smoke runs (3 browsers × 2 specs = 6 invocations) and full-suite cross-spec runs (this plan together with 10-02, 10-04, 10-05 once landed) are all green."
    - "Per D-08, 03-buy uses `NVDA` (in seed; isolated from 04 / 05 / 06) and 04-sell uses `JPM` (in seed; isolated from 03 / 05 / 06). No cross-spec ticker collision within a single `up` invocation."
    - "Cash-decreased assertion is RELATIVE (`< 10000`) not absolute — the simulator price moves; hard-coding a specific value (e.g. `$9,809.98`) WILL flake per RESEARCH.md Pitfall 1. Quantity-of-1 assertion in 04-sell uses a regex `\\b1\\b` so float-formatting variants (`1`, `1.0`, `1.00`) all match."
  artifacts:
    - path: "test/03-buy.spec.ts"
      provides: "PLAN.md §12 row 3 — buy-shares scenario via TradeBar UI."
      contains: "import { test, expect } from '@playwright/test';"
    - path: "test/04-sell.spec.ts"
      provides: "PLAN.md §12 row 4 — sell-shares scenario via TradeBar UI."
      contains: "import { test, expect } from '@playwright/test';"
  key_links:
    - from: "test/03-buy.spec.ts (TradeBar input fill)"
      to: "frontend/src/components/terminal/TradeBar.tsx:84-86, 100-102 (`<label>` wrapping `<input>` for Ticker and Quantity)"
      via: "Playwright `getByLabel('Ticker')` / `getByLabel('Quantity')` — label-text resolves to the wrapped input via the accessibility tree. Stable across all three engines."
      pattern: "getByLabel('Ticker')"
    - from: "test/03-buy.spec.ts (positions table assertion)"
      to: "frontend/src/components/terminal/PositionRow.tsx:53-54 (`<tr role=\"button\" aria-label=\"Select {ticker}\">`)"
      via: "Same `aria-label=\"Select {TICKER}\"` pattern as WatchlistRow — Playwright `getByRole('button', { name: 'Select NVDA' })` matches both the watchlist row AND the positions row, but in the buy-test context the assertion is that BOTH appear (and the test only uses `META`/`NVDA`/etc that aren't ambiguous between watchlist+positions)."
      pattern: "Select NVDA"
    - from: "test/03-buy.spec.ts (cash assertion)"
      to: "frontend/src/components/terminal/Header.tsx (`data-testid=\"header-cash\"` from Plan 10-00) OR `header > span` DOM-index fallback"
      via: "Cash value is `formatMoney(cashBalance)` rendered into a `<span>`. After 10-00 ships, use `getByTestId('header-cash')`; before, use the locator+nth(1) fallback documented in RESEARCH.md §03-buy line 471."
      pattern: "header-cash"
    - from: "test/03-buy.spec.ts + test/04-sell.spec.ts (UI trade flow)"
      to: "backend/app/portfolio/routes.py (POST /api/portfolio/trade)"
      via: "TradeBar's submit handler calls `POST /api/portfolio/trade` with `{ticker, quantity, side}` and on success invalidates the `['portfolio']` React Query, which the positions table consumes. End-to-end flow: UI → REST → DB → polled cache → UI. Test sees the observable result without touching the API directly."
      pattern: "/api/portfolio/trade"
---

<objective>
Land the second pair of Wave 2 spec files: `03-buy.spec.ts` (PLAN.md §12 row 3 — buy NVDA × 1, observe cash drop and position appearance) and `04-sell.spec.ts` (PLAN.md §12 row 4 — buy JPM × 2 then sell JPM × 1, observe quantity halve). Both specs drive the real TradeBar UI; both fill orders through the production `POST /api/portfolio/trade` endpoint; both observe the downstream React Query refetch via the rendered positions table.

Purpose: Closes 2 of the 7 §12 scenarios for TEST-04 — the trading UI flows. Exercises the same code path Phase 7's TradeBar component tests cover at a Vitest level, but against a real browser, real backend, real SQLite, and real simulator price ticks.

Output: Two new TypeScript spec files under `test/`. No other files touched.

Wave 2 rationale: This plan creates two new files; no overlap with 10-02, 10-04, 10-05. All four Wave 2 plans depend only on 10-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-e2e-validation/10-CONTEXT.md
@.planning/phases/10-e2e-validation/10-RESEARCH.md
@.planning/phases/10-e2e-validation/10-PATTERNS.md
@.planning/phases/10-e2e-validation/10-VALIDATION.md
@.planning/phases/10-e2e-validation/10-01-PLAN.md
@frontend/src/components/terminal/TradeBar.tsx
@frontend/src/components/terminal/PositionRow.tsx
@frontend/src/components/terminal/Header.tsx
@backend/app/portfolio/routes.py
@backend/app/market/seed_prices.py

<interfaces>
<!-- Reference selectors and reference test bodies (verbatim from RESEARCH.md). -->

### TradeBar UI selectors (verified frontend/src/components/terminal/TradeBar.tsx:82-141):
- Root `<section>`: after Plan 10-00, has `data-testid="trade-bar"`.
- Ticker input: `<label>Ticker <input type="text" placeholder="AAPL" .../></label>` — Playwright `getByLabel('Ticker')` resolves to the input.
- Quantity input: `<label>Quantity <input type="number" placeholder="1" .../></label>` — `getByLabel('Quantity')`.
- Buy button: `<button>Buy</button>` — `getByRole('button', { name: 'Buy' })`.
- Sell button: `<button>Sell</button>` — `getByRole('button', { name: 'Sell' })`.

### Positions row selector (verified frontend/src/components/terminal/PositionRow.tsx:53-54):
Each row is `<tr role="button" aria-label="Select {TICKER}">`. Same shape as WatchlistRow — `getByRole('button', { name: 'Select NVDA' })` works. The full `<tr>` text content includes the ticker symbol, quantity, avg cost, current price, P&L, and % — `toContainText(/\bNVDA\b.*\b1\b/)` matches the qty=1 cell.

### Header cash selector:
- Post-10-00: `page.getByTestId('header-cash')`.
- Pre-10-00 fallback: `page.locator('header').getByText(/^\$/).nth(1)` — the `Total` $-text is index 0, `Cash` is index 1 (verified Header.tsx:42-52 layout).

### Reference test bodies (verbatim from RESEARCH.md §"Per-Scenario Spec Outlines")

**03-buy.spec.ts** — RESEARCH.md lines 475-495:

```typescript
import { test, expect } from '@playwright/test';

test('buy NVDA 1: cash decreases, position appears', async ({ page }) => {
  await page.goto('/');
  await expect(page.getByText('$10,000.00')).toBeVisible();

  await page.getByLabel('Ticker').fill('NVDA');
  await page.getByLabel('Quantity').fill('1');
  await page.getByRole('button', { name: 'Buy' }).click();

  // Position row appears (PositionRow uses aria-label="Select NVDA")
  await expect(
    page.getByRole('button', { name: 'Select NVDA' }),
  ).toBeVisible({ timeout: 10_000 });

  // Cash strictly less than $10,000.00. Simulator price moves; do NOT assert
  // an exact value (RESEARCH.md Pitfall 1). Read header-cash and parse.
  // After Plan 10-00 ships:
  const cashText = await page.getByTestId('header-cash').innerText();
  // Fallback (pre-10-00):
  // const cashText = await page.locator('header').getByText(/^\$/).nth(1).innerText();
  const cashAmount = parseFloat(cashText.replace(/[$,]/g, ''));
  expect(cashAmount).toBeLessThan(10_000);
});
```

**04-sell.spec.ts** — RESEARCH.md lines 515-533:

```typescript
import { test, expect } from '@playwright/test';

test('sell JPM 1 after buying 2: position quantity drops to 1', async ({ page }) => {
  await page.goto('/');

  // Buy 2
  await page.getByLabel('Ticker').fill('JPM');
  await page.getByLabel('Quantity').fill('2');
  await page.getByRole('button', { name: 'Buy' }).click();
  await expect(page.getByRole('button', { name: 'Select JPM' })).toBeVisible({
    timeout: 10_000,
  });

  // Sell 1
  await page.getByLabel('Ticker').fill('JPM');
  await page.getByLabel('Quantity').fill('1');
  await page.getByRole('button', { name: 'Sell' }).click();

  // Wait for the qty cell in the JPM row to read "1" (or "1.0" / "1.00").
  const jpmRow = page.getByRole('button', { name: 'Select JPM' });
  await expect(jpmRow).toContainText(/\bJPM\b.*\b1(?:\.0+)?\b/, {
    timeout: 10_000,
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write `test/03-buy.spec.ts`</name>
  <read_first>
    - frontend/src/components/terminal/TradeBar.tsx (lines 82-141 — confirm `<label>Ticker</label>` / `<label>Quantity</label>` wrap inputs; Buy/Sell buttons have plain text content)
    - frontend/src/components/terminal/PositionRow.tsx (lines 53-54 — confirm `<tr role="button" aria-label="Select {TICKER}">` shape; full row text includes the qty cell)
    - frontend/src/components/terminal/Header.tsx (lines 38-53 — confirm cash value `<span>`; if Plan 10-00 has shipped, the span has `data-testid="header-cash"`)
    - backend/app/portfolio/routes.py (Phase 3 — confirm `POST /api/portfolio/trade` request body shape `{ticker, quantity, side}` and 200/400 response)
    - backend/app/market/seed_prices.py (line ~12 — confirm `NVDA` is in the simulator's seed list so the first tick lands within ~500ms of healthcheck-ready)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 454-505 — full assertion list, code shape, edge cases)
  </read_first>
  <action>
    Write `test/03-buy.spec.ts` based on the verbatim shape in the `<interfaces>` block above.

    Specific decisions for the executor:
    1. **Imports:** `import { test, expect } from '@playwright/test';` — single line, same as 01/02.
    2. **`page.goto('/')`** — root path; `baseURL` resolves to `http://app:8000/`.
    3. **Pre-trade cash sanity:** `await expect(page.getByText('$10,000.00')).toBeVisible();` confirms the page loaded with full starting cash before mutating state. This guards against running against a dirty (re-used) volume.
    4. **TradeBar input fill:** Use `getByLabel('Ticker')` and `getByLabel('Quantity')` — these resolve through the `<label>` wrapping pattern verified in TradeBar.tsx. DO NOT use `getByPlaceholderText('AAPL')` even though the placeholder exists; placeholder text is the Vitest pattern (Vitest's jsdom respects placeholder), but Playwright's accessibility-tree-based `getByLabel` is the canonical user-facing locator (RESEARCH.md PATTERNS.md selector hierarchy rule 2).
    5. **Click Buy:** `getByRole('button', { name: 'Buy' }).click()` — exact button text. PATTERNS.md selector rule 3.
    6. **Position-appears assertion:** `getByRole('button', { name: 'Select NVDA' })` is the `<tr role="button" aria-label="Select NVDA">` PositionRow (same role-button-aria-label pattern as WatchlistRow). Use `{ timeout: 10_000 }` — the trade roundtrip + React Query refetch + render takes 1-3s on cold cache, with WebKit slower.
    7. **Cash-decreased assertion:** PARSE the cash text and assert `< 10000`. Do NOT hardcode a value (RESEARCH.md Pitfall 1: simulator prices move; Phase 9 verification's observed `$9,809.98` is one historical sample, not a contract). Implementation:
       ```typescript
       const cashText = await page.getByTestId('header-cash').innerText();
       const cashAmount = parseFloat(cashText.replace(/[$,]/g, ''));
       expect(cashAmount).toBeLessThan(10_000);
       ```
       If Plan 10-00 has not shipped, fall back to:
       ```typescript
       const cashText = await page.locator('header').getByText(/^\$/).nth(1).innerText();
       ```
       and document the fallback in the SUMMARY.
    8. **D-08 ticker:** `'NVDA'` (in default seed; not used by 04-sell, 05-portfolio-viz, or 06-chat).
    9. **No emojis. No `waitForTimeout`. No `test.only`/`test.skip`.** Single `test()` block.
    10. **Test name:** `'buy NVDA 1: cash decreases, position appears'`.
  </action>
  <verify>
    <automated>test -f test/03-buy.spec.ts && grep -q "import { test, expect } from '@playwright/test';" test/03-buy.spec.ts && grep -q "getByLabel('Ticker')" test/03-buy.spec.ts && grep -q "getByLabel('Quantity')" test/03-buy.spec.ts && grep -q "{ name: 'Buy' }" test/03-buy.spec.ts && grep -q "NVDA" test/03-buy.spec.ts && grep -q "toBeLessThan(10_000)" test/03-buy.spec.ts && cd test && npx playwright test 03-buy.spec.ts --list 2>&1 | grep -qE "(buy NVDA|1 test)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/03-buy.spec.ts` exists and parses as valid TypeScript.
    - Uses `getByLabel('Ticker')` and `getByLabel('Quantity')` (NOT `getByPlaceholderText`).
    - Fills `'NVDA'` and `'1'`, clicks `getByRole('button', { name: 'Buy' })`.
    - Asserts `getByRole('button', { name: 'Select NVDA' })` is visible (10s timeout).
    - Reads cash text via `getByTestId('header-cash')` (post-10-00) or `page.locator('header').getByText(/^\$/).nth(1)` (pre-10-00 fallback).
    - Parses cash via `parseFloat(cashText.replace(/[$,]/g, ''))`.
    - Asserts `cashAmount < 10_000` (strictly less than) — NOT an exact-value assertion.
    - Uses ticker `'NVDA'` (D-08 isolation from other Wave 2 specs).
    - Single `test(...)` block; no `describe`, no `beforeEach`, no `.only`/`.skip`, no emojis, no `waitForTimeout`.
    - Smoke run: `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 03-buy.spec.ts --project=chromium` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write `test/04-sell.spec.ts`</name>
  <read_first>
    - frontend/src/components/terminal/TradeBar.tsx (same selectors as 03-buy)
    - frontend/src/components/terminal/PositionRow.tsx (line 53-54 — full row text contains qty cell; format depends on Phase 7 PositionRow implementation but `parseFloat(qty)` is rendered as either `1`, `1.0`, or `1.00`)
    - backend/app/portfolio/routes.py (Phase 3 — confirm sell flow returns 200 OK with updated position quantity)
    - .planning/phases/10-e2e-validation/10-RESEARCH.md (lines 508-540 — full code shape + buy-then-sell rationale)
  </read_first>
  <action>
    Write `test/04-sell.spec.ts` based on the verbatim shape in the `<interfaces>` block above.

    Specific decisions for the executor:
    1. **Imports + goto:** Same as 03-buy.
    2. **Buy 2 first:** Fill `'JPM'` + `'2'`, click Buy. Wait for `getByRole('button', { name: 'Select JPM' })` to be visible (10s timeout) — this proves the buy fully processed before the sell fires.
    3. **Sell 1:** Fill `'JPM'` + `'1'` (TradeBar should remember nothing across operations — both buy and sell are independent submits). Click Sell.
    4. **Quantity-halved assertion:** The JPM PositionRow's full text contains the qty. After buy=2, sell=1, qty=1. Use `toContainText(/\bJPM\b.*\b1(?:\.0+)?\b/, { timeout: 10_000 })`. The regex tolerates `1`, `1.0`, `1.00` — Phase 7 PositionRow may render any of these; do NOT hardcode a specific format.
    5. **D-08 ticker:** `'JPM'` (in default seed; not used by 03-buy / 05 / 06).
    6. **Why JPM × 2 → sell × 1 (not × 1 → sell × 1):** Per RESEARCH.md line 509, this leaves a remaining position so the test asserts qty=1 (a transformation), not row-disappearance. Row-disappearance is more brittle (timing-coupled to React Query refetch); qty-text assertion is observable and stable.
    7. **No emojis. No `waitForTimeout`. No `test.only`/`test.skip`.** Single `test()` block.
    8. **Test name:** `'sell JPM 1 after buying 2: position quantity drops to 1'`.
  </action>
  <verify>
    <automated>test -f test/04-sell.spec.ts && grep -q "import { test, expect } from '@playwright/test';" test/04-sell.spec.ts && grep -q "JPM" test/04-sell.spec.ts && grep -q "{ name: 'Buy' }" test/04-sell.spec.ts && grep -q "{ name: 'Sell' }" test/04-sell.spec.ts && grep -q "Select JPM" test/04-sell.spec.ts && grep -qE "1\\(\\?:\\\\\\.0\\+\\)\\?" test/04-sell.spec.ts || grep -qE "1\\(\\?:" test/04-sell.spec.ts && cd test && npx playwright test 04-sell.spec.ts --list 2>&1 | grep -qE "(sell JPM|1 test)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/04-sell.spec.ts` exists and parses as valid TypeScript.
    - Issues a Buy of `'JPM'` × `'2'`, waits for `Select JPM` row to appear, then a Sell of `'JPM'` × `'1'`.
    - Final assertion uses `toContainText(...)` against the JPM row with a regex that tolerates `1`, `1.0`, `1.00` formatting.
    - Uses ticker `'JPM'` (D-08 isolation).
    - Single `test(...)` block; no emojis, no `waitForTimeout`, no `.only`/`.skip`.
    - Smoke run: `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 04-sell.spec.ts --project=chromium` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Per-spec 3-browser harness gate (03 + 04 only)</name>
  <read_first>
    - test/03-buy.spec.ts + test/04-sell.spec.ts (just-written)
    - test/playwright.config.ts (confirm three projects defined)
    - test/docker-compose.test.yml (confirm `playwright` service ready)
  </read_first>
  <action>
    Validate both new specs across all three browsers, scoped to JUST the new specs to keep this task's runtime predictable.

    Command (from repo root):
    `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 03-buy.spec.ts 04-sell.spec.ts`

    Expected:
    - All `2 specs × 3 projects = 6` (spec, project) pairs pass.
    - Compose `run --rm` exits 0.
    - The two new specs co-exist with any specs from 10-02 (already shipped) without state interference — D-08 unique tickers (NVDA, JPM, PYPL, AAPL/...10-tickers) prevent collisions.

    **If any (spec, project) fails:** Common causes:
    - Cold-cache `price_unavailable` on the very first trade (RESEARCH.md Pitfall 1): The simulator's first tick lands within ~500ms of healthcheck-ready; the test's `goto('/')` + `getByText('$10,000.00')` warm-up usually covers it. If a flake appears, add an explicit wait: `await expect(page.getByRole('button', { name: 'Select NVDA' }).getByText(/\$/)).toBeVisible()` BEFORE the trade fires (proves NVDA's price has streamed at least once).
    - WebKit timing (Pitfall 7): Bump the position-appears timeout from 10s to 15s.
    - Header cash selector mismatch (10-00 not yet shipped): Switch to the `page.locator('header').getByText(/^\$/).nth(1)` fallback documented in 03's task action.
    - Quantity rendering format mismatch in 04: Read PositionRow.tsx to discover the exact `formatQty` implementation; tighten the regex if needed.

    Apply the minimal fix and re-run. DO NOT run the FULL one-command harness (`up --build`) here — that's the integration gate covered by the next plan. This task uses `run --rm` for fast-feedback per-spec validation against the already-built `app` image (which 10-02's harness gate built).
  </action>
  <verify>
    <automated>docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 03-buy.spec.ts 04-sell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f test/docker-compose.test.yml run --rm playwright npx playwright test 03-buy.spec.ts 04-sell.spec.ts` exits 0.
    - Stdout shows all 6 (spec, project) pairs passed.
    - No transient SSE-related flakes (these belong to 10-05, not here).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test browser ↔ TradeBar UI | Both specs cross this boundary by filling form inputs and clicking buttons. The DOM contract (label-wrapped inputs, exact button text) is stable across browsers per Phase 7 design. |
| TradeBar UI ↔ `/api/portfolio/trade` REST | Trade submission goes through TradeBar's mutation handler → `POST /api/portfolio/trade` → Phase 3's validate-then-write pipeline → SQLite. Test asserts the OBSERVABLE result (positions row, cash header) — not the API contract directly. |
| Per-`up` SQLite ↔ cross-spec state | Plans 10-02, 10-03, 10-04, 10-05 all share the same SQLite within one `up` invocation. D-08 unique tickers (`NVDA`, `JPM`, `PYPL`, `META`, `AMZN`) are the isolation mechanism — no spec should see a position created by another spec. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | T (Tampering) | Production code | mitigate | Phase 10 Wave 2 modifies only spec files under `test/`. No frontend, backend, Dockerfile, or compose changes. The TradeBar UI behavior is exercised, not modified. |
| T-10-03-02 | I (Information disclosure) | Cash + position values in test reports | accept | Trade values are within the simulated $10k starting cash; positions are seeded `NVDA`/`JPM` tickers. No PII; no real account data. Reports are gitignored per Plan 10-01. |
| T-10-03-03 | D (Denial of service) | Cold-cache `price_unavailable` flake | mitigate | RESEARCH.md Pitfall 1: simulator first tick lands within ~500ms post-healthcheck. Both specs `goto('/')` + assert `$10,000.00` visible before trading, which gives React Query + the simulator ample warm-up. The `start_period: 15s` healthcheck grace window from Plan 10-01 is the second line of defense. The 10s position-appears timeout absorbs WebKit slowness. |
| T-10-03-04 | E (Elevation of privilege) | TradeBar fuzzing | accept-out-of-scope | This plan exercises only valid happy-path inputs. Trade-validation negative-path tests (insufficient cash, insufficient shares) live in Phase 3's pytest suite, not Phase 10. |

</threat_model>

<verification>
- `test/03-buy.spec.ts` and `test/04-sell.spec.ts` exist; both pass `cd test && npx playwright test --list` (Tasks 1-2 grep gates).
- Both specs run green across chromium, firefox, webkit (Task 3 gate).
- Cash-decreased assertion uses `parseFloat` + `toBeLessThan(10_000)` — relative, not absolute.
- Quantity-halved regex tolerates `1`, `1.0`, `1.00` formatting variants.
- D-08 unique tickers: 03 uses NVDA, 04 uses JPM. Combined with 10-02's PYPL, 10-04's META + AMZN, and 10-05's no-ticker, no cross-spec collision exists within a single compose `up`.
- No files modified outside `test/03-buy.spec.ts` and `test/04-sell.spec.ts`.
</verification>

<success_criteria>
- [ ] `test/03-buy.spec.ts` drives TradeBar → asserts NVDA position visible + cash < $10k.
- [ ] `test/04-sell.spec.ts` buys JPM × 2, sells JPM × 1, asserts qty = 1 in the JPM row.
- [ ] Both pass on chromium, firefox, webkit.
- [ ] No emojis, no `waitForTimeout`, no `test.only`/`test.skip`.
- [ ] D-08 isolation maintained (NVDA + JPM unique to this plan within Wave 2).
- [ ] Per-spec scoped harness run (Task 3) exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-validation/10-03-SUMMARY.md` recording the two spec files, which header-cash selector strategy each used (testId vs locator-fallback — depending on 10-00 ship order), Task 3's green run output, and any per-browser timing observations (especially WebKit) that may inform future timeout tuning.
</output>
