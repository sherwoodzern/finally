---
phase: 10-e2e-validation
plan: 08
type: execute
wave: 4
depends_on: [10-06, 10-07]
files_modified:
  - frontend/src/components/portfolio/Heatmap.tsx
  - test/01-fresh-start.spec.ts
  - test/04-sell.spec.ts
autonomous: true
gap_closure: true
requirements: [TEST-03, TEST-04]
tags: [playwright, e2e, gap-closure, recharts-tooltip, sqlite-volume-leak, react-query-race]

must_haves:
  truths:
    - "Mode A closed: After the heatmap renders, hovering a Recharts Treemap cell never blocks a sibling click on `tab-pnl`. The tooltip's wrapper does not intercept pointer events. Holds across chromium, firefox, and webkit."
    - "Mode B closed: 01-fresh-start passes on every browser project regardless of cross-project order. The spec no longer asserts an absolute cash value that depends on prior chromium-spec debits leaking through the compose anonymous SQLite volume."
    - "Mode C closed: 04-sell passes deterministically (no `flaky` retries) on every browser project. The `postBuyQty` snapshot captures only after React Query has refetched the post-buy quantity, so the existing `expect.poll(...).toBe(postBuyQty - 1)` assertion is given a stable baseline."
    - "The canonical harness command from CONTEXT.md D-03 finishes green reproducibly: `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 with `21 passed`, `0 failed`, `0 flaky`, and `Container test-appsvc-1 Healthy` in the log."
    - "ROADMAP Phase 10 SC#3 is met. TEST-03 and TEST-04 are satisfied in evidence terms (REQUIREMENTS.md ticks are owned by the verifier on its next pass)."
  artifacts:
    - path: "frontend/src/components/portfolio/Heatmap.tsx"
      provides: "Explicit Recharts <Tooltip wrapperStyle={{ pointerEvents: 'none' }} /> rendered as a child of <Treemap>. Default tooltip overlay no longer intercepts clicks on neighbouring tabs."
      contains: "wrapperStyle={{ pointerEvents: 'none' }}"
    - path: "test/01-fresh-start.spec.ts"
      provides: "Streaming + watchlist seed proof without an absolute cash assertion. The 10-ticker watchlist row visibility (lines 25-31) and the em-dash streaming-proof assertion (line 41) prove fresh start without coupling to cross-project SQLite state."
      contains: "Cross-project SQLite leak"
    - path: "test/04-sell.spec.ts"
      provides: "expect.poll-stabilised postBuyQty snapshot — read only after React Query refetch settles to a value >= 2 (the buy added exactly 2 shares). Existing post-sell relative-delta assertion at lines 47-52 (10-06 commit ee45f65) preserved."
      contains: "toBeGreaterThanOrEqual(2)"
    - path: "/tmp/phase10-final-harness.log"
      provides: "Captured stdout+stderr of the canonical harness invocation. Used as evidence in the SUMMARY for the 21 passed line and the appsvc Healthy line."
      contains: "21 passed"
  key_links:
    - from: "Heatmap.tsx <Tooltip wrapperStyle={{ pointerEvents: 'none' }} />"
      to: "Recharts Treemap default tooltip overlay"
      via: "Recharts Tooltip wrapperStyle prop — passes a CSS style object onto the <div> wrapper Recharts renders around the tooltip body. `pointer-events: none` makes the wrapper transparent to mouse events while remaining visible."
      pattern: "wrapperStyle=\\{\\{ pointerEvents: 'none' \\}\\}"
    - from: "test/01-fresh-start.spec.ts (10-ticker watchlist visibility + em-dash streaming proof)"
      to: "Live appsvc/api/portfolio + /api/stream/prices"
      via: "Watchlist rows scoped to data-testid=watchlist-panel + AAPL row not.toContainText('—') — both prove the page is on a working app without coupling to cash level"
      pattern: "not\\.toContainText\\('—'"
    - from: "test/04-sell.spec.ts expect.poll(...).toBeGreaterThanOrEqual(2)"
      to: "React Query refetched post-buy JPM qty cell"
      via: "expect.poll re-reads jpmQty.innerText() until parseFloat >= 2; only then snapshots postBuyQty. Mirrors the 10-06 expect.poll pattern at lines 47-52 but applied to the snapshot read."
      pattern: "toBeGreaterThanOrEqual\\(2\\)"
    - from: "Canonical command from CONTEXT.md D-03"
      to: "21 passed + 0 failed + 0 flaky + Healthy appsvc"
      via: "docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright (verbatim)"
      pattern: "21 passed"
---

<objective>
Close the three remaining gaps from the refreshed `10-VERIFICATION.md` (commit 4f690e6) — Mode A (Recharts heatmap tooltip intercepts `tab-pnl` click on all 3 browsers), Mode B (cross-project SQLite leak makes the absolute `$10,000.00` cash assertion non-deterministic), and Mode C (`postBuyQty` snapshot races React Query refetch in 04-sell) — with three small atomic edits and then own the canonical harness 21/21 green gate that Plan 10-07 abandoned.

Purpose: meet ROADMAP Phase 10 SC#3 ("single command finishes green reproducibly") so TEST-03 and TEST-04 can be ticked. Plans 10-06 and 10-07 closed the prior failure shape and revealed three new failure modes; this plan closes all three in one pass and proves the gate green.

Output:
- One-line production-side fix to `frontend/src/components/portfolio/Heatmap.tsx` (closes Mode A on all 3 browsers)
- One-block test-side edit to `test/01-fresh-start.spec.ts` (drops the absolute `$10,000.00` cash assertion; closes Mode B)
- One-block test-side edit to `test/04-sell.spec.ts` (wraps `postBuyQty` snapshot in `expect.poll`; closes Mode C)
- A captured canonical-command run at `/tmp/phase10-final-harness.log` proving 21 passed / 0 failed / 0 flaky / appsvc Healthy / exit 0
- A `.planning/phases/10-e2e-validation/10-08-SUMMARY.md` documenting all three fixes, the override of 10-07's "do not modify Heatmap" guidance with reasoning, and the harness summary line + Healthy line copied from the log as self-contained evidence
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-e2e-validation/10-CONTEXT.md
@.planning/phases/10-e2e-validation/10-VERIFICATION.md
@.planning/phases/10-e2e-validation/10-PATTERNS.md
@.planning/phases/10-e2e-validation/10-04-SUMMARY.md
@.planning/phases/10-e2e-validation/10-06-SUMMARY.md
@.planning/phases/10-e2e-validation/10-07-PLAN.md
@frontend/src/components/portfolio/Heatmap.tsx
@test/01-fresh-start.spec.ts
@test/04-sell.spec.ts
@test/05-portfolio-viz.spec.ts
@CLAUDE.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from the source files this plan modifies. -->

From `frontend/src/components/portfolio/Heatmap.tsx` (current state — lines 1-132):
```typescript
import { ResponsiveContainer, Treemap } from 'recharts';
// ... existing imports ...

export function Heatmap() {
  // ... query/cache/early-return logic at lines 61-110 ...
  return (
    <section className="...">
      <h2 className="text-xl font-semibold mb-3">Heatmap</h2>
      <div className="flex-1 w-full" data-testid="heatmap-treemap">
        <ResponsiveContainer width="100%" height="100%">
          <Treemap
            data={treeData}
            dataKey="weight"
            stroke="#30363d"
            content={<HeatmapCell />}
            isAnimationActive
            animationDuration={300}
            onClick={(node) => handleHeatmapCellClick(node)}
          />
        </ResponsiveContainer>
      </div>
    </section>
  );
}
```

From `test/01-fresh-start.spec.ts` (current state — line 34):
```typescript
// 2. Header cash reads $10,000.00 (Plan 10-00 testid).
await expect(page.getByTestId('header-cash')).toHaveText('$10,000.00');
```

From `test/04-sell.spec.ts` (current state — lines 33-37):
```typescript
// Capture post-buy JPM qty so the post-sell assertion can be a relative
// delta rather than an absolute `1`. Robust to any prior state on JPM.
const jpmRow = positionsTable.getByRole('button', { name: 'Select JPM' });
const jpmQty = jpmRow.locator('td').nth(1);
const postBuyQtyText = await jpmQty.innerText();
const postBuyQty = parseFloat(postBuyQtyText.trim());
```

From `test/04-sell.spec.ts` (current state — lines 47-52, the existing expect.poll for the post-sell delta — DO NOT MODIFY this block):
```typescript
await expect
  .poll(
    async () => parseFloat((await jpmQty.innerText()).trim()),
    { timeout: 10_000 },
  )
  .toBe(postBuyQty - 1);
```

From `test/05-portfolio-viz.spec.ts` (current state — lines 26-29 + line 49, the `dismissChartTooltip` helper that 10-07 landed — leave AS IS, it becomes belt-and-suspenders after Task 1):
```typescript
const dismissChartTooltip = async () => {
  await page.keyboard.press('Escape');
  await page.mouse.move(0, 0);
};
// ... line 49 ...
await dismissChartTooltip();
await page.getByTestId('tab-pnl').click();
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan touches one production frontend file (one prop on a Recharts `<Tooltip>` element rendered on the user's own portfolio data) and two `test/` files. The compose stack still runs `LLM_MOCK=true` against an anonymous SQLite volume on `localhost`. No new endpoints, no new secrets, no new network surface, no new external dependencies, no auth changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | T (Tampering) | `Heatmap.tsx` `<Tooltip>` rendering of position data | accept | Tooltip renders strings derived from the user's own positions (ticker symbols, P&L %, weights). Same data already shown in the heatmap labels and the positions table. No DOM injection — `<Tooltip>` body content is rendered by Recharts using React text nodes, no `dangerouslySetInnerHTML`. `wrapperStyle={{ pointerEvents: 'none' }}` is a CSS attribute on the wrapper `<div>`, no script, no event-handler change. |
| T-10-08-02 | I (Information disclosure) | tooltip text content | accept | Tooltip exposes the same per-position figures the rest of the UI already shows. No additional fields surfaced. Single-user demo, no PII at risk. |

No new authentication, network, or data-flow surfaces are introduced.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Production fix — render an explicit Recharts &lt;Tooltip&gt; with `wrapperStyle={{ pointerEvents: 'none' }}` inside &lt;Treemap&gt; (closes Mode A)</name>
  <files>frontend/src/components/portfolio/Heatmap.tsx</files>
  <read_first>
    - frontend/src/components/portfolio/Heatmap.tsx (current state — lines 1-132; particularly the import block at lines 9-14 and the `<Treemap>` element at lines 119-127)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md gaps[0] (Mode A) — explains that the Recharts DEFAULT internal Treemap tooltip has wrapper `pointer-events: auto` and intercepts clicks on sibling tabs; harness lines 626 (chromium NVDA), 675 (firefox META), 860 (webkit META) all show `<td class="px-4 font-semibold">{ticker}</td> from <div class="flex flex-col gap-4">…</div> subtree intercepts pointer events`
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md "Recommended Fix Plan" Fix 1 — prescribes the exact one-line production fix this task implements
    - .planning/phases/10-e2e-validation/10-04-SUMMARY.md — original Heatmap.tsx scope (FE-05). Confirms the Treemap currently uses Recharts' internal tooltip (no explicit `<Tooltip>` rendered) — that is what Mode A exploits.
    - .planning/phases/10-e2e-validation/10-07-PLAN.md "do NOT modify the heatmap or PnLChart frontend components — the tooltip is correct production behavior; only the test needs to handle it" — this task DELIBERATELY OVERRIDES that guidance. The override is justified: the verifier's harness traces (10-VERIFICATION.md lines 619-647, 668-696) show that the `dismissChartTooltip` helper from 10-07 (Escape + mouse displacement) does NOT actually retract the Recharts default tooltip on any of the 3 browsers. The production Tooltip wrapper IS the root cause; a hover overlay that blocks neighbouring clicks is a UX defect regardless of test coverage. The user could hit this manually by hovering a heatmap tile and then trying to click the P&L tab. Document this override in the SUMMARY (Task 4 of this plan).
    - CLAUDE.md root rules — "Identify root cause before fixing issues. Prove with evidence, then fix." The verifier proved the root cause; this fix addresses it directly.
  </read_first>
  <action>
    Make exactly two structural edits to `frontend/src/components/portfolio/Heatmap.tsx`:

    **Edit 1 — extend the recharts import at line 10.**
    Change:
    ```typescript
    import { ResponsiveContainer, Treemap } from 'recharts';
    ```
    to:
    ```typescript
    import { ResponsiveContainer, Tooltip, Treemap } from 'recharts';
    ```
    Add `Tooltip` between `ResponsiveContainer` and `Treemap` so the named imports stay alphabetical (matches the project's ESLint import-order convention; not strictly enforced but observed across the codebase). No other import changes.

    **Edit 2 — render `<Tooltip>` as a child of `<Treemap>` with `wrapperStyle={{ pointerEvents: 'none' }}`.**
    The current `<Treemap>` element (lines 119-127) is self-closing-style with no children. Convert it to an opening tag + a single `<Tooltip>` child + closing tag. Final shape (replace lines 119-127 verbatim):
    ```tsx
              <Treemap
                data={treeData}
                dataKey="weight"
                stroke="#30363d"
                content={<HeatmapCell />}
                isAnimationActive
                animationDuration={300}
                onClick={(node) => handleHeatmapCellClick(node)}
              >
                {/*
                  Explicit Recharts Tooltip with wrapperStyle pointerEvents: 'none'.
                  The default Treemap tooltip wrapper has pointer-events: auto and
                  intercepts clicks on sibling tabs (10-VERIFICATION.md Mode A —
                  failed all 3 browsers in 05-portfolio-viz). With pointerEvents:
                  'none' the tooltip remains visible on hover but its wrapper no
                  longer absorbs clicks targeted at neighbouring elements. This is
                  also better production UX: a hover tooltip should never block a
                  click on something else.
                */}
                <Tooltip wrapperStyle={{ pointerEvents: 'none' }} />
              </Treemap>
    ```

    Indentation is 12 spaces for the `<Treemap>` opening line (matching the surrounding JSX). Do NOT change any other prop on `<Treemap>` (data, dataKey, stroke, content, isAnimationActive, animationDuration, onClick all preserved). Do NOT touch `<ResponsiveContainer>` or the surrounding `<div data-testid="heatmap-treemap">`. Do NOT add any other Tooltip props (no `formatter`, no `content`, no `cursor` — Recharts' defaults render the existing tooltip body unchanged; only the wrapper's pointer-events behaviour changes).

    Do NOT modify any other file. Do NOT touch `HeatmapCell.tsx`, `PnLChart.tsx`, the watchlist components, or any test file. Do NOT modify the `dismissChartTooltip` helper in `test/05-portfolio-viz.spec.ts` (it stays as belt-and-suspenders — harmless after this fix lands; removing it is out of scope).

    After the edits, run `cd frontend && npm run build` to confirm the change compiles cleanly. The build must succeed (any tsc error means the import or JSX change introduced a regression and must be fixed before commit).
  </action>
  <verify>
    <automated>grep -c "wrapperStyle={{ pointerEvents: 'none' }}" frontend/src/components/portfolio/Heatmap.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "wrapperStyle={{ pointerEvents: 'none' }}" frontend/src/components/portfolio/Heatmap.tsx` outputs at least `1` (the prop landed).
    - `grep -c "import { ResponsiveContainer, Tooltip, Treemap } from 'recharts'" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (Tooltip imported between ResponsiveContainer and Treemap, alphabetical).
    - `grep -c "<Tooltip wrapperStyle={{ pointerEvents: 'none' }} />" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (the element rendered exactly once).
    - `grep -c "<Treemap" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (Treemap still rendered exactly once; not duplicated by accident).
    - `grep -c "</Treemap>" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (closing tag present — the conversion from self-closing to opening+closing succeeded).
    - `grep -c "onClick={(node) => handleHeatmapCellClick(node)}" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (existing onClick preserved).
    - `grep -c "data-testid=\"heatmap-treemap\"" frontend/src/components/portfolio/Heatmap.tsx` outputs `1` (existing test-id preserved — 05-portfolio-viz.spec.ts depends on it).
    - `cd frontend && npm run build 2>&1 | tail -1` includes no `Failed to compile` and no `error` token. The build either completes successfully or the task is blocked.
  </acceptance_criteria>
  <done>`Tooltip` imported from recharts; `<Tooltip wrapperStyle={{ pointerEvents: 'none' }} />` rendered as the only child of `<Treemap>`; all other Treemap props and the surrounding `<div data-testid="heatmap-treemap">` preserved unchanged; `npm run build` green.</done>
</task>

<task type="auto">
  <name>Task 2: Test fix — drop the absolute `$10,000.00` cash assertion in 01-fresh-start (closes Mode B)</name>
  <files>test/01-fresh-start.spec.ts</files>
  <read_first>
    - test/01-fresh-start.spec.ts (current state — lines 1-42; particularly the `// 2. Header cash reads $10,000.00 (Plan 10-00 testid).` comment at line 33 and the assertion at line 34 — these two lines are what this task removes)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md gaps[1] (Mode B) — proves the assertion fails on firefox (`Received: "$7,820.46"`) and webkit (`Received: "$5,447.88"`) due to the compose anonymous SQLite volume persisting across the 3 browser projects within a single `up`. With workers: 1, projects run alphabetically (chromium first), so firefox + webkit see chromium's debits.
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md "Recommended Fix Plan" Fix 2 — prescribes the exact test-side drop this task implements.
    - .planning/phases/10-e2e-validation/10-06-SUMMARY.md "Decisions Made" → "Drop pre-trade $10k assertion entirely instead of conditionally branching" — establishes the precedent: 10-06 dropped the same kind of absolute cash assertion from `test/03-buy.spec.ts` for the same fragility reason. This task applies the same pattern to `test/01-fresh-start.spec.ts:34`.
    - .planning/phases/10-e2e-validation/10-CONTEXT.md D-03 (canonical command — single `up`, NO per-project compose orchestration) and D-06 (NO test-only `/api/test/reset` endpoint). These two locked decisions block the heavier alternatives (per-project SQLite reset, per-project compose runs); test-side drop is the only path consistent with locked context.
  </read_first>
  <action>
    Make exactly one structural edit to `test/01-fresh-start.spec.ts`: remove the absolute `$10,000.00` cash assertion at line 34 and its comment at line 33, replacing them with a single explanatory comment that documents WHY the absolute assertion is gone (mirrors the 10-06 pattern in `test/03-buy.spec.ts`).

    **Edit — replace lines 33-34.**
    Current state (lines 33-34):
    ```typescript
      // 2. Header cash reads $10,000.00 (Plan 10-00 testid).
      await expect(page.getByTestId('header-cash')).toHaveText('$10,000.00');
    ```

    Replace those two lines with this single comment block (use the same 2-space indentation as the surrounding test body):
    ```typescript
      // No absolute cash assertion: the compose anonymous SQLite volume persists
      // across all 3 browser projects within a single `up`. Under workers: 1
      // projects run alphabetically (chromium → firefox → webkit), so by the time
      // firefox/webkit run this spec, chromium's 03-buy/04-sell/05-portfolio-viz/
      // 06-chat have already debited cash. Cross-project SQLite leak — see
      // 10-VERIFICATION.md Mode B (commit 4f690e6) and 10-06's parallel drop in
      // 03-buy. The 10-ticker watchlist visibility above and the streaming-proof
      // assertion below are sufficient to prove `fresh start` without coupling
      // to cross-project state.
    ```

    Leave all other lines unchanged: the `import` block (lines 1-6), the `SEED_TICKERS` array (lines 8-19), the test name and `page.goto('/')` (lines 21-22), the SEED_TICKERS visibility loop (lines 24-31), and the AAPL streaming-proof block at lines 36-41 (which becomes the new lines after this edit; the line numbers shift down by one because the assertion line is removed). Do NOT change selectors, do NOT change timeouts, do NOT remove the comment numbering ("1.", "3.") or any of the watchlist visibility logic. Do NOT add a `cashIsAtMost10k` lower-bound assertion or any other replacement assertion — the streaming-proof + watchlist visibility are sufficient (see 10-06's drop in 03-buy: the post-trade `< 10_000` assertion was already in place; here, no equivalent is needed because 01-fresh-start's purpose is `default watchlist exists + prices are streaming`, NOT `cash is exactly seed`).

    Do NOT modify any other file. The change scope is exactly two consecutive lines deleted + a comment block inserted in their place.
  </action>
  <verify>
    <automated>grep -c '\$10,000.00' test/01-fresh-start.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\$10,000.00' test/01-fresh-start.spec.ts` outputs `0` (the absolute literal no longer appears in the spec).
    - `grep -c "toHaveText('\$10,000.00')" test/01-fresh-start.spec.ts` outputs `0` (the absolute assertion is removed).
    - `grep -c 'Cross-project SQLite leak' test/01-fresh-start.spec.ts` outputs `1` (the explanatory comment landed; this exact phrase is uniquely greppable).
    - `grep -c "getByTestId('watchlist-panel')" test/01-fresh-start.spec.ts` outputs `2` (10-06's selector scoping preserved on both Select-button locators).
    - `grep -c "not.toContainText('—'" test/01-fresh-start.spec.ts` outputs `1` (the streaming-proof assertion preserved — this is what proves "fresh start" now that the cash assertion is gone).
    - `grep -c 'SEED_TICKERS' test/01-fresh-start.spec.ts` outputs at least `2` (array declaration + loop reference; both preserved).
    - `cd test && npx playwright test 01-fresh-start.spec.ts --list 2>&1 | grep -c 'fresh start'` outputs `3` (one entry per browser project; spec still parses across all 3).
  </acceptance_criteria>
  <done>The two-line absolute cash assertion at the original lines 33-34 is replaced with a single multi-line comment explaining the cross-project SQLite leak; watchlist visibility loop, SEED_TICKERS array, em-dash streaming proof, and selector scoping are all preserved unchanged; spec discoverable across all 3 browser projects.</done>
</task>

<task type="auto">
  <name>Task 3: Test fix — wrap `postBuyQty` snapshot in `expect.poll(...).toBeGreaterThanOrEqual(2)` to settle the React Query refetch race in 04-sell (closes Mode C)</name>
  <files>test/04-sell.spec.ts</files>
  <read_first>
    - test/04-sell.spec.ts (current state — lines 1-53; particularly lines 33-37 where the snapshot is captured pre-refetch, and lines 47-52 where 10-06's existing `expect.poll(...).toBe(postBuyQty - 1)` for the post-sell delta lives — DO NOT MODIFY lines 47-52)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md gaps[2] (Mode C) — proves the snapshot races React Query: harness line 1090 (firefox: Expected 0, Received 2 — postBuyQty captured 1, expected 1-1=0, actual is 2 because the buy of 2 fully refetched after the snapshot), harness line 1119 (webkit: Expected 2, Received 4 — same shape, postBuyQty captured 3 instead of 4). Both retry on Playwright's auto-retry and pass — but `flaky` reports block SC#3's `reproducibly`.
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md "Recommended Fix Plan" Fix 3 — prescribes the exact poll wrapper this task implements (the verifier's example uses an outer `let postBuyQty = 0; await expect.poll(async () => { postBuyQty = parseFloat(...); return postBuyQty; }).toBeGreaterThanOrEqual(2);` shape).
    - .planning/phases/10-e2e-validation/10-06-SUMMARY.md "Decisions Made" → "expect.poll over toHaveText regex for 04-sell qty" — explains the existing expect.poll pattern at lines 47-52. This task applies the SAME pattern to the snapshot read.
    - .planning/phases/10-e2e-validation/10-CONTEXT.md D-08 (per-spec ticker isolation: 04-sell uses JPM exclusively).
  </read_first>
  <action>
    Make exactly one structural edit to `test/04-sell.spec.ts`: replace the pre-refetch snapshot at lines 36-37 with an `expect.poll`-wrapped capture that waits for `parseFloat(jpmQty.innerText()) >= 2` before snapshotting. The existing post-sell `expect.poll(...).toBe(postBuyQty - 1)` block at lines 47-52 stays exactly as it is — this task only changes how `postBuyQty` is initialised.

    **Edit — replace lines 36-37 with the polling capture.**

    Current state (lines 33-37):
    ```typescript
      // Capture post-buy JPM qty so the post-sell assertion can be a relative
      // delta rather than an absolute `1`. Robust to any prior state on JPM.
      const jpmRow = positionsTable.getByRole('button', { name: 'Select JPM' });
      const jpmQty = jpmRow.locator('td').nth(1);
      const postBuyQtyText = await jpmQty.innerText();
      const postBuyQty = parseFloat(postBuyQtyText.trim());
    ```

    Replace lines 36-37 (`const postBuyQtyText = ...` and `const postBuyQty = ...`) — lines 33-35 stay untouched — with this block (use the same 2-space indentation as the surrounding test body):
    ```typescript
      // Wait for React Query to refetch the post-buy qty before snapshotting.
      // The Select-JPM visibility wait above only proves the row exists; the
      // qty cell may still be a pre-refetch stale value, which would race the
      // sell mutation and produce flaky retries on firefox/webkit (see
      // 10-VERIFICATION.md Mode C, commit 4f690e6). Poll until the qty parses
      // to >= 2 (the buy added exactly 2 shares, so any settled post-buy state
      // must be at least 2) and snapshot the latest value the poll observed.
      let postBuyQty = 0;
      await expect
        .poll(
          async () => {
            postBuyQty = parseFloat((await jpmQty.innerText()).trim());
            return postBuyQty;
          },
          { timeout: 10_000 },
        )
        .toBeGreaterThanOrEqual(2);
    ```

    Notes:
    - Use `let postBuyQty = 0` (NOT `const`) so the closure inside `.poll()` can reassign it on each iteration. The final value of `postBuyQty` after the poll resolves is the most recent reading, which is the post-refetch value.
    - The `.poll` callback returns `postBuyQty` so the matcher `.toBeGreaterThanOrEqual(2)` operates on the latest reading — preserves the same numeric-comparison style 10-06 used for the post-sell delta.
    - `{ timeout: 10_000 }` matches the timeout used by 10-06 in the post-sell `expect.poll` at lines 47-52 (consistent budget across both polls in this spec).
    - The post-sell assertion block at lines 47-52 (which becomes lines 56-61 after this insertion shifts numbering) is NOT modified — it still asserts `parseFloat((await jpmQty.innerText()).trim()) === postBuyQty - 1` via the existing `expect.poll(...).toBe(postBuyQty - 1)`. The closure captures the now-stable `postBuyQty` from the outer scope.

    Leave the rest of the spec unchanged:
    - The import block (lines 1-8) and the test name + `page.goto('/')` (lines 10-11) — unchanged.
    - The positions-table scope declaration at line 17 — unchanged.
    - The buy-2 sequence (lines 22-27) — unchanged.
    - `const jpmRow = ...` and `const jpmQty = ...` (lines 34-35) — unchanged. They MUST be declared before the new poll block uses `jpmQty`.
    - The sell-1 sequence (lines 39-43) — unchanged.
    - The post-sell `expect.poll(...).toBe(postBuyQty - 1)` block (current lines 47-52) — unchanged.

    Do NOT modify any other file. Do NOT add `await page.waitForTimeout(...)` (anti-pattern; the poll is the correct primitive). Do NOT change the buy quantity (D-08 isolation: still buy JPM × 2). Do NOT change the sell quantity (still sell JPM × 1). Do NOT introduce any new selectors.
  </action>
  <verify>
    <automated>grep -c 'toBeGreaterThanOrEqual(2)' test/04-sell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'toBeGreaterThanOrEqual(2)' test/04-sell.spec.ts` outputs `1` (the new poll matcher landed exactly once).
    - `grep -c 'let postBuyQty = 0' test/04-sell.spec.ts` outputs `1` (the closure-mutable binding is in place).
    - `grep -c 'const postBuyQty' test/04-sell.spec.ts` outputs `0` (the old immutable snapshot binding is gone).
    - `grep -c 'postBuyQtyText' test/04-sell.spec.ts` outputs `0` (the intermediate string variable is gone — the new poll body inlines the parse).
    - `grep -c 'postBuyQty - 1' test/04-sell.spec.ts` outputs `1` (the existing post-sell relative-delta assertion at the original lines 47-52 is preserved).
    - `grep -c "getByLabel('Quantity').fill('2')" test/04-sell.spec.ts` outputs `1` (the buy-2 step preserved — D-08 isolation).
    - `grep -c "getByLabel('Quantity').fill('1')" test/04-sell.spec.ts` outputs `1` (the sell-1 step preserved).
    - `grep -c "page.waitForTimeout" test/04-sell.spec.ts` outputs `0` (no anti-pattern hard sleeps introduced).
    - `cd test && npx playwright test 04-sell.spec.ts --list 2>&1 | grep -c 'sell JPM 1'` outputs `3` (one entry per browser project; spec still parses).
  </acceptance_criteria>
  <done>postBuyQty is initialised to 0 with `let`, populated by an `expect.poll(...).toBeGreaterThanOrEqual(2)` block that waits for React Query to refetch to a post-buy value of at least 2 before continuing; intermediate `postBuyQtyText` variable removed; `jpmRow` / `jpmQty` declarations preserved at lines 34-35; the existing post-sell relative-delta assertion at the original lines 47-52 is unchanged; buy-2 / sell-1 quantities preserved per D-08; no waitForTimeout anti-pattern; spec parseable across all 3 browser projects.</done>
</task>

<task type="auto">
  <name>Task 4: Verification gate — run the canonical command verbatim and prove 21 passed / 0 failed / 0 flaky / appsvc Healthy / exit 0</name>
  <files>(no files modified — verification only; captures /tmp/phase10-final-harness.log as evidence for the SUMMARY)</files>
  <read_first>
    - .planning/phases/10-e2e-validation/10-CONTEXT.md D-03 (canonical command verbatim — `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright`. Do NOT add flags. Do NOT use `-p chromium` or any spec filter. The whole point is the unmodified D-03 invocation.)
    - .planning/phases/10-e2e-validation/10-VERIFICATION.md (lists the 5 hard failures + 2 flaky retries from the prior run; this task proves all 5 hard failures are now green and 0 flaky retries occur)
    - frontend/src/components/portfolio/Heatmap.tsx (must contain `wrapperStyle={{ pointerEvents: 'none' }}` from Task 1 of this plan)
    - test/01-fresh-start.spec.ts (must NOT contain `$10,000.00` after Task 2 of this plan)
    - test/04-sell.spec.ts (must contain `toBeGreaterThanOrEqual(2)` and `let postBuyQty = 0` after Task 3 of this plan)
    - test/playwright.config.ts (must already have `workers: 1` from Plan 10-06 Task 1 — depends_on enforces ordering)
    - test/05-portfolio-viz.spec.ts (must already have the `dismissChartTooltip` helper from Plan 10-07 Task 1 — depends_on enforces ordering. After Task 1 of this plan lands, the helper becomes belt-and-suspenders rather than the primary fix; that is fine.)
    - CLAUDE.md root rules — "Try one test at a time. Be methodical. Don't jump to conclusions. Don't apply workarounds." — if this task fails, follow the same protocol Plan 10-07 established: stop, capture evidence, hand back to a verifier; do NOT silently retry or add ad-hoc flags.
  </read_first>
  <action>
    Execute the canonical Phase 10 harness command from the repo root and capture its full output. This task IS the SC#3 gate. The command is fixed by CONTEXT.md D-03 — do not substitute, do not add flags, do not change the working directory away from the repo root:

    ```
    docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright
    ```

    Capture stdout+stderr to `/tmp/phase10-final-harness.log` so the SUMMARY can reference exact lines from the run. Capture the exit code separately into a shell variable. The recommended invocation pattern (matches what 10-07 used and what the harness logs at `/tmp/phase10-gap-closure-harness.log` were captured with):

    ```bash
    docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright 2>&1 | tee /tmp/phase10-final-harness.log
    EXIT_CODE=${PIPESTATUS[0]}
    echo "Harness exit code: $EXIT_CODE"
    ```

    After the command completes:

    1. **Check the exit code is `0`.** If non-zero:
       - Do NOT mark this task done.
       - Do NOT silently retry (no `-p chromium`, no `--retries=2`, no spec filter — those mask root cause).
       - Inspect `/tmp/phase10-final-harness.log` to identify the failed (spec, project) pair(s).
       - If the failure is in a file that Tasks 1-3 of this plan were supposed to fix (Heatmap.tsx, 01-fresh-start.spec.ts, 04-sell.spec.ts), re-read the file via the Task's `<read_first>` protocol and verify the expected changes are present at the expected lines. If the changes are present but the failure persists, the assumed root cause is wrong — surface that as a NEW VERIFICATION.md update; do NOT patch around it.
       - If the failure surfaces in a file NEITHER this plan nor 10-06/10-07 touched, that is a NEW gap and should be surfaced via a fresh VERIFICATION.md update (NOT silently retried within this task).

    2. **Verify the harness summary line is `21 passed`.** Playwright's list reporter ends with a line like `21 passed (1.5m)`. The acceptance criteria below assert four distinct invariants that together prove SC#3:
       - `21 passed` appears (count must be exactly 21 — proves all 7 spec files ran for all 3 browser projects).
       - The pattern ` failed` (with leading space, to avoid matching unrelated tokens) does NOT appear in the harness summary section. If a single spec failed, Playwright prints `X failed` somewhere in the tail; absence of that pattern proves zero failures.
       - The string `flaky` does NOT appear (no retry markers — proves reproducibility).
       - `Container test-appsvc-1\s+Healthy` appears (proves the appsvc container reached healthy state, ruling out infrastructure issues).

    3. **Do NOT delete `/tmp/phase10-final-harness.log`.** The SUMMARY (the implicit final step in the `<output>` block of this plan) MUST reference it as evidence and copy the exact `21 passed (...)` line and the exact `Container test-appsvc-1 Healthy` line into the SUMMARY's "harness-gate result" subsection. The log file is the verifier's audit trail.

    4. **If the gate is green:** the task is complete. Phase 10 ROADMAP SC#3 is met. Proceed to write the SUMMARY per the `<output>` block below.
  </action>
  <verify>
    <automated>docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright 2>&1 | tee /tmp/phase10-final-harness.log | tail -10 | grep -E '21 passed' && ! grep -E ' [1-9][0-9]* failed' /tmp/phase10-final-harness.log && ! grep -E '^\s*[1-9][0-9]* flaky' /tmp/phase10-final-harness.log && grep -E 'Container test-appsvc-1\s+Healthy' /tmp/phase10-final-harness.log</automated>
  </verify>
  <acceptance_criteria>
    - The canonical command `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 (captured via `${PIPESTATUS[0]}` immediately after the command).
    - `grep -E '21 passed' /tmp/phase10-final-harness.log` exits 0 (Playwright reports exactly 21 passing tests — 7 specs × 3 browser projects).
    - `grep -E ' [1-9][0-9]* failed' /tmp/phase10-final-harness.log` exits non-zero (no `N failed` line where N is positive — zero failures).
    - `grep -c 'flaky' /tmp/phase10-final-harness.log` outputs `0` (no `flaky` anywhere — no retries — Modes A/B/C all eliminated, not just masked by retry).
    - `grep -c 'No tests found' /tmp/phase10-final-harness.log` outputs `0` (the `--pass-with-no-tests` flag from Plan 10-01 should not be triggered now that all 7 specs exist).
    - `grep -E 'Container test-appsvc-1\s+Healthy' /tmp/phase10-final-harness.log` exits 0 (the appsvc container reported healthy — proves the HSTS rename + healthcheck wiring still working from 10-01 / 10-05).
    - The log shows results for all 3 browser projects: `grep -c '\[chromium\]\|\[firefox\]\|\[webkit\]' /tmp/phase10-final-harness.log` outputs at least `21` (every test entry tagged with a project, no project skipped).
    - `/tmp/phase10-final-harness.log` exists, is non-empty, and contains the `21 passed` line. The SUMMARY (created via the `<output>` block) references it.
  </acceptance_criteria>
  <done>Canonical command exits 0; harness reports `21 passed` and zero failed across all 3 browser projects; no `flaky` retries; appsvc container Healthy; `/tmp/phase10-final-harness.log` captured for SUMMARY evidence; ROADMAP Phase 10 SC#3 met.</done>
</task>

</tasks>

<verification>
This plan owns the authoritative ROADMAP Phase 10 SC#3 verification gate (the gate that 10-07 abandoned). Task 4's automated command IS the gate. Tasks 1-3 are the three independent fixes the verifier identified; Task 4 proves they all close the gap simultaneously.

Cross-cutting verification (run before declaring 10-08 complete):
- `git diff --stat frontend/src/components/portfolio/Heatmap.tsx test/01-fresh-start.spec.ts test/04-sell.spec.ts` shows exactly 3 files changed; no other files touched by this plan.
- The Plan 10-06 + 10-07 edits MUST be present (depends_on: [10-06, 10-07] enforces ordering). Sanity-check:
  - `grep -E '^\s*workers:\s*1\b' test/playwright.config.ts` exits 0 (10-06 Task 1)
  - `grep -c "getByTestId('watchlist-panel')" test/01-fresh-start.spec.ts` outputs at least `2` (10-06 Task 2)
  - `grep -c '\$10,000.00' test/03-buy.spec.ts` outputs `0` (10-06 Task 3)
  - `grep -c 'postBuyQty - 1' test/04-sell.spec.ts` outputs `1` (10-06 Task 4 — preserved through Task 3 of this plan)
  - `grep -c 'dismissChartTooltip' test/05-portfolio-viz.spec.ts` outputs at least `2` (10-07 Task 1; helper stays in place as belt-and-suspenders)
- `/tmp/phase10-final-harness.log` exists and contains the `21 passed` line and the `Container test-appsvc-1 Healthy` line.
- After Task 4 succeeds, `git status` shows only the 3 modified files staged (or already committed via per-task atomic commits).
</verification>

<success_criteria>
- `frontend/src/components/portfolio/Heatmap.tsx` imports `Tooltip` from `recharts` and renders `<Tooltip wrapperStyle={{ pointerEvents: 'none' }} />` as a child of `<Treemap>`. `npm run build` green.
- `test/01-fresh-start.spec.ts` no longer asserts the absolute literal `$10,000.00`; the explanatory comment ("Cross-project SQLite leak") replaces the old assertion.
- `test/04-sell.spec.ts` declares `let postBuyQty = 0` and populates it via `await expect.poll(async () => { postBuyQty = parseFloat(...); return postBuyQty; }, { timeout: 10_000 }).toBeGreaterThanOrEqual(2)` BEFORE the sell action. The existing post-sell `expect.poll(...).toBe(postBuyQty - 1)` block (10-06) is preserved unchanged.
- The canonical command `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0.
- The harness reports `21 passed`, zero failures, zero flaky retries, across chromium + firefox + webkit. appsvc container Healthy.
- `/tmp/phase10-final-harness.log` captures the run; the SUMMARY copies the `21 passed` and `Healthy` lines into its evidence block.
- ROADMAP Phase 10 SC#3 ("single command finishes green reproducibly") is met. Phase 10 verification status will move from `gaps_found` to `verified` on the next verifier pass (this plan does NOT write VERIFICATION.md itself).
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-validation/10-08-SUMMARY.md` per the standard summary template (`templates/summary.md`). The SUMMARY MUST include:

- **frontmatter**:
  - `phase: 10-e2e-validation`
  - `plan: 08`
  - `subsystem: testing`
  - `tags: [playwright, e2e, recharts-tooltip, sqlite-volume-leak, react-query-race, gap-closure]`
  - `requires:` Plan 10-06 (Gap Group A — workers: 1, scoped selectors, dropped $10k in 03-buy, relative-delta in 04-sell) + Plan 10-07 (Gap Group B Task 1 — `dismissChartTooltip` helper in 05-portfolio-viz)
  - `provides:` Mode A/B/C closure + the canonical 21/21 green gate
  - `affects:` (none — Phase 10 is the final phase; the SUMMARY notes Phase 10 ROADMAP SC#3 is now MET upon successful 10-08 completion)
  - `tech-stack.added: []` (no new dependencies)
  - `tech-stack.patterns: ["wrapperStyle pointerEvents: 'none' for non-blocking Recharts hover tooltips", "expect.poll-stabilised snapshot reads before downstream relative-delta assertions"]`
  - `key-files.created: []`, `key-files.modified: [frontend/src/components/portfolio/Heatmap.tsx, test/01-fresh-start.spec.ts, test/04-sell.spec.ts]`
  - `requirements-completed: [TEST-03, TEST-04]`
  - `duration:` actual end-to-end time
  - `completed:` ISO date

- **key-decisions** section (must include all four):
  1. **Override of 10-07's "do NOT modify Heatmap" guidance.** 10-07-PLAN.md Task 1 `<action>` block stated `Do not modify the Heatmap or PnLChart frontend components — the tooltip is correct production behavior; only the test needs to handle it.` Today's verifier (10-VERIFICATION.md commit 4f690e6) demonstrated by direct inspection of `/tmp/phase10-gap-closure-harness.log` lines 619-647, 668-696 that the test-only mitigation (Escape + mouse displacement) does NOT actually retract the Recharts default tooltip on any of the 3 browsers. The production component IS the root cause: a hover tooltip whose wrapper has `pointer-events: auto` is a UX defect (a real user could hover a heatmap tile and find their click on the P&L tab silently absorbed). The override is justified by evidence, addresses root cause per CLAUDE.md ("Identify root cause before fixing"), and is the simplest fix (one prop on a Tooltip element). The test-side `dismissChartTooltip` helper from 10-07 stays in place as belt-and-suspenders — it is harmless after this fix and removing it is out of scope.
  2. **Test-side drop of `$10,000.00` in 01-fresh-start matches the 10-06 pattern in 03-buy.** Same fragility (absolute cash assertion across cross-project SQLite state), same fix (drop the absolute, rely on contextual proofs — watchlist visibility + streaming proof — for `fresh start`). Heavier alternatives (per-project compose runs, test-only `/api/test/reset` endpoint) are blocked by locked CONTEXT decisions D-03 and D-06 and would expand scope beyond test-side robustness.
  3. **expect.poll snapshot stabilisation in 04-sell mirrors 10-06's expect.poll for the post-sell delta.** The visibility wait at lines 25-27 only proves the row exists; it does NOT guarantee the qty cell has refetched its post-buy value. Wrapping the snapshot in `expect.poll(...).toBeGreaterThanOrEqual(2)` (the buy added exactly 2, so any settled state must be >= 2) eliminates the React Query refetch race and gives the existing post-sell assertion a stable baseline. Same pattern, applied symmetrically.
  4. **Canonical command run verbatim per CONTEXT D-03 — no flags, no filters, no per-project orchestration.** The whole purpose of the gate is the unmodified D-03 invocation. Adding `--retries=2` or `-p chromium` would mask root cause and produce a "green" result that doesn't actually prove SC#3.

- **patterns-established** section:
  - "Recharts Treemap tooltip non-blocking: explicitly render `<Tooltip wrapperStyle={{ pointerEvents: 'none' }} />` as a child of `<Treemap>`. Default tooltip wrapper has `pointer-events: auto` and intercepts clicks on sibling elements; the prop is a one-line, no-functional-change fix."
  - "Snapshot-then-assert race avoidance: when capturing a numeric value that depends on a downstream React Query refetch (post-mutation state), wrap the capture in `expect.poll(...)` matched against a known-good lower bound (e.g. `toBeGreaterThanOrEqual(N)` where N is the minimum the data must reach after the mutation). Pair with a `let` binding so the closure can update the captured value on each iteration."

- **harness-gate result** subsection: copy the EXACT `21 passed (...)` line and the EXACT `Container test-appsvc-1 ... Healthy` line from `/tmp/phase10-final-harness.log` so the SUMMARY is self-contained evidence. Include the harness exit code (0).

- **Phase 10 closure note**: "ROADMAP Phase 10 SC#3 (single command finishes green reproducibly) is now MET. TEST-03 and TEST-04 will be ticked in REQUIREMENTS.md by the verifier on its next pass — this plan does not write VERIFICATION.md itself."

- **Self-Check** (per the template's standard checklist), with each line greppable. Examples:
  - `[ ] frontend/src/components/portfolio/Heatmap.tsx contains wrapperStyle={{ pointerEvents: 'none' }}` (verified by grep)
  - `[ ] test/01-fresh-start.spec.ts does not contain $10,000.00` (verified by `grep -c` outputs `0`)
  - `[ ] test/04-sell.spec.ts contains toBeGreaterThanOrEqual(2)` (verified by `grep -c` outputs `1`)
  - `[ ] /tmp/phase10-final-harness.log exists and contains 21 passed`
  - `[ ] git diff HEAD~3 HEAD --name-only returns exactly the 3 expected paths` (or HEAD~4 HEAD if Task 4's gate run included a docs-update commit)
  - `[ ] cd test && npx playwright test --list 2>&1 | tail -1 reports Total: 21 tests in 7 files`
</output>
