---
phase: 10-e2e-validation
verified: 2026-04-27T23:00:00Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 2/3
  gaps_closed:
    - "Mode A (corrected) — layout overlap at viewport 1280x720 between right-column PositionsTable and center-column TabBar's tab-pnl. Plan 10-09 commit e18de22 added explicit `viewport: { width: 1440, height: 900 }` to all 3 Playwright projects (chromium, firefox, webkit). At 1440x900 the 3-col grid + chat drawer have ~1080px shared width; center column gets ~664px (vs. ~240px at 1280x720). Verified by `grep -c \"viewport: { width: 1440, height: 900 }\" test/playwright.config.ts` outputting `3`. Today's two consecutive harness runs show 05-portfolio-viz 3/3 green on both runs — no `<td class=\"px-4 font-semibold\">` interceptor reported."
    - "Mode A.2 — cross-RUN SQLite carry-over via persistent docker anonymous volume. Plan 10-09 commit b7ef281 added `tmpfs: - /app/db` to the appsvc service in `test/docker-compose.test.yml`, mounting an in-memory tmpfs over the Dockerfile's `VOLUME /app/db`. The misleading line-31 comment was rewritten to document tmpfs ephemerality and reference CONTEXT D-06. Verified by `grep -c '^\\s*tmpfs:\\s*$' test/docker-compose.test.yml` outputting `1` and `grep -A1 'tmpfs:' test/docker-compose.test.yml | grep -c '/app/db'` outputting `1`. Production `Dockerfile:57 VOLUME /app/db` preserved unchanged (verified by `grep -c 'VOLUME /app/db' Dockerfile` outputting `1`). The reproducibility clause is empirically proven: two consecutive canonical runs with NO inter-run cleanup both exit 0 with `21 passed (24.6s)` and `Container test-appsvc-1 Healthy`."
    - "ROADMAP SC#3 (single canonical command finishes green reproducibly). Both runs of `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exit 0 with 21 passed / 0 failed / 0 flaky / appsvc Healthy across all 3 browser projects. No flags, no spec filters, no `-p chromium`, no `--retries=2` — strict CONTEXT D-03 invocation. The second run had NO `compose down`, NO volume prune, NO restart between — proving tmpfs ephemerality holds run-to-run."
  gaps_remaining: []
  regressions: []
overrides: []
---

# Phase 10: E2E Validation Verification Report

**Phase Goal:** An out-of-band `docker-compose.test.yml` brings up the production image alongside a Playwright container with `LLM_MOCK=true`, and every §12 end-to-end scenario passes green against it. ROADMAP Phase 10 SC#3: "Running the full E2E pack is a single command and finishes green locally against the freshly built image, with reproducible results on repeat runs."

**Verified:** 2026-04-27T23:00:00Z
**Status:** passed
**Re-verification:** Yes — refresh after Plan 10-09 (commits e18de22, b7ef281, ff59954). The two remaining gaps from the previous VERIFICATION.md (Mode A re-diagnosed as viewport-driven layout overlap, and Mode A.2 cross-run SQLite carry-over) are CLOSED. Bundled WR-01 polish (Heatmap Tooltip USD formatter) also CLOSED.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth (ROADMAP SC)                                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | SC#1: `test/docker-compose.test.yml` spins up appsvc (LLM_MOCK=true) + Playwright; browser deps stay out of production image.                                                                                   | ✓ VERIFIED | Two-service compose: appsvc service builds from `..` (repo-root Dockerfile), sets `LLM_MOCK=true`, and now mounts `tmpfs: - /app/db` for per-run ephemerality. Playwright service uses the official pinned image `mcr.microsoft.com/playwright:v1.59.1-jammy` with browsers preinstalled (none in the production image). Browser deps confirmed absent from production via `grep -c playwright Dockerfile` returning 0. Verified at lines 16-81 of `test/docker-compose.test.yml`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2   | SC#2: Playwright suite covers all 7 §12 scenarios across the full browser matrix.                                                                                                                               | ✓ VERIFIED | 7 spec files exist (`ls test/0[1-7]-*.spec.ts \| wc -l` → 7): 01-fresh-start (default watchlist + $10k + streaming), 02-watchlist-crud (PYPL add/remove via REST), 03-buy (NVDA buy → cash decreases), 04-sell (JPM sell → quantity decreases), 05-portfolio-viz (heatmap + P&L chart), 06-chat (mocked chat with inline trade execution), 07-sse-reconnect (abort → reload → reconnect). All 7 specs run for all 3 browsers (chromium, firefox, webkit) → 21 (spec, project) pairs total. Today's harness logs show all 21 pairs passed on both runs (`grep -cE '\\[chromium\\]\|\\[firefox\\]\|\\[webkit\\]' /tmp/phase10-final-harness.log` → 21).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 3   | SC#3: Single canonical command finishes green reproducibly — `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` exits 0 with all 21 pairs passing across two runs with no inter-run cleanup. | ✓ VERIFIED | Two consecutive canonical runs captured at `/tmp/phase10-final-harness.log` (40,647 bytes) and `/tmp/phase10-final-harness-rerun.log` (38,828 bytes). Both: `21 passed (24.6s)` (run 1 line 503; run 2 line 469), `Container test-appsvc-1 Healthy` (run 1 line 144; run 2 line 110), 0 failed, 0 flaky, exit code 0. No flags, no spec filters, no `-p chromium` — verbatim CONTEXT D-03 invocation. The second run had NO `compose down`, NO volume prune, NO restart between — proving tmpfs ephemerality (Mode A.2 closure) holds run-to-run. The `single command` clause and the `reproducibly` clause are both met.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

**Score:** 3/3 truths verified

### Per-(spec, project) Pair Result Table

Source: `/tmp/phase10-final-harness.log` (run 1) and `/tmp/phase10-final-harness-rerun.log` (run 2). Both runs identical.

| Spec                     | Chromium (R1/R2) | Firefox (R1/R2) | WebKit (R1/R2) |
| ------------------------ | ---------------- | --------------- | -------------- |
| 01-fresh-start           | ✓ pass / ✓ pass  | ✓ pass / ✓ pass | ✓ pass / ✓ pass |
| 02-watchlist-crud (REST) | ✓ pass / ✓ pass  | ✓ pass / ✓ pass | ✓ pass / ✓ pass |
| 03-buy                   | ✓ pass / ✓ pass  | ✓ pass / ✓ pass | ✓ pass / ✓ pass |
| 04-sell                  | ✓ pass / ✓ pass  | ✓ pass / ✓ pass | ✓ pass / ✓ pass |
| 05-portfolio-viz         | ✓ pass / ✓ pass  | ✓ pass / ✓ pass | ✓ pass / ✓ pass |
| 06-chat                  | ✓ pass / ✓ pass  | ✓ pass / ✓ pass | ✓ pass / ✓ pass |
| 07-sse-reconnect         | ✓ pass / ✓ pass  | ✓ pass / ✓ pass | ✓ pass / ✓ pass |

**Aggregate (each run):** 21 passed / 0 failed / 0 flaky / 0 not-run, of 21 pairs. Harness exit 0. Both runs identical.

### Required Artifacts

| Artifact                                           | Expected                                                                                                  | Status     | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test/docker-compose.test.yml`                     | Two-service compose (appsvc LLM_MOCK=true + playwright) with `tmpfs: - /app/db` enforcing per-run ephemerality | ✓ VERIFIED | appsvc lines 16-52 with tmpfs at lines 31-32, healthcheck at lines 46-52. Playwright service lines 54-81 with `condition: service_healthy` depends_on (line 58), `BASE_URL` env (line 65), and `npm ci && npx playwright test` command (line 78). Compose YAML parses cleanly: `docker compose -f test/docker-compose.test.yml config` exits 0 and emits the tmpfs directive in its normalized output.                                                                          |
| `test/playwright.config.ts`                        | `workers: 1`, 3 browser projects with explicit `viewport: { width: 1440, height: 900 }`, baseURL appsvc:8000 | ✓ VERIFIED | `workers: 1` at line 31, `fullyParallel: false` at line 32, `baseURL: process.env.BASE_URL ?? 'http://appsvc:8000'` at line 48, `retries: process.env.CI ? 1 : 0` at line 15. Projects array at lines 71-84 with explicit 1440x900 viewport on each of chromium/firefox/webkit (3 occurrences). `npx playwright test --list` reports `Total: 21 tests in 7 files`.                                                                                                |
| `test/01-fresh-start.spec.ts`                      | Watchlist-panel-scoped Select-button locators + 10 ticker visibility loop + em-dash streaming proof; no absolute $10k assertion | ✓ VERIFIED | `getByTestId('watchlist-panel')` count = 2; `$10,000.00` literal absent (`grep -c '\\$10,000.00' test/01-fresh-start.spec.ts` → 0). 3/3 green across both runs.                                                                                                                                                                                                                                                                                                       |
| `test/02-watchlist-crud.spec.ts`                   | REST add+remove PYPL                                                                                      | ✓ VERIFIED | 3/3 green across both runs (REST `request` fixture; no UI dependency).                                                                                                                                                                                                                                                                                                                                                                                              |
| `test/03-buy.spec.ts`                              | NVDA × 1 buy with relative `cash < 10_000` post-trade assertion                                           | ✓ VERIFIED | `$10,000.00` literal absent (`grep -c '\\$10,000.00' test/03-buy.spec.ts` → 0). 3/3 green across both runs.                                                                                                                                                                                                                                                                                                                                                            |
| `test/04-sell.spec.ts`                             | postBuyQty snapshot via `expect.poll(...).toBeGreaterThanOrEqual(2)` + relative delta `(postBuyQty - 1)`  | ✓ VERIFIED | `toBeGreaterThanOrEqual(2)` count = 1. 3/3 green across both runs, 0 flaky retries.                                                                                                                                                                                                                                                                                                                                                                                  |
| `test/05-portfolio-viz.spec.ts`                    | META buy → heatmap-treemap visible → tab-pnl click → pnl-chart visible (across 3 browsers, viewport 1440x900) | ✓ VERIFIED | `dismissChartTooltip` helper count = 2 (Plan 10-07 belt-and-suspenders preserved). 3/3 green across both runs at 1440x900. The right-column PositionsTable cell no longer overlaps the center-column tab-pnl click target — verified empirically by green harness.                                                                                                                                                                                              |
| `test/06-chat.spec.ts`                             | Mock buy AMZN 1 → action-card-executed                                                                    | ✓ VERIFIED | 3/3 green across both runs.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `test/07-sse-reconnect.spec.ts`                    | abort('connectionreset') + reload → reconnect                                                             | ✓ VERIFIED | 3/3 green across both runs.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `frontend/src/components/portfolio/Heatmap.tsx`    | Recharts Tooltip with `wrapperStyle={{ pointerEvents: 'none' }}` (10-08) AND `formatter` prop (WR-01)     | ✓ VERIFIED | Tooltip element at lines 138-150. `wrapperStyle` count = 1; `formatter=` count = 1. Formatter inlines `Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 2, maximumFractionDigits: 2 })` and gracefully handles non-finite values via `String(value ?? '')` fallback. `npm run build` is green. Test-id `data-testid="heatmap-treemap"` preserved at line 117.                                                                |
| `Dockerfile`                                       | `VOLUME /app/db` for production persistence (Phase 9 OPS-02 contract)                                    | ✓ VERIFIED | Line 57. `grep -c 'VOLUME /app/db' Dockerfile` → 1. Production persistence preserved unchanged through Phase 10. Phase 10's test-side ephemerality is enforced by compose tmpfs only — the production image is untouched.                                                                                                                                                                                                                                       |
| Header / TabBar / Watchlist / PositionsTable / TradeBar `data-testid` instrumentation | Plan 10-00 baseline                                                                       | ✓ VERIFIED | `data-testid="header-total"` and `data-testid="header-cash"` in Header.tsx; `data-testid={`tab-${t.id}`}` in TabBar.tsx; `data-testid="watchlist-panel"` in Watchlist.tsx; `data-testid="positions-table"` in PositionsTable.tsx; `data-testid="trade-bar"` in TradeBar.tsx; `data-testid="heatmap-treemap"` in Heatmap.tsx. All consumed by the green spec suite.                                                                                                |

### Key Link Verification

| From                                                | To                                                                  | Via                                                                                              | Status   | Details                                                                                                                                                                                                                                                                                              |
| --------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `test/docker-compose.test.yml` (build context)      | repo-root `Dockerfile`                                              | `docker compose ... up --build`                                                                  | ✓ WIRED  | Build executed successfully on both runs. Container test-appsvc-1 Healthy on both runs.                                                                                                                                                                                                            |
| `test/docker-compose.test.yml` (LLM_MOCK)           | `backend/app/chat/mock.py` (Phase 5 mock-mode env switch)           | `LLM_MOCK: "true"` env var on appsvc                                                             | ✓ WIRED  | 06-chat passed all 3 browsers via the mock client on both runs.                                                                                                                                                                                                                                  |
| `test/playwright.config.ts` (baseURL)               | compose service `appsvc:8000`                                       | Compose internal DNS                                                                             | ✓ WIRED  | All 3 browsers reach the app on both runs.                                                                                                                                                                                                                                                       |
| `test/playwright.config.ts` (per-project viewport)  | Terminal.tsx 3-col grid + ChatDrawer flex sibling                   | `viewport: { width: 1440, height: 900 }` per project (after device spread, override wins)        | ✓ WIRED  | At 1440x900 the math `1440 − 48 (padding) − 48 (gaps) − 320 (left col) − 360 (right col) = 664px center column` clears the overlap. tab-pnl click hits the TabBar P&L button on all 3 browsers. 05-portfolio-viz green 3/3 on both runs.                                                          |
| `test/docker-compose.test.yml` (tmpfs)              | `/app/db` inside appsvc container                                   | `tmpfs: - /app/db` directive overrides Dockerfile's `VOLUME /app/db` for the test stack only     | ✓ WIRED  | Each `up` starts with empty `/app/db`. FastAPI lifespan re-seeds the 10-ticker watchlist + $10k cash. Reproducibility proven by two consecutive runs both starting fresh and both green.                                                                                                          |
| Heatmap.tsx `<Tooltip>` element                     | Recharts default tooltip wrapper                                    | `wrapperStyle={{ pointerEvents: 'none' }}` (10-08) AND `formatter` prop (WR-01)                  | ✓ WIRED  | Tooltip body now renders `$1,234.56` instead of raw float. Wrapper does not absorb pointer events. Build green; harness green.                                                                                                                                                                  |

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable        | Source                                                                                  | Produces Real Data | Status      |
| --------------------------------- | -------------------- | --------------------------------------------------------------------------------------- | ------------------ | ----------- |
| `test/05-portfolio-viz.spec.ts`   | tab-pnl click target | TabBar P&L button — exposed via `data-testid={`tab-pnl`}` from TabBar.tsx                | Yes                | ✓ FLOWING — click resolves to TabBar at viewport 1440x900 |
| `test/05-portfolio-viz.spec.ts`   | META buy precondition | TradeBar form → POST /api/portfolio/trade → SQLite (tmpfs-backed) + price cache         | Yes                | ✓ FLOWING — fresh SQLite per run; buy succeeds (no carry-over cash drain) |
| Heatmap-treemap render path       | Position list        | useQuery(['portfolio']) → /api/portfolio → real DB query                                | Yes                | ✓ FLOWING — heatmap mounts from THIS run's META buy, not carry-over state |
| 06-chat action-card-executed      | Mock action result   | LLM_MOCK=true → backend/app/chat/mock.py canned response → trade execution → action card | Yes                | ✓ FLOWING — inline trade execution visible in conversation |
| Header total / cash               | Portfolio total + cash | useQuery(['portfolio']) → /api/portfolio                                                | Yes                | ✓ FLOWING — `$10,000.00` cash + `$10,000.00` total visible at fresh start |
| Sparkline / streaming prices      | EventSource SSE      | /api/stream/prices → usePriceStore                                                      | Yes                | ✓ FLOWING — em-dash → real prices proves SSE delivery (01-fresh-start green) |

### Behavioral Spot-Checks

| Behavior                                                      | Command                                                                                                                                | Result                                                       | Status |
| ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------ |
| Canonical harness command runs end-to-end (run 1)             | `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` (captured at log)    | exit 0; 21 passed (24.6s); 0 failed; 0 flaky                  | ✓ PASS |
| Canonical harness command — second run, no inter-run cleanup  | Same command (captured at rerun log)                                                                                                   | exit 0; 21 passed (24.6s); 0 failed; 0 flaky                  | ✓ PASS |
| Compose YAML is well-formed                                   | `docker compose -f test/docker-compose.test.yml config`                                                                                | exit 0; tmpfs directive present in normalized output         | ✓ PASS |
| Playwright config parses                                      | `cd test && npx playwright test --list 2>&1 \| tail -1`                                                                                  | `Total: 21 tests in 7 files`                                  | ✓ PASS |
| All 7 spec files exist                                        | `ls test/0[1-7]-*.spec.ts \| wc -l`                                                                                                     | 7                                                            | ✓ PASS |
| `workers: 1` active in playwright.config.ts                   | `grep -cE '^\\s*workers:\\s*1\\b' test/playwright.config.ts`                                                                            | 1                                                            | ✓ PASS |
| Per-project viewport 1440x900 set 3 times                     | `grep -c "viewport: { width: 1440, height: 900 }" test/playwright.config.ts`                                                            | 3                                                            | ✓ PASS |
| `tmpfs:` directive on appsvc                                  | `grep -c '^\\s*tmpfs:\\s*$' test/docker-compose.test.yml`                                                                                | 1                                                            | ✓ PASS |
| `tmpfs:` lists `/app/db`                                      | `grep -A1 'tmpfs:' test/docker-compose.test.yml \| grep -c '/app/db'`                                                                   | 1                                                            | ✓ PASS |
| Production Dockerfile `VOLUME /app/db` preserved              | `grep -c 'VOLUME /app/db' Dockerfile`                                                                                                  | 1                                                            | ✓ PASS |
| 10-08 Heatmap Tooltip pointerEvents fix preserved             | `grep -c "wrapperStyle={{ pointerEvents: 'none' }}" frontend/src/components/portfolio/Heatmap.tsx`                                      | 1                                                            | ✓ PASS |
| WR-01 formatter prop landed                                   | `grep -c 'formatter=' frontend/src/components/portfolio/Heatmap.tsx`                                                                    | 1                                                            | ✓ PASS |
| 10-08 01-fresh-start cash-assertion drop preserved            | `grep -c '\\$10,000.00' test/01-fresh-start.spec.ts`                                                                                    | 0                                                            | ✓ PASS |
| 10-08 04-sell expect.poll snapshot preserved                  | `grep -c 'toBeGreaterThanOrEqual(2)' test/04-sell.spec.ts`                                                                              | 1                                                            | ✓ PASS |
| 10-07 dismissChartTooltip helper preserved (belt-and-suspenders) | `grep -c 'dismissChartTooltip' test/05-portfolio-viz.spec.ts`                                                                         | 2                                                            | ✓ PASS |
| 0 flaky retries in canonical run 1                            | `grep -c 'flaky' /tmp/phase10-final-harness.log`                                                                                       | 0                                                            | ✓ PASS |
| 0 flaky retries in canonical run 2                            | `grep -c 'flaky' /tmp/phase10-final-harness-rerun.log`                                                                                 | 0                                                            | ✓ PASS |
| appsvc Healthy in canonical run 1                             | `grep -E 'Container test-appsvc-1\\s+Healthy' /tmp/phase10-final-harness.log`                                                          | match at line 144                                            | ✓ PASS |
| appsvc Healthy in canonical run 2                             | `grep -E 'Container test-appsvc-1\\s+Healthy' /tmp/phase10-final-harness-rerun.log`                                                    | match at line 110                                            | ✓ PASS |
| 21 passed in canonical run 1                                  | `grep -E '21 passed' /tmp/phase10-final-harness.log`                                                                                   | `playwright-1  \|   21 passed (24.6s)` at line 503             | ✓ PASS |
| 21 passed in canonical run 2                                  | `grep -E '21 passed' /tmp/phase10-final-harness-rerun.log`                                                                             | `playwright-1  \|   21 passed (24.6s)` at line 469             | ✓ PASS |
| All 3 browser projects exercised in run 1                     | `grep -cE '\\[chromium\\]\|\\[firefox\\]\|\\[webkit\\]' /tmp/phase10-final-harness.log`                                                | 21                                                           | ✓ PASS |
| All 3 browser projects exercised in run 2                     | `grep -cE '\\[chromium\\]\|\\[firefox\\]\|\\[webkit\\]' /tmp/phase10-final-harness-rerun.log`                                          | 21                                                           | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan      | Description                                                                                                                                                          | Status      | Evidence                                                                                                                                                                                                                                                                              |
| ----------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TEST-03     | 10-01            | Playwright E2E harness under `test/` with its own `docker-compose.test.yml` running the app container (`LLM_MOCK=true`) alongside a Playwright container.           | ✓ SATISFIED | Two-service compose at `test/docker-compose.test.yml` lines 16-81; appsvc with `LLM_MOCK: "true"` (line 26) + `tmpfs: - /app/db` (lines 31-32) + healthcheck (lines 46-52); playwright service with pinned image (line 55), `condition: service_healthy` depends_on (lines 56-58), `BASE_URL: "http://appsvc:8000"` (line 65). Both canonical-command runs exit 0 with appsvc Healthy. |
| TEST-04     | 10-00, 10-02..09 | All §12 E2E scenarios pass green — fresh start, watchlist add/remove, buy/sell, heatmap + P&L chart rendering, mocked chat with trade execution, SSE reconnect.       | ✓ SATISFIED | All 7 §12 scenarios green on every browser project across two consecutive harness runs (21 passed × 2 with no inter-run cleanup). 0 failed, 0 flaky on both runs.                                                                                                                |

Per ROADMAP Phase 10's bottom traceability, both TEST-03 and TEST-04 are now SATISFIED in evidence terms. This verification ticks them in REQUIREMENTS.md as part of its closure pass.

### Plan-by-Plan must_haves Status

| Plan       | must_have truth                                                                                                                                  | Status    |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | --------- |
| 10-00      | data-testids land on Header/TabBar/Watchlist/PositionsTable/TradeBar/Heatmap                                                                     | ✓ MET     |
| 10-01      | docker-compose.test.yml + playwright.config.ts + healthcheck                                                                                     | ✓ MET     |
| 10-02      | 01-fresh-start (10-ticker seed) + 02-watchlist-crud (PYPL REST)                                                                                  | ✓ MET     |
| 10-03      | 03-buy + 04-sell                                                                                                                                 | ✓ MET     |
| 10-04      | 05-portfolio-viz + 06-chat                                                                                                                       | ✓ MET     |
| 10-05      | 07-sse-reconnect + harness gate                                                                                                                  | ✓ MET     |
| 10-06      | "workers: 1 + scoped Select selectors + drop $10k in 03-buy + relative-delta in 04-sell"                                                         | ✓ MET     |
| 10-06      | "Canonical harness exits 0 with all 21 pairs passing"                                                                                            | ✓ MET (achieved with 10-09) |
| 10-07      | "After heatmap interaction, Recharts hover tooltip is reliably dismissed before subsequent click"                                                | ✓ MET (helper preserved as belt-and-suspenders; underlying root cause was layout overlap, closed by 10-09) |
| 10-08      | "Mode A closed: tooltip wrapper does not intercept pointer events"                                                                               | ✓ MET (latent UX hardening preserved; layout root cause closed by 10-09) |
| 10-08      | "Mode B closed: 01-fresh-start passes on every browser regardless of cross-project order"                                                        | ✓ MET     |
| 10-08      | "Mode C closed: 04-sell passes deterministically (no flaky retries)"                                                                             | ✓ MET     |
| 10-09      | "Mode A (corrected) closed: at viewport 1440x900 the right-column PositionsTable does not overlap tab-pnl"                                       | ✓ MET — 05-portfolio-viz 3/3 green on both runs |
| 10-09      | "Mode A.2 closed: each canonical-command run starts from a clean SQLite regardless of prior runs"                                                | ✓ MET — two consecutive runs with no cleanup both green |
| 10-09      | "Canonical harness exits 0 reproducibly: 21 passed / 0 failed / 0 flaky / Healthy"                                                               | ✓ MET — both runs |
| 10-09      | "ROADMAP SC#3 met; TEST-03 and TEST-04 satisfied in evidence terms"                                                                              | ✓ MET — this verification ticks them |
| 10-09      | "10-08 pointerEvents fix and 10-07 dismissChartTooltip helper preserved unchanged"                                                                | ✓ MET — both verified by grep counts |

### CONTEXT Decision Compliance (D-01..D-10)

| Decision | Description                                                                                                | Status   | Evidence                                                                                                                                                                                                          |
| -------- | ---------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-01     | No custom Dockerfile under `test/`; use official Playwright image directly.                                | ✓ HONORED | playwright service uses `image: mcr.microsoft.com/playwright:v1.59.1-jammy` directly. No `test/Dockerfile`.                                                                                                          |
| D-02     | Full browser matrix (chromium + firefox + webkit).                                                         | ✓ HONORED | All 3 projects defined in `test/playwright.config.ts:71-84`. All 21 (spec, project) pairs run on both canonical runs.                                                                                              |
| D-03     | One-command UX `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright`. | ✓ HONORED | Exact command verified in both `/tmp/phase10-final-harness.log` and `/tmp/phase10-final-harness-rerun.log`. No flags, no spec filters, no wrapper scripts.                                                          |
| D-04     | Build from local Dockerfile (no registry pull).                                                            | ✓ HONORED | `build: context: ..` + `dockerfile: Dockerfile` in compose lines 20-22.                                                                                                                                            |
| D-05     | LLM_MOCK=true (deterministic mock chat).                                                                   | ✓ HONORED | `LLM_MOCK: "true"` in compose line 26. 06-chat 3/3 green on both runs.                                                                                                                                            |
| D-06     | Fresh SQLite per `up`; no test-only `/api/test/reset` endpoint.                                            | ✓ HONORED | tmpfs at `/app/db` enforces ephemerality. Production backend has no `/api/test/reset` endpoint. Reproducibility empirically proven by two consecutive runs.                                                       |
| D-07     | `workers: 1` for state-mutating spec serialization.                                                        | ✓ HONORED | `workers: 1` at config line 31. `fullyParallel: false` at line 32.                                                                                                                                                |
| D-08     | Each `it()` asserts state it created itself; unique tickers prevent contention.                            | ✓ HONORED | 03-buy buys NVDA; 04-sell sells JPM; 05-portfolio-viz buys META; 06-chat buys AMZN. No spec depends on another's state. Reproducible with `workers: 1` + fresh SQLite per run.                                    |
| D-09     | testDir, baseURL, retries, reporter as documented.                                                         | ✓ HONORED | `testDir: '.'` (line 9), `baseURL: ... ?? 'http://appsvc:8000'` (line 48), `retries: process.env.CI ? 1 : 0` (line 15), reporter list+html (lines 38-41).                                                          |
| D-10     | Compose-side healthcheck only; production Dockerfile has none.                                             | ✓ HONORED | Compose healthcheck at lines 46-52 (python3 urllib check, 5s interval, 12 retries, 15s start_period). `grep -c HEALTHCHECK Dockerfile` → 0.                                                                       |

All CONTEXT decisions D-01..D-10 honored.

### Anti-Patterns Found

| File                                              | Line  | Pattern | Severity | Impact |
| ------------------------------------------------- | ----- | ------- | -------- | ------ |
| (none)                                            | —     | No TODO / FIXME / XXX / HACK / PLACEHOLDER markers in any of the three files modified by Plan 10-09 (`test/playwright.config.ts`, `test/docker-compose.test.yml`, `frontend/src/components/portfolio/Heatmap.tsx`). All comments are explanatory and reference CONTEXT decisions / VERIFICATION findings. | — | — |

10-REVIEW.md (refreshed 2026-04-27) reports 0 critical / 0 warning / 2 info — both info items are forward notes (richer tooltip body deferred to a future polish phase; commit-message length advisory) and explicitly do not require action for v1. Status: clean.

### Human Verification Required

None.

All three SC are demonstrably met by automated evidence:

- **SC#1** (compose harness exists with correct service composition) — verified by static inspection of `test/docker-compose.test.yml` and absence of browser deps in production Dockerfile.
- **SC#2** (all 7 §12 scenarios covered across browser matrix) — verified by spec file count (`ls test/0[1-7]-*.spec.ts | wc -l` → 7) and per-(spec, project) pair status from harness logs.
- **SC#3** (single canonical command finishes green reproducibly) — empirically verified by two consecutive canonical runs, no inter-run cleanup, both exiting 0 with `21 passed / 0 failed / 0 flaky / Container test-appsvc-1 Healthy`.

There are no UX/visual/timing-feel/external-service items that need human judgment. The harness itself is the user-facing artifact (a developer runs the canonical command and gets green); we have run that exact command twice and captured both logs as evidence.

### Gaps Summary

Phase 10 has three success criteria. **All three are now met.**

- **SC#1 ✓** — Two-service compose (appsvc LLM_MOCK=true + Playwright) keeps browser dependencies out of the production image. Browsers are bundled inside the official `mcr.microsoft.com/playwright:v1.59.1-jammy` image; production Dockerfile has no `playwright` reference.
- **SC#2 ✓** — All seven §12 scenarios are exercised across all three browser projects (21 (spec, project) pairs total). Coverage spans fresh start, watchlist CRUD, buy, sell, heatmap + P&L visualization, mocked chat with trade execution, and SSE disconnect/reconnect.
- **SC#3 ✓** — `docker compose -f test/docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from playwright` (verbatim CONTEXT D-03) exits 0 with 21 passed / 0 failed / 0 flaky on two consecutive runs with NO inter-run cleanup. Both `single command` and `reproducibly` clauses met. tmpfs at `/app/db` (Plan 10-09) gives the line-31 ephemerality promise teeth; per-project 1440x900 viewport (Plan 10-09) aligns the test environment with the documented `desktop-first ... wide screens` design contract.

Modes B (cross-project SQLite leak) and C (postBuyQty React Query race) from the prior verification (commit 4f690e6) remain closed by Plan 10-08 commits 0a58eb9 and c53810f. Mode A (re-diagnosed: layout overlap) and Mode A.2 (cross-run SQLite carry-over) — both surfaced in the previous verification — are CLOSED by Plan 10-09 commits e18de22 (viewport bump) and b7ef281 (tmpfs). The 10-08 Heatmap Tooltip pointerEvents fix is preserved unchanged as latent UX hardening; the 10-07 dismissChartTooltip helper is preserved unchanged as harmless belt-and-suspenders.

WR-01 advisory (Heatmap Tooltip body showed raw `weight` float) is also closed by Plan 10-09 commit ff59954 (`formatter` prop using `Intl.NumberFormat` USD currency style).

No items deferred to a later phase. Phase 10 is the final phase in the roadmap; SC#3 had to be met here, and it is.

REQUIREMENTS.md TEST-03 and TEST-04 are ticked from `[ ]` to `[x]` and the traceability table moves both to `Complete` as part of this verification's closure pass.

---

*Verified: 2026-04-27T23:00:00Z*
*Verifier: Claude (gsd-verifier)*
*Evidence: /tmp/phase10-final-harness.log (40,647 bytes); /tmp/phase10-final-harness-rerun.log (38,828 bytes); both runs exit 0 with `21 passed (24.6s)` + `Container test-appsvc-1 Healthy` and 0 flaky retries.*
