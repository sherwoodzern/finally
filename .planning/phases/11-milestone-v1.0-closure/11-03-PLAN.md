---
phase: 11-milestone-v1.0-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md
  - .planning/phases/09-dockerization-packaging/09-VERIFICATION.md
autonomous: true
requirements: [OPS-02, OPS-03]
tags: [milestone-closure, human-acceptance, doc-only]

must_haves:
  truths:
    - "07-VERIFICATION.md frontmatter retains `status: human_needed` AND adds `human_acceptance: indefinite` with `human_acceptance_recorded: 2026-04-28` and a multi-line `human_acceptance_rationale:` block citing canonical Phase 10 harness evidence (21 passed × 2 consecutive runs)"
    - "09-VERIFICATION.md frontmatter retains `status: human_needed` AND adds the same three sibling keys with a Phase 9-specific rationale (Windows pwsh + browser auto-open + visual UI deferred to per-host human spot-check)"
    - "Frontmatter `status:` enum value is unchanged (only `passed`, `gaps_found`, `human_needed` are valid — Pitfall 3 in 11-RESEARCH.md)"
    - "Neither phase's `human_verification:` list is altered"
  artifacts:
    - path: ".planning/phases/07-market-data-trading-ui/07-VERIFICATION.md"
      provides: "Recorded acceptance decision for Phase 7 (G3 closure)"
      contains: "human_acceptance: indefinite"
    - path: ".planning/phases/09-dockerization-packaging/09-VERIFICATION.md"
      provides: "Recorded acceptance decision for Phase 9 (G4 closure)"
      contains: "human_acceptance: indefinite"
  key_links:
    - from: "Phase 10 canonical harness evidence (`21 passed × 2 consecutive runs`)"
      to: "07-VERIFICATION.md and 09-VERIFICATION.md `human_acceptance_rationale` blocks"
      via: "Inline citation in the rationale text — no schema change"
      pattern: "human_acceptance: indefinite + rationale citing harness"
---

<objective>
Close G3 (Phase 7) and G4 (Phase 9) from `.planning/v1.0-MILESTONE-AUDIT.md`: both phases sit at
`status: human_needed` because their `human_verification:` blocks describe items that are visual
feel / platform spot-checks the harness cannot exercise. The runtime behavior underlying every
item IS exercised by the canonical Phase 10 harness (`21 passed × 2 consecutive runs`, 0 failed,
0 flaky, `Container test-appsvc-1 Healthy`). This plan records that evidence as accepted policy
debt — Option B from `11-RESEARCH.md` "Pattern 3" (recommended in the planning context).

Purpose: replace silent stuck `human_needed` status with explicit, dated acceptance backed by
harness evidence. The audit re-run can then classify these as `policy_debt`, not `tech_debt`.

Output: two YAML frontmatter mutations — sibling keys `human_acceptance: indefinite`,
`human_acceptance_recorded: 2026-04-28`, and a multi-line `human_acceptance_rationale:` block
added to each VERIFICATION.md frontmatter. Neither file's body is touched.

Scope boundary: zero production source changes. Zero `status:` enum mutation (Pitfall 3 — Option B
keeps the existing `human_needed` value). The `human_verification:` lists themselves are NOT
modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/11-milestone-v1.0-closure/11-RESEARCH.md
@.planning/phases/11-milestone-v1.0-closure/11-PATTERNS.md
@.planning/phases/11-milestone-v1.0-closure/11-VALIDATION.md
@.planning/phases/07-market-data-trading-ui/07-VERIFICATION.md
@.planning/phases/09-dockerization-packaging/09-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add human_acceptance frontmatter block to 07-VERIFICATION.md</name>
  <files>.planning/phases/07-market-data-trading-ui/07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md (current frontmatter lines 1-39 — 6-item human_verification list)
    - .planning/phases/11-milestone-v1.0-closure/11-PATTERNS.md (lines 261-342: full pattern map for Phase 7 frontmatter mutation; Option B reference YAML)
    - .planning/phases/11-milestone-v1.0-closure/11-RESEARCH.md (lines 169-194: Pattern 3 — VERIFICATION.md status flip mechanic; Pitfall 3 schema constraints)
  </read_first>

  <action>
Insert three new sibling keys into the 07-VERIFICATION.md frontmatter using the Edit tool. The
insertion point is **immediately after** `status: human_needed` (line 4 in the current file) —
the new block sits BEFORE `score:`. This placement keeps `status:` and the new acceptance keys
visually grouped while preserving every other existing key.

**The insertion (verbatim YAML — preserve indentation exactly as shown, two-space indent for
the rationale block):**

```yaml
status: human_needed
human_acceptance: indefinite
human_acceptance_recorded: 2026-04-28
human_acceptance_rationale: |
  All 6 human_verification items are visual-feel checks (CSS price-flash cadence,
  Lightweight Charts sparkline canvas, click-to-select cross-panel flow, instant-fill
  UX, EventSource reconnect state-machine dot, three-column Bloomberg-style aesthetic).
  The runtime behavior underlying every item is exercised by the canonical Phase 10
  harness (7 specs x 3 browsers x 2 consecutive runs = 21 passed x 2, 0 failed, 0 flaky,
  Container test-appsvc-1 Healthy). The "feel" is the only deferred item. Recorded
  here as accepted policy debt for v1.0 milestone closure (Phase 11 G3).
score: 5/5 must-haves verified
```

Use the Edit tool with `old_string` matching the exact current line `status: human_needed\nscore: 5/5 must-haves verified` and `new_string` containing the full block above.

**Critical do-NOTs (Pitfall 3 in 11-RESEARCH.md):**
- Do NOT change `status: human_needed` to any other value. The valid enum is
  `{passed, gaps_found, human_needed}` and Option B preserves `human_needed`.
- Do NOT modify or remove any of the 6 entries in the `human_verification:` list (lines 20-38).
  That list documents the deferred items; the new keys document the acceptance decision ABOUT
  those deferred items.
- Do NOT touch the body of the file — frontmatter only.
- Do NOT use emojis in the rationale text (project CLAUDE.md prohibition).
- Do NOT use the literal `×` Unicode character in the rationale; use ASCII `x` (the canonical
  harness logs use `21 passed (24.6s)` and the audit cites `×3 browsers ×2 runs`, but YAML
  multiline strings are safer with ASCII to avoid encoding gotchas).
  </action>

  <verify>
    <automated>
grep -q "^human_acceptance: indefinite" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md \
  && grep -q "^human_acceptance_recorded: 2026-04-28" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md \
  && grep -q "^human_acceptance_rationale:" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md \
  && grep -q "21 passed x 2" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md \
  && grep -q "^status: human_needed" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md \
  && test "$(grep -c "^  - test:" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md)" = "6"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "^human_acceptance: indefinite" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md` exits 0
    - `grep -q "^human_acceptance_recorded: 2026-04-28" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md` exits 0
    - `grep -q "^human_acceptance_rationale:" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md` exits 0
    - `grep -q "21 passed x 2" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md` exits 0 (canonical harness evidence cited)
    - `grep -q "^status: human_needed" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md` exits 0 (status enum unchanged — Pitfall 3 mitigation)
    - `grep -c "^  - test:" .planning/phases/07-market-data-trading-ui/07-VERIFICATION.md` outputs `6` (human_verification list intact)
  </acceptance_criteria>

  <done>
    07-VERIFICATION.md frontmatter records the acceptance decision with three new sibling keys.
    `status:` enum still `human_needed`. `human_verification:` list intact (6 items). G3 closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add human_acceptance frontmatter block to 09-VERIFICATION.md</name>
  <files>.planning/phases/09-dockerization-packaging/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-dockerization-packaging/09-VERIFICATION.md (current frontmatter lines 1-20 — 4-item human_verification list)
    - .planning/phases/11-milestone-v1.0-closure/11-PATTERNS.md (lines 346-397: full pattern map for Phase 9 frontmatter mutation; Phase-9-specific Option B rationale)
    - .planning/phases/11-milestone-v1.0-closure/11-RESEARCH.md (lines 169-194: Pattern 3; Pitfall 3 schema constraints)
  </read_first>

  <action>
Same mutation as Task 1, applied to 09-VERIFICATION.md with Phase-9-specific rationale text.
Insertion point is **immediately after** `status: human_needed` (line 4) — the new block
sits BEFORE `score:`.

**The insertion (verbatim YAML):**

```yaml
status: human_needed
human_acceptance: indefinite
human_acceptance_recorded: 2026-04-28
human_acceptance_rationale: |
  All 4 human_verification items are platform/visual checks (Windows pwsh runtime,
  macOS browser auto-open, visual UI of the live terminal, cross-arch buildx). The
  structural validation in 09-VERIFICATION.md SC#1 through SC#11 passes (Dockerfile
  invariants, .dockerignore exclusions, .env.example shape, idempotent scripts,
  integration-test PASS on the canonical run command). The macOS/Linux demo path is
  fully proven; the Windows pwsh runtime, browser auto-open, and visual UI are
  deferred to per-host human spot-check. Recorded here as accepted policy debt for
  v1.0 milestone closure (Phase 11 G4).
score: 11/11 must-haves verified
```

Use the Edit tool with `old_string` matching `status: human_needed\nscore: 11/11 must-haves verified` and `new_string` containing the full block above.

**Critical do-NOTs (same as Task 1):**
- Do NOT change `status: human_needed` (Pitfall 3).
- Do NOT modify or remove any of the 4 entries in the `human_verification:` list (lines 7-19).
- Do NOT touch the body of the file — frontmatter only.
- Do NOT use emojis.
- Use ASCII characters in the rationale (no Unicode `×`, no em-dash `—` — use `-` or `:`).
  </action>

  <verify>
    <automated>
grep -q "^human_acceptance: indefinite" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md \
  && grep -q "^human_acceptance_recorded: 2026-04-28" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md \
  && grep -q "^human_acceptance_rationale:" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md \
  && grep -q "Windows pwsh" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md \
  && grep -q "^status: human_needed" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md \
  && test "$(grep -c "^  - test:" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md)" = "4"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "^human_acceptance: indefinite" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md` exits 0
    - `grep -q "^human_acceptance_recorded: 2026-04-28" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md` exits 0
    - `grep -q "^human_acceptance_rationale:" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md` exits 0
    - `grep -q "Windows pwsh" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md` exits 0 (Phase-9-specific rationale present)
    - `grep -q "^status: human_needed" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md` exits 0 (status enum unchanged)
    - `grep -c "^  - test:" .planning/phases/09-dockerization-packaging/09-VERIFICATION.md` outputs `4` (human_verification list intact)
  </acceptance_criteria>

  <done>
    09-VERIFICATION.md frontmatter records the acceptance decision with three new sibling keys.
    `status:` enum still `human_needed`. `human_verification:` list intact (4 items). G4 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Doc-only phase; no runtime code paths, no auth surface, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering (frontmatter schema integrity) | Phase 7 + Phase 9 VERIFICATION.md frontmatter | mitigate | Pitfall 3 mitigation: keep `status:` enum value `human_needed`; only ADD sibling keys (`human_acceptance:`, `human_acceptance_recorded:`, `human_acceptance_rationale:`); never invent new `status:` values. Pre-commit grep verifies the existing list lengths (6 for Phase 7, 4 for Phase 9) so the human_verification block is provably untouched. |

Doc-only phase; no production code paths; no other STRIDE categories apply.
</threat_model>

<verification>
- Both VERIFICATION.md files now expose `human_acceptance: indefinite` + recorded date + rationale.
- `status:` enum value unchanged in both (Pitfall 3 mitigation).
- `human_verification:` lists unchanged (Phase 7: 6 items; Phase 9: 4 items).
- Phase-7-specific rationale cites Phase 10 harness evidence (`21 passed x 2`).
- Phase-9-specific rationale cites Windows pwsh / browser auto-open / visual UI as deferred items.
- No file body modified.
</verification>

<success_criteria>
Phase 11 SC#3 (G3 — Phase 7) and SC#3 (G4 — Phase 9) closed: each phase's VERIFICATION.md
frontmatter records explicit, dated acceptance backed by canonical harness evidence. The
`/gsd-audit-milestone v1.0` re-run will classify these as `policy_debt`, not silent stuck
`human_needed`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-milestone-v1.0-closure/11-03-SUMMARY.md` with
the standard SUMMARY frontmatter (including `requirements-completed: [OPS-02, OPS-03]`) and
a body citing the two grep blocks plus a `git diff --stat` showing only the two
VERIFICATION.md files modified.
</output>
