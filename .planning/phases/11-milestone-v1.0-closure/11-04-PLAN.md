---
phase: 11-milestone-v1.0-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reports/MILESTONE_SUMMARY-v1.0.md
autonomous: true
requirements: [FE-01, FE-05, FE-06, APP-02]
tags: [milestone-closure, planning-delta, doc-only]

must_haves:
  truths:
    - "MILESTONE_SUMMARY-v1.0.md §6.3 (currently '— Post-milestone fixes not retrofitted into a phase') is rewritten so the closing line states the commits ARE recorded as planning deltas under Phase 11 (not 'if a v1.1 / Phase 10.1 is desired')"
    - "Both commit SHAs `73abc58` (Heatmap+PnLChart deterministic-height fix) and `e79ad18` (light theme switch) appear inside §6.3 with their fix description and harness-green status"
    - "The §6.3 closure line cites Phase 11 as the recording mechanism (e.g. 'Recorded as planning delta under Phase 11 — see .planning/phases/11-milestone-v1.0-closure/')"
    - "No new file is created (lightweight option chosen — no thin Phase 10.1 SUMMARY would break ROADMAP plan count and STATE.md invariants per 11-PATTERNS.md Pitfall 4)"
    - "§6.1 (Verification debt) closing line is updated to note Phase 11 closes G1/G3/G4 (so the v1.0 audit re-run reads the §6 block as resolved, not open)"
    - "§6.2 (REQUIREMENTS.md drift) closing line is updated to note Phase 11 closes G2"
    - "No other section of MILESTONE_SUMMARY-v1.0.md is altered (§1-§5, §6.4, §6.5, §7-§N untouched)"
  artifacts:
    - path: ".planning/reports/MILESTONE_SUMMARY-v1.0.md"
      provides: "Planning delta record for post-milestone source commits 73abc58 + e79ad18 (G5 closure); Phase 11 closure annotations on §6.1 + §6.2 + §6.3 (G1-G5 paper trail)"
      contains: "73abc58, e79ad18, 'Recorded as planning delta under Phase 11'"
  key_links:
    - from: "git log commits 73abc58 + e79ad18 (post-Phase-10 source-impacting commits)"
      to: "MILESTONE_SUMMARY-v1.0.md §6.3 planning delta block"
      via: "Inline §6.3 rewrite — no new files, no SUMMARY split"
      pattern: "§6.<digit> — <title>; commits cited inline with SHAs and harness status"
    - from: "v1.0-MILESTONE-AUDIT.md gaps G1, G2, G3, G4"
      to: "MILESTONE_SUMMARY-v1.0.md §6.1 + §6.2 closure annotations"
      via: "One closing line per subsection citing Phase 11 closure"
      pattern: "Closure annotation line at end of subsection"
---

<objective>
Close G5 from `.planning/v1.0-MILESTONE-AUDIT.md`: two post-milestone source commits
(`73abc58` — Heatmap + PnLChart deterministic-height fix for the Recharts 3.8.1 + React 19
`0×0` regression; and `e79ad18` — light theme switch) shipped AFTER Phase 10 was verified
passed. Both are green under the canonical Phase 10 harness, but neither has a planning
record. This plan adds that record by promoting `MILESTONE_SUMMARY-v1.0.md` §6.3 from
"deferred / would-need-a-Phase-10.1" to "recorded as planning delta under Phase 11".

Purpose: complete the milestone v1.0 paper trail so `/gsd-audit-milestone v1.0` re-run
classifies G5 as resolved. While editing the same file, also annotate §6.1 (verification
debt closed by Phase 11) and §6.2 (REQUIREMENTS.md drift closed by Phase 11) so the §6
block reads as a resolution log rather than an open backlog.

Output: one modified file — `.planning/reports/MILESTONE_SUMMARY-v1.0.md` — with §6.1,
§6.2, and §6.3 closing lines updated. No new files (per `11-PATTERNS.md` Pitfall 4: a thin
Phase 10.1 SUMMARY would break ROADMAP `Plans:` counts and STATE.md `completed_plans`
invariants).

Scope boundary: zero production source changes. Zero touch to §1-§5, §6.4 (verification
gaps to watch), §6.5 (out of scope), or §7+ (Getting Started, etc.). The body table at
§6.3 (commit / fix / why-it-surfaced) is preserved verbatim — only the closing line
("If a v1.1 / Phase 10.1 is desired...") is rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

@.planning/phases/11-milestone-v1.0-closure/11-RESEARCH.md
@.planning/phases/11-milestone-v1.0-closure/11-PATTERNS.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite MILESTONE_SUMMARY-v1.0.md §6.3 closing line; annotate §6.1 + §6.2 closure</name>
  <files>.planning/reports/MILESTONE_SUMMARY-v1.0.md</files>

  <read_first>
    - .planning/reports/MILESTONE_SUMMARY-v1.0.md (the file to edit — §6 starts around line 101; §6.1 line 103-105; §6.2 line 107-108; §6.3 line 110-118; §6.4 line 120 — DO NOT touch §6.4+)
    - .planning/v1.0-MILESTONE-AUDIT.md (frozen — read-only confirmation that G1-G5 are the gap names; do NOT edit)
    - .planning/phases/11-milestone-v1.0-closure/11-PATTERNS.md (the §6 numbering pattern and the "no new file" pitfall)
    - .planning/phases/11-milestone-v1.0-closure/11-RESEARCH.md (G5 closure rationale)
  </read_first>

  <action>
**Step 1 — Update §6.1 (Verification debt) closure line.**

Use the Edit tool to replace the §6.1 block. Match `old_string` exactly:

```
### 6.1 — Verification debt
- **Phase 5 (AI chat) has no VERIFICATION.md.** The chat works (proven by `06-chat.spec.ts` green across all 3 browsers + 16 Vitest tests in Phase 8) but no formal verifier pass exists. Run `/gsd-verify-work 5` to backfill.
- **Phase 7 (market-data trading UI) and Phase 9 (dockerization) have `status: human_needed`** in their VERIFICATION.md — automated checks pass but human acceptance was never recorded. Run `/gsd-verify-work 7` and `/gsd-verify-work 9`.
```

Replace with:

```
### 6.1 — Verification debt
- **Phase 5 (AI chat) has no VERIFICATION.md.** The chat works (proven by `06-chat.spec.ts` green across all 3 browsers + 16 Vitest tests in Phase 8) but no formal verifier pass exists. Run `/gsd-verify-work 5` to backfill.
- **Phase 7 (market-data trading UI) and Phase 9 (dockerization) have `status: human_needed`** in their VERIFICATION.md — automated checks pass but human acceptance was never recorded. Run `/gsd-verify-work 7` and `/gsd-verify-work 9`.

**Closure (2026-04-28):** Recorded as planning deltas under Phase 11 — see `.planning/phases/11-milestone-v1.0-closure/`. Phase 11 plan 11-01 backfills `05-VERIFICATION.md` (G1). Phase 11 plan 11-03 records explicit indefinite human acceptance with rationale on Phase 7 and Phase 9 VERIFICATION.md frontmatter (G3 + G4) — `status: human_needed` is preserved as the honest enum value; the new `human_acceptance:` sibling key carries the decision.
```

**Step 2 — Update §6.2 (REQUIREMENTS.md drift) closure line.**

Match `old_string`:

```
### 6.2 — REQUIREMENTS.md drift
15 of 40 requirements remain unticked despite the implementation existing and passing tests. Recommend a sweep pass to reconcile REQUIREMENTS.md with reality (see Section 4 table).
```

Replace with:

```
### 6.2 — REQUIREMENTS.md drift
15 of 40 requirements remain unticked despite the implementation existing and passing tests. Recommend a sweep pass to reconcile REQUIREMENTS.md with reality (see Section 4 table).

**Closure (2026-04-28):** Phase 11 plan 11-02 sweeps the 15 drift IDs (DB-01..03, PORT-05, WATCH-01..03, CHAT-01..06, FE-03/04/07/08/10, TEST-01) to `Complete (NN-MM, ...)` with plan-ID evidence. Coverage count remains 40/40 (math unchanged — the satisfied set was already complete; this is a status-line refresh, not new coverage). G2 closed.
```

**Step 3 — Rewrite §6.3 closing paragraph.**

Match `old_string`:

```
If a v1.1 / Phase 10.1 is desired, both commits can be retroactively assigned. Otherwise, surface them as pre-archive notes.
```

Replace with:

```
**Closure (2026-04-28):** Recorded as planning delta under Phase 11 — see `.planning/phases/11-milestone-v1.0-closure/`. Both commits remain green under the canonical harness (21 passed x 2 consecutive runs, 0 failed, 0 flaky, Container test-appsvc-1 Healthy) so no Phase 10.1 SUMMARY is created (would break ROADMAP plan count and STATE.md `completed_plans` invariants). The Section 6.3 table above is the authoritative paper trail. G5 closed.
```

**Critical do-NOTs:**
- Do NOT modify the table at §6.3 (lines 113-116) — Commit / Fix / Why-it-surfaced columns must be preserved verbatim.
- Do NOT touch §6.4 (Verification gaps to watch) or §6.5 (Out of scope).
- Do NOT touch §1-§5 or §7+.
- Do NOT renumber any section.
- Do NOT create any new file (no `10-10-SUMMARY.md`, no Phase 10.1 stub).
- Do NOT modify ROADMAP.md or STATE.md from this plan.
- Do NOT use emojis.
- Do NOT use Unicode `×`, em-dash `—`, or smart quotes in the new closure paragraphs — use ASCII `x`, `-`, and straight quotes for grep stability.
  </action>

  <verify>
    <automated>
grep -q "^### 6.3" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && grep -q "73abc58" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && grep -q "e79ad18" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && grep -q "Recorded as planning delta under Phase 11" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && grep -q "G5 closed" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && grep -q "G2 closed" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && grep -q "11-01 backfills" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && ! grep -q "If a v1.1 / Phase 10.1 is desired" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && grep -q "^### 6.4 — Verification gaps to watch" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && grep -q "^### 6.5 — Out of scope" .planning/reports/MILESTONE_SUMMARY-v1.0.md \
  && test "$(grep -c "^### 6\." .planning/reports/MILESTONE_SUMMARY-v1.0.md)" = "5"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "^### 6.3" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (§6.3 heading still present)
    - `grep -q "73abc58" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (Heatmap fix commit cited)
    - `grep -q "e79ad18" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (light-theme commit cited)
    - `grep -q "Recorded as planning delta under Phase 11" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (canonical closure phrase appears in §6.1 and §6.3)
    - `grep -q "G5 closed" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (G5 marked closed)
    - `grep -q "G2 closed" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (G2 marked closed in §6.2 annotation)
    - `grep -q "11-01 backfills" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (G1 closure cites plan 11-01 in §6.1 annotation)
    - `! grep -q "If a v1.1 / Phase 10.1 is desired" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (the deferred phrase is gone)
    - `grep -q "^### 6.4 — Verification gaps to watch" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (§6.4 untouched)
    - `grep -q "^### 6.5 — Out of scope" .planning/reports/MILESTONE_SUMMARY-v1.0.md` exits 0 (§6.5 untouched)
    - `grep -c "^### 6\." .planning/reports/MILESTONE_SUMMARY-v1.0.md` outputs `5` (still §6.1 through §6.5 — no renumbering, no new section added)
    - `test ! -e .planning/phases/10-e2e-validation/10-10-PLAN.md` passes (no rogue Phase 10.1 file created)
    - `git diff --name-only HEAD` lists only `.planning/reports/MILESTONE_SUMMARY-v1.0.md` (after the task commits, this should be the only file changed by this task)
  </acceptance_criteria>

  <done>
    §6.1, §6.2, and §6.3 of MILESTONE_SUMMARY-v1.0.md carry explicit Phase 11 closure
    annotations. The §6.3 table (commits + fixes) is preserved verbatim. Both commit SHAs
    (`73abc58`, `e79ad18`) appear inline. No new file created. §6.4+, §1-§5, and §7+ are
    untouched. G5 closed, with G1-G4 closure also annotated for paper-trail completeness.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Doc-only phase; no runtime code paths, no auth surface, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering (report integrity) | MILESTONE_SUMMARY-v1.0.md §6 substructure | mitigate | Anchor edits with exact-match `old_string` blocks (Edit tool) so renumbering is impossible. Pre-commit grep verifies (a) §6.1-§6.5 still exist and (b) `grep -c "^### 6\."` returns exactly 5. The §6.3 commit/fix table is preserved verbatim by anchoring `old_string` only on the closing paragraph, not the table rows. |

Doc-only phase; no production code paths; no other STRIDE categories apply.
</threat_model>

<verification>
- §6.1, §6.2, §6.3 each carry a `**Closure (2026-04-28):**` annotation citing Phase 11.
- §6.3 commits `73abc58` and `e79ad18` are cited inline with their fix descriptions; the table at lines ~113-116 is preserved verbatim.
- §6.4, §6.5, §1-§5, §7+ are untouched (grep confirms 5 §6.x subsections, no renumbering).
- No new file created (no `10-10-SUMMARY.md`, no Phase 10.1 stub).
- No production source changes.
- `git diff --name-only` lists only `.planning/reports/MILESTONE_SUMMARY-v1.0.md`.
</verification>

<success_criteria>
Phase 11 SC#4 (G5 — post-milestone commits have a planning record) closed. The §6 block of
MILESTONE_SUMMARY-v1.0.md now reads as a resolution log: each subsection (§6.1, §6.2, §6.3)
carries an explicit Phase 11 closure line citing the plan that closed it. The audit re-run
will classify G5 as resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/11-milestone-v1.0-closure/11-04-SUMMARY.md` with
standard SUMMARY frontmatter (including `requirements-completed: [FE-01, FE-05, FE-06, APP-02]`)
and a body that:
1. Cites the three §6.x annotations added (with the canonical phrases that grep matches).
2. Includes a `git diff --stat` excerpt showing only `MILESTONE_SUMMARY-v1.0.md` modified.
3. Notes that no new file was created (Pitfall 4 mitigation from 11-PATTERNS.md).
4. Confirms the §6.3 commit/fix table was preserved verbatim.
</output>
