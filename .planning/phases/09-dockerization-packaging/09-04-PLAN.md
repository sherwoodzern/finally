---
phase: 09
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - docs/DOCKER.md
  - README.md
autonomous: true
requirements: [OPS-02, OPS-04]
must_haves:
  truths:
    - "`docs/DOCKER.md` exists with seven sections: Quickstart, Canonical run, Image architecture, Volume, .env workflow, Troubleshooting, Windows."
    - "`README.md` Quick Start section is ≤ 10 non-comment lines and cites all four scripts (start_mac.sh, stop_mac.sh, start_windows.ps1, stop_windows.ps1)."
    - "`README.md` links to `docs/DOCKER.md` for long-form reference."
    - "Documentation accurately reflects the actual files shipped in Plans 09-01 (Dockerfile + .dockerignore), 09-02 (.env.example), and 09-03 (four scripts)."
  artifacts:
    - path: "docs/DOCKER.md"
      provides: "Long-form Docker reference: build, run, volumes, env workflow, troubleshooting, Windows variants."
      min_lines: 80
      contains: "## Quickstart, ## Canonical run, ## Image architecture, ## Volume, ## .env workflow, ## Troubleshooting, ## Windows"
    - path: "README.md"
      provides: "10-line Quick Start; pointer to docs/DOCKER.md."
      contains: "scripts/start_mac.sh, scripts/start_windows.ps1, .env.example, docs/DOCKER.md"
  key_links:
    - from: "README.md Quick Start"
      to: "scripts/start_mac.sh and scripts/start_windows.ps1"
      via: "Copy-paste block at the top of README; shipped in Plan 09-03."
      pattern: "scripts/start_mac.sh|scripts/start_windows.ps1"
    - from: "README.md"
      to: "docs/DOCKER.md"
      via: "Markdown link to long-form reference."
      pattern: "docs/DOCKER.md"
    - from: "docs/DOCKER.md (Canonical run section)"
      to: "PLAN.md §11 + Dockerfile + the canonical `docker run -v finally-data:/app/db -p 8000:8000 --env-file .env finally`"
      via: "Verbatim invocation matching the scripts' arguments."
      pattern: "docker run -v finally-data:/app/db -p 8000:8000 --env-file .env"
---

<objective>
Document the Phase 9 outputs so a fresh user can run the demo with one command from the README, and a power user has a complete reference in `docs/DOCKER.md`. The README change is minimal (10-line Quick Start per D-14); the long-form lives in `docs/DOCKER.md` with seven canonical sections covering build, run, volumes, env workflow, troubleshooting, and Windows variants.

Purpose: ROADMAP SC#2 + SC#4 reinforcement (the docs are how users discover the canonical invocation and `.env` workflow). Closes the documentation legs of OPS-02 and OPS-04.

Output: `docs/DOCKER.md` (NEW), `README.md` (UPDATED — Quick Start verified + long-form link added).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dockerization-packaging/09-CONTEXT.md
@.planning/phases/09-dockerization-packaging/09-PATTERNS.md
@.planning/phases/09-dockerization-packaging/09-VALIDATION.md
@.planning/phases/09-dockerization-packaging/09-01-SUMMARY.md
@.planning/phases/09-dockerization-packaging/09-02-SUMMARY.md
@.planning/phases/09-dockerization-packaging/09-03-SUMMARY.md
@planning/PLAN.md
@README.md
@Dockerfile
@.env.example
@scripts/start_mac.sh

<interfaces>
<!-- The current README.md (50 lines) already has a Quick Start section at lines 16-23.
     This plan VERIFIES it points at the right scripts and ADDS a single docs/DOCKER.md link.
     Do NOT rewrite the entire README. -->

Current README.md (lines 16-23, verbatim from `cat README.md`):
```markdown
## Quick Start

```bash
cp .env.example .env     # add OPENROUTER_API_KEY
./scripts/start_mac.sh   # or scripts/start_windows.ps1
```

Open http://localhost:8000. You start with a 10-ticker watchlist and $10,000 in virtual cash.
```

Required additions (D-14, minimal):
1. Confirm the four script paths are mentioned (start_mac, stop_mac, start_windows, stop_windows).
2. Add a single line linking to `docs/DOCKER.md` for long-form reference.
3. Optionally add a one-line stop hint.

Total Quick Start block must remain ≤ 10 NON-comment lines (VALIDATION row 09-06-02).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed docs -> public repo | Docs ship in version control; must contain no real secrets and no real customer data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Information Disclosure | Real API key pasted into a doc example | mitigate | All docs reference `OPENROUTER_API_KEY=` (empty) and `your-api-key-here` placeholders. Verification: `grep -E "sk-or-v1-|pk_test_" docs/DOCKER.md README.md` returns no matches. |
| T-09-12 | Tampering | Doc drifts from actual scripts/Dockerfile | accept | Docs are authored AFTER the scripts (Wave 3 depends on Wave 2); the planner reads the actual artifacts before writing. Drift is a maintenance concern caught by `/gsd-verify-work`. |
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Author docs/DOCKER.md (long-form Docker reference, 7 sections per D-14)</name>
  <files>docs/DOCKER.md</files>
  <read_first>
    - .planning/phases/09-dockerization-packaging/09-CONTEXT.md (D-14 section list; D-01..D-13 — every decision becomes a doc fact)
    - .planning/phases/09-dockerization-packaging/09-PATTERNS.md (`docs/DOCKER.md` constraint inventory — 7 required sections + tone guidance)
    - .planning/phases/09-dockerization-packaging/09-VALIDATION.md (row 09-06-01 — the 7-heading grep)
    - planning/PLAN.md §11 (canonical `docker run`, volume semantics)
    - Dockerfile (Plan 09-01) — describe what each stage does
    - .env.example (Plan 09-02) — describe each var
    - scripts/start_mac.sh (Plan 09-03) — Quickstart command
    - scripts/start_windows.ps1 (Plan 09-03) — Windows section
  </read_first>
  <action>
First create the `docs/` directory:

```bash
mkdir -p docs
```

Then author `docs/DOCKER.md` with the EXACT seven sections below (the grep on row 09-06-01 matches `^## (Quickstart|Canonical run|Image architecture|Volume|.env workflow|Troubleshooting|Windows)`). Write in the project's house voice: concise, no emojis, short paragraphs, code blocks for invocations.

```markdown
# FinAlly — Docker Reference

Long-form reference for the FinAlly Docker image. For the absolute minimum needed
to run the demo, see the **Quick Start** section in the top-level
[`README.md`](../README.md).

## Quickstart

From a fresh clone:

```bash
cp .env.example .env
./scripts/start_mac.sh        # macOS / Linux
# or
./scripts/start_windows.ps1   # Windows PowerShell 5.1+
```

Open <http://localhost:8000>. You start with a 10-ticker watchlist and
$10,000 in simulated cash. To stop the container while preserving your data:

```bash
./scripts/stop_mac.sh         # or .\scripts\stop_windows.ps1
```

## Canonical run

The four start/stop scripts wrap this single invocation, taken verbatim from
[`planning/PLAN.md`](../planning/PLAN.md) section 11:

```bash
docker run -d \
  --name finally-app \
  -v finally-data:/app/db \
  -p 8000:8000 \
  --env-file .env \
  finally:latest
```

Build the image first if you have not already:

```bash
docker build -t finally:latest .
```

After the container is running, you can inspect it directly:

```bash
docker logs -f finally-app                                # tail logs
curl -fsS http://localhost:8000/api/health                # health check
docker exec -it finally-app /bin/bash                     # shell inside the container
```

## Image architecture

The Dockerfile is multi-stage:

| Stage | Base image          | Purpose                                                       |
|-------|---------------------|---------------------------------------------------------------|
| 1     | `node:20-slim`      | `npm ci` then `npm run build` produces `frontend/out/`        |
| 2     | `python:3.12-slim`  | Installs `uv`, syncs `backend/uv.lock` (no dev extras), copies the frontend export from Stage 1 |

Inside the final image:

```
/app/
├── backend/        WORKDIR; uvicorn runs here; .venv lives here
│   └── app/
│       ├── lifespan.py   # parents[2] resolves to /app
│       └── main.py
├── frontend/
│   └── out/        # static export served by FastAPI's StaticFiles
└── db/             # named volume mount target (SQLite finally.db)
```

The runtime command is:

```
CMD ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

Notes:
- `uv` is installed via `COPY --from=ghcr.io/astral-sh/uv:0.9.26 /uv /uvx /bin/`
  (smaller and more reproducible than `pip install uv`).
- The image runs as `root`; this is a single-user localhost demo and the
  named volume avoids the permission edge cases that come with non-root
  ownership of bind-mounted directories on Docker Desktop.
- `STOPSIGNAL SIGINT` is set so `docker stop` triggers uvicorn's graceful
  shutdown directly.
- No `HEALTHCHECK` is declared — orchestration platforms (App Runner,
  Render, Fly.io) provide their own probes.

## Volume

The container declares `VOLUME /app/db` and the canonical run command attaches
the named volume `finally-data`:

```bash
-v finally-data:/app/db
```

`backend/app/lifespan.py` reads `DB_PATH` (set inside the image to
`/app/db/finally.db`) and lazily creates the SQLite file on first request.
The volume persists across `docker stop`, `docker rm`, and image rebuilds.

The stop scripts deliberately do **not** remove the volume:

```bash
./scripts/stop_mac.sh
# Stopped finally-app. Data preserved in volume 'finally-data'.
```

To wipe all data and start fresh:

```bash
./scripts/stop_mac.sh
docker volume rm finally-data
```

This is destructive — it removes your simulated portfolio, trade history,
watchlist edits, and chat history.

## .env workflow

The image is portable: secrets are **not** baked in. The `.env` file is read
at run time via `--env-file .env`. Three keys are documented:

| Variable               | Required?                          | What happens if empty                                                                 |
|------------------------|------------------------------------|---------------------------------------------------------------------------------------|
| `OPENROUTER_API_KEY`   | required for AI chat               | `/api/chat` returns 502; the rest of the terminal works (heatmap, P&L, trades, watchlist) |
| `MASSIVE_API_KEY`      | optional                           | The built-in market simulator runs (recommended for the demo)                          |
| `LLM_MOCK`             | optional (default `false`)         | Real LLM calls go out via OpenRouter; set to `true` for deterministic mock responses    |

Get an OpenRouter API key from <https://openrouter.ai/>. Edit `.env` and
restart the container:

```bash
./scripts/stop_mac.sh
./scripts/start_mac.sh
```

The image is **never** rebuilt to rotate a key — `.env` is read fresh on
every `docker run`.

## Troubleshooting

**Port 8000 already in use.**

```bash
# macOS / Linux
lsof -i :8000

# Windows PowerShell
Get-NetTCPConnection -LocalPort 8000
```

Stop the conflicting process or change the port mapping in
`scripts/start_mac.sh` (`PORT=8000` → another value, then re-run).

**`Error: .env not found`.**

The start scripts pre-flight-check for `.env`. Copy the example:

```bash
cp .env.example .env
```

**Image not found.**

```bash
./scripts/start_mac.sh --build
```

The `--build` flag forces a rebuild even when an older `finally:latest`
image is cached.

**Browser does not auto-open.**

Either `--no-open` was passed, or the OS browser launcher (`open` on macOS,
`xdg-open` on Linux, `Start-Process` on Windows) is unavailable. Visit
<http://localhost:8000> manually.

**Container starts but `/` returns 404.**

The static export from Stage 1 did not land in `/app/frontend/out/`. Confirm
with:

```bash
docker run --rm finally:latest test -f /app/frontend/out/index.html
```

If this exits non-zero, force a rebuild: `./scripts/start_mac.sh --build`.

**Volume reset (data loss).**

```bash
./scripts/stop_mac.sh
docker volume rm finally-data
./scripts/start_mac.sh
```

This wipes the SQLite database; the lifespan re-seeds the default user,
$10k cash, and the 10 default watchlist tickers on the next request.

## Windows

Every command above has a PowerShell 5.1+ equivalent:

| macOS / Linux                          | Windows PowerShell                          |
|----------------------------------------|---------------------------------------------|
| `cp .env.example .env`                 | `Copy-Item .env.example .env`               |
| `./scripts/start_mac.sh`               | `.\scripts\start_windows.ps1`               |
| `./scripts/start_mac.sh --build`       | `.\scripts\start_windows.ps1 -Build`        |
| `./scripts/start_mac.sh --no-open`     | `.\scripts\start_windows.ps1 -NoOpen`       |
| `./scripts/stop_mac.sh`                | `.\scripts\stop_windows.ps1`                |
| `lsof -i :8000`                        | `Get-NetTCPConnection -LocalPort 8000`      |
| `docker logs -f finally-app`           | `docker logs -f finally-app`                |
| `docker volume rm finally-data`        | `docker volume rm finally-data`             |

The PowerShell scripts target PowerShell 5.1 (Windows default) and 7.x. They
require Docker Desktop for Windows with WSL2 enabled (the standard install).
```

Critical invariants:
- All seven `## ` headings present, EXACTLY matching: `Quickstart`, `Canonical run`, `Image architecture`, `Volume`, `.env workflow`, `Troubleshooting`, `Windows` — verbatim casing for the row 09-06-01 grep.
- Internal markdown links use relative paths (`../README.md`, `../planning/PLAN.md`).
- No emojis (CLAUDE.md).
- No real API keys in any code block (T-09-11).
- Mention all four scripts (start_mac, stop_mac, start_windows, stop_windows) at least once.
- Mention `--build` and `--no-open` flags (and PowerShell `-Build`, `-NoOpen` switches).
- File ends with a single trailing newline.
  </action>
  <verify>
    <automated>
test -f docs/DOCKER.md && \
[ "$(grep -cE '^## (Quickstart|Canonical run|Image architecture|Volume|\.env workflow|Troubleshooting|Windows)$' docs/DOCKER.md)" = "7" ] && \
grep -q "docker run -d" docs/DOCKER.md && \
grep -q "finally-data:/app/db" docs/DOCKER.md && \
grep -q "8000:8000" docs/DOCKER.md && \
grep -q -- "--env-file .env" docs/DOCKER.md && \
grep -q "scripts/start_mac.sh" docs/DOCKER.md && \
grep -q "scripts/stop_mac.sh" docs/DOCKER.md && \
grep -q "scripts/start_windows.ps1" docs/DOCKER.md && \
grep -q "scripts/stop_windows.ps1" docs/DOCKER.md && \
grep -q -- "--build" docs/DOCKER.md && \
grep -q -- "--no-open" docs/DOCKER.md && \
grep -q "OPENROUTER_API_KEY" docs/DOCKER.md && \
grep -q "MASSIVE_API_KEY" docs/DOCKER.md && \
grep -q "LLM_MOCK" docs/DOCKER.md && \
! grep -qE "sk-or-v1-|pk_test_|sk_live_" docs/DOCKER.md && \
[ "$(wc -l < docs/DOCKER.md | tr -d ' ')" -ge "80" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION row 09-06-01: `grep -cE "^## (Quickstart|Canonical run|Image architecture|Volume|\.env workflow|Troubleshooting|Windows)$" docs/DOCKER.md` outputs `7`.
    - Canonical `docker run` invocation appears verbatim with all six args (per parity table).
    - All four script names cited at least once.
    - All three env vars (`OPENROUTER_API_KEY`, `MASSIVE_API_KEY`, `LLM_MOCK`) documented.
    - `--build` and `--no-open` flags documented.
    - PowerShell switches `-Build` and `-NoOpen` documented.
    - No real API keys in the file (T-09-11 closed).
    - File length ≥ 80 lines (sanity bound — 7 sections with examples is at least this long).
    - No emojis.
    - File ends with a single trailing newline.
  </acceptance_criteria>
  <done>`docs/DOCKER.md` exists with seven correctly-named sections, accurate references to the actual scripts/Dockerfile/.env.example shipped in Plans 09-01..03, and no leaked secrets.</done>
</task>

<task type="execute">
  <name>Task 2: Update README.md Quick Start (verify ≤ 10 lines + link to docs/DOCKER.md)</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/09-dockerization-packaging/09-CONTEXT.md (D-14: 10-line Quick Start, no expansion)
    - .planning/phases/09-dockerization-packaging/09-VALIDATION.md (row 09-06-02 — Quick Start ≤ 10 non-comment lines; row 09-06-03 — cite four scripts)
    - .planning/phases/09-dockerization-packaging/09-PATTERNS.md (`README.md` update inventory — minimal edit, do not rewrite)
    - README.md (existing, 50 lines; Quick Start at lines 16-23)
    - docs/DOCKER.md (Task 1 output)
  </read_first>
  <action>
The current README.md Quick Start section (lines 16-23) already cites two of the four scripts. Add a one-line link to `docs/DOCKER.md` and a one-line stop hint that cites the remaining two scripts. Total Quick Start content stays ≤ 10 non-comment, non-blank lines.

Use `Edit` to make the minimal change. Replace this block:

```markdown
## Quick Start

```bash
cp .env.example .env     # add OPENROUTER_API_KEY
./scripts/start_mac.sh   # or scripts/start_windows.ps1
```

Open http://localhost:8000. You start with a 10-ticker watchlist and $10,000 in virtual cash.
```

With this block:

```markdown
## Quick Start

```bash
cp .env.example .env                                      # add OPENROUTER_API_KEY
./scripts/start_mac.sh        # or .\scripts\start_windows.ps1
./scripts/stop_mac.sh         # or .\scripts\stop_windows.ps1 (when done)
```

Open http://localhost:8000. You start with a 10-ticker watchlist and $10,000 in virtual cash.

For the full Docker reference (image layout, volume semantics, troubleshooting, Windows variants), see [`docs/DOCKER.md`](docs/DOCKER.md).
```

Critical invariants:
- The new block contains references to ALL FOUR scripts: `scripts/start_mac.sh`, `scripts/stop_mac.sh`, `scripts/start_windows.ps1`, `scripts/stop_windows.ps1` (row 09-06-03).
- The link `[`docs/DOCKER.md`](docs/DOCKER.md)` is present.
- The Quick Start section content (between `## Quick Start` and the next `## ` heading) is ≤ 10 lines counting only NON-blank, NON-comment, NON-fence lines (per the awk in row 09-06-02). The block above has 4 prose/link lines + 3 in-fence command lines = 7 non-blank-non-fence lines including the prose sentences and the link line; well under 10.
- No emojis.
- No other section of README.md is modified.

Use the `Edit` tool — read README.md first, then make the targeted replacement.
  </action>
  <verify>
    <automated>
test -f README.md && \
grep -q "## Quick Start" README.md && \
grep -q "scripts/start_mac.sh" README.md && \
grep -q "scripts/stop_mac.sh" README.md && \
grep -q "scripts/start_windows.ps1" README.md && \
grep -q "scripts/stop_windows.ps1" README.md && \
grep -q "docs/DOCKER.md" README.md && \
[ "$(grep -c "scripts/start_mac.sh\|scripts/stop_mac.sh\|scripts/start_windows.ps1\|scripts/stop_windows.ps1" README.md)" -ge "4" ] && \
awk '/^## Quick Start/{flag=1; next} /^## /{flag=0} flag && NF && !/^```/ && !/^[[:space:]]*$/ {count++} END{exit (count <= 10) ? 0 : 1}' README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION row 09-06-02: README Quick Start non-blank, non-fence lines ≤ 10 (the awk command above enforces this; passes for the planned content).
    - VALIDATION row 09-06-03: `grep -c "scripts/start_mac.sh\|scripts/stop_mac.sh\|scripts/start_windows.ps1\|scripts/stop_windows.ps1" README.md` outputs ≥ 4 (one match per script name).
    - The README contains exactly one `## Quick Start` heading.
    - The README contains a markdown link to `docs/DOCKER.md`.
    - No other README section was modified (Stack, Environment, Project Layout, Status, License all intact — verified by reading the file before/after).
    - No emojis.
  </acceptance_criteria>
  <done>README.md Quick Start section updated to cite all four scripts and link to docs/DOCKER.md; section length still ≤ 10 lines; rest of README unchanged.</done>
</task>

</tasks>

<verification>
Wave 3 / docs gate:

```bash
# 1. docs/DOCKER.md has exactly the 7 required sections
[ "$(grep -cE '^## (Quickstart|Canonical run|Image architecture|Volume|\.env workflow|Troubleshooting|Windows)$' docs/DOCKER.md)" = "7" ]

# 2. docs/DOCKER.md cites all four scripts and the canonical docker run
grep -q "scripts/start_mac.sh" docs/DOCKER.md && \
grep -q "scripts/stop_mac.sh" docs/DOCKER.md && \
grep -q "scripts/start_windows.ps1" docs/DOCKER.md && \
grep -q "scripts/stop_windows.ps1" docs/DOCKER.md && \
grep -q "docker run -d" docs/DOCKER.md && \
grep -q "finally-data:/app/db" docs/DOCKER.md && \
grep -q -- "--env-file .env" docs/DOCKER.md

# 3. No leaked secrets in any doc
! grep -qE "sk-or-v1-|pk_test_|sk_live_" docs/DOCKER.md README.md

# 4. README Quick Start invariants
grep -q "## Quick Start" README.md && \
grep -q "docs/DOCKER.md" README.md && \
[ "$(grep -c "scripts/start_mac.sh\|scripts/stop_mac.sh\|scripts/start_windows.ps1\|scripts/stop_windows.ps1" README.md)" -ge "4" ] && \
awk '/^## Quick Start/{flag=1; next} /^## /{flag=0} flag && NF && !/^```/ && !/^[[:space:]]*$/ {count++} END{exit (count <= 10) ? 0 : 1}' README.md
```

All four blocks must exit 0 to close the phase.
</verification>

<success_criteria>
ROADMAP SC#2 documentation leg satisfied: `docs/DOCKER.md` documents the canonical `docker run` invocation that Plan 09-03's scripts wrap.

ROADMAP SC#4 documentation leg satisfied: `docs/DOCKER.md` and the README Quick Start both document the `cp .env.example .env` pattern.

OPS-02 fully closed (runtime + docs).

OPS-04 fully closed (`.env.example` from Plan 09-02 + this plan's docs explaining the workflow).
</success_criteria>

<output>
After completion, create `.planning/phases/09-dockerization-packaging/09-04-SUMMARY.md` containing:
- The exact `docs/DOCKER.md` content shipped (or a diff if the executor adjusted formatting).
- The before/after diff of README.md (just the Quick Start section).
- Output of all four verification grep blocks.
- A final phase-level checklist confirming all 26 rows in 09-VALIDATION.md per-task map are covered (cross-reference each row to the plan and task that addressed it).
- Any deviations and root-cause justification.
</output>
